---
phase: 01-foundation-data-runtime-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/domain/entities/data_source.py
  - app/application/datasource/schemas/datasource_schemas.py
  - app/application/datasource/handlers/create_datasource_handler.py
  - app/interfaces/api/v1/datasources.py
  - app/infrastructure/cache/table_cache_service.py
  - app/infrastructure/tasks/jobs/datasource_catalog_sync_job.py
  - app/infrastructure/scheduler.py
  - app/__init__.py
  - tests/unit/application/datasource/test_handler_coverage.py
  - tests/integration/test_datasource_api.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-05]
user_setup:
  - service: maxcompute
    why: "真实 MaxCompute 凭证只在内网环境可验证"
must_haves:
  truths:
    - 新建 PostgreSQL 或 MaxCompute 数据源后会记录目录同步摘要，并触发首次目录同步入队，不阻塞 HTTP 请求。
    - 数据源 API 会暴露目录同步摘要，至少包含 `status`、`last_run_at`、`last_error`、`tracked_databases`、`database_count`。
    - 平台存在统一固定周期的数据源目录同步注册点，并复用现有 APScheduler + RQ。
  artifacts:
    - app/infrastructure/tasks/jobs/datasource_catalog_sync_job.py
    - app/interfaces/api/v1/datasources.py
    - tests/unit/application/datasource/test_handler_coverage.py
  key_links:
    - create_datasource_handler 写入的 `extra_config.catalog_sync` 会被 datasources API 和前端数据源列表消费。
    - scheduler 注册点会调用 datasource catalog sync job 路径，而不是直接在 APScheduler 线程里做长耗时连接。
---

<objective>
把数据源侧的“连接可用”推进到“连接 + 目录刷新 + 可见状态”。

Purpose: Phase 1 的元数据同步必须先把 datasource 侧底座做稳，否则后续 dataset sync 只能靠页面内临时直连。
Output: 数据源目录同步状态模型、手动同步 API、首次同步入队、固定周期同步注册点、对应后端测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-runtime-stabilization/01-CONTEXT.md
@.planning/phases/01-foundation-data-runtime-stabilization/01-RESEARCH.md
@app/domain/entities/data_source.py
@app/application/datasource/handlers/create_datasource_handler.py
@app/interfaces/api/v1/datasources.py
@app/infrastructure/cache/table_cache_service.py
@app/infrastructure/scheduler.py
@app/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在数据源实体与响应契约中补齐目录同步摘要</name>
  <files>app/domain/entities/data_source.py, app/application/datasource/schemas/datasource_schemas.py, app/application/datasource/handlers/create_datasource_handler.py</files>
  <read_first>app/domain/entities/data_source.py, app/application/datasource/schemas/datasource_schemas.py, app/application/datasource/handlers/create_datasource_handler.py, .planning/phases/01-foundation-data-runtime-stabilization/01-CONTEXT.md</read_first>
  <action>在 `DataSource.extra_config` 下统一落 `catalog_sync` 摘要，不新增独立数据表。补充实体方法，精确维护以下键：`status`（`pending|syncing|synced|failed`）、`last_run_at`、`last_error`、`database_count`、`tracked_databases`。在 `CreateDatasourceHandler.handle()` 中为新建数据源初始化 `catalog_sync.status='pending'` 和空 `tracked_databases`；在 schema/序列化层保持这些键可直接透出给 API 调用方。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/datasource/test_handler_coverage.py</verify>
  <acceptance_criteria>
    - app/domain/entities/data_source.py contains `catalog_sync`
    - app/domain/entities/data_source.py contains `pending|syncing|synced|failed` 中至少 4 个状态值的处理逻辑
    - app/application/datasource/handlers/create_datasource_handler.py contains `catalog_sync`
    - app/application/datasource/schemas/datasource_schemas.py contains `catalog_sync` or `extra_config`
  </acceptance_criteria>
  <done>新建数据源后，后端响应已稳定包含目录同步摘要，且实体自身负责状态流转。</done>
</task>

<task type="auto">
  <name>Task 2: 实现首次同步与手动目录同步入队</name>
  <files>app/interfaces/api/v1/datasources.py, app/infrastructure/cache/table_cache_service.py, app/infrastructure/tasks/jobs/datasource_catalog_sync_job.py</files>
  <read_first>app/interfaces/api/v1/datasources.py, app/infrastructure/cache/table_cache_service.py, app/application/datasource/handlers/get_tables_handler.py, app/shared/exceptions.py, app/interfaces/api/middleware/error_handler.py</read_first>
  <action>新增 `app/infrastructure/tasks/jobs/datasource_catalog_sync_job.py`，定义 `execute_datasource_catalog_sync_job(datasource_id: int)`，执行顺序固定为：把 `catalog_sync.status` 置为 `syncing`，调用适配器列出 database，使用 `TableCacheService.get_cached_tables(..., force_refresh=True)` 刷新 `tracked_databases` 与当前返回数据库的并集，成功后回写 `catalog_sync.status='synced'`、`database_count`、`last_run_at`、清空 `last_error`；失败时回写 `status='failed'` 与 `last_error`。在 `datasources.py` 新增 `POST /api/v1/data-center/datasources/<id>/sync-catalog`，返回 `success(data={'job_id': ..., 'status': 'queued'}, message='目录同步已触发')`。在创建数据源成功后，直接通过现有 RQ 通用 `enqueue()` 方式投递第一次目录同步 job，不在请求里同步执行外部连接。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/integration/test_datasource_api.py</verify>
  <acceptance_criteria>
    - app/interfaces/api/v1/datasources.py contains `/sync-catalog`
    - app/interfaces/api/v1/datasources.py contains `job_id`
    - app/infrastructure/tasks/jobs/datasource_catalog_sync_job.py contains `execute_datasource_catalog_sync_job`
    - app/infrastructure/tasks/jobs/datasource_catalog_sync_job.py contains `tracked_databases`
  </acceptance_criteria>
  <done>首次目录同步和手动目录同步都已经后台化，HTTP 只负责入队与返回状态。</done>
</task>

<task type="auto">
  <name>Task 3: 把平台固定周期目录同步接入现有调度启动点并补测试</name>
  <files>app/infrastructure/scheduler.py, app/__init__.py, tests/unit/application/datasource/test_handler_coverage.py, tests/integration/test_datasource_api.py</files>
  <read_first>app/infrastructure/scheduler.py, app/__init__.py, docs/architecture/decisions/ADR-003-rq-for-tasks-and-events.md, app/application/services/app_center/scheduler_service.py, tests/unit/application/datasource/test_handler_coverage.py, tests/integration/test_datasource_api.py</read_first>
  <action>在 `app/infrastructure/scheduler.py` 增加平台级目录同步注册函数，固定使用 cron `0 2 * * *`，job id 为 `platform_datasource_catalog_sync`。该 job 只做“枚举所有活跃且类型属于 `postgresql|maxcompute` 的数据源并投递 RQ 目录同步任务”，不直接调用适配器。把该注册函数挂到现有 `init_jobs()` 里，保持与 app-center schedules 共存。同步扩展 `tests/unit/application/datasource/test_handler_coverage.py` 和 `tests/integration/test_datasource_api.py`，覆盖新状态字段、sync-catalog 路由和首次同步入队路径。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/datasource/test_handler_coverage.py tests/integration/test_datasource_api.py</verify>
  <acceptance_criteria>
    - app/infrastructure/scheduler.py contains `platform_datasource_catalog_sync`
    - app/infrastructure/scheduler.py contains `0 2 * * *`
    - app/__init__.py contains `init_jobs()`
    - tests/integration/test_datasource_api.py contains `sync-catalog`
  </acceptance_criteria>
  <done>目录同步已经具备首次触发、手动触发和平台固定周期触发三种入口，且测试能覆盖关键路径。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/datasource/test_handler_coverage.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/integration/test_datasource_api.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/integration/test_api_routes_smoke.py` still passes after新增 sync 路由
</verification>

<success_criteria>

- All tasks completed
- 数据源创建后不再依赖用户手工点击才能进入首次目录同步
- 数据源目录同步状态可以被前端稳定消费
- 固定周期目录同步复用 APScheduler + RQ，而不是新增执行底座
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-runtime-stabilization/01-01-SUMMARY.md`
</output>
