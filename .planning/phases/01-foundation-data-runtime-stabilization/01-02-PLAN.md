---
phase: 01-foundation-data-runtime-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/application/dataset/handlers/preview_dataset_handler.py
  - app/application/dataset/handlers/sync_schema_handler.py
  - app/application/dataset/schemas/dataset_schemas.py
  - app/application/dataset/services/dataset_metadata_refresh_service.py
  - app/infrastructure/tasks/jobs/dataset_sync_job.py
  - app/application/query/handlers/execute_sql_preview_handler.py
  - app/interfaces/api/v1/datasets.py
  - app/interfaces/api/v1/files.py
  - app/interfaces/api/middleware/error_handler.py
  - tests/unit/application/dataset/test_handler_coverage.py
  - tests/integration/test_dataset_api.py
  - tests/integration/test_api_routes_smoke.py
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04, DATA-05]
user_setup:
  - service: maxcompute
    why: "真实大表预览与 SQL 执行超时场景只能在内网凭证环境确认"
must_haves:
  truths:
    - 物理表预览返回字段识别、统计信息和 `LIMIT 20` 样本行。
    - `POST /api/v1/data-center/datasets/<id>/sync-schema` 对 physical、virtual、file 三种数据集都会入队后台刷新任务。
    - 预览和同步失败路径会返回可区分的 `error_code` / `details.reason_code`，供前端展示摘要与详情。
    - 文件上传支持 `csv/xls/xlsx`，但文件数据集仍然只能新建，不能覆盖旧对象。
  artifacts:
    - app/application/dataset/services/dataset_metadata_refresh_service.py
    - app/infrastructure/tasks/jobs/dataset_sync_job.py
    - app/interfaces/api/v1/files.py
  key_links:
    - preview_dataset_handler 复用表预览能力生成 `sample_rows`
    - dataset_sync_job 调用按类型分派的 metadata refresh service，而不是只支持 physical
---

<objective>
把三类数据集的后端运行模型补齐到同一个稳定层级。

Purpose: 没有 dataset 侧的统一 preview / sync / error contract，前端就无法完成统一骨架和失败可见性。
Output: 三类数据集统一的刷新入口、物理表样本预览、文件 Excel 支持、可分类的失败契约、对应测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-runtime-stabilization/01-CONTEXT.md
@.planning/phases/01-foundation-data-runtime-stabilization/01-RESEARCH.md
@app/application/dataset/handlers/preview_dataset_handler.py
@app/application/dataset/handlers/sync_schema_handler.py
@app/application/query/handlers/execute_sql_preview_handler.py
@app/interfaces/api/v1/datasets.py
@app/interfaces/api/v1/files.py
@app/interfaces/api/middleware/error_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一物理表预览与文件上传的样本预览契约</name>
  <files>app/application/dataset/handlers/preview_dataset_handler.py, app/application/dataset/schemas/dataset_schemas.py, app/interfaces/api/v1/files.py, app/application/query/handlers/execute_sql_preview_handler.py</files>
  <read_first>app/application/dataset/handlers/preview_dataset_handler.py, app/application/datasource/handlers/preview_table_data_handler.py, app/application/dataset/schemas/dataset_schemas.py, app/interfaces/api/v1/files.py, app/application/query/handlers/execute_sql_preview_handler.py, .planning/phases/01-foundation-data-runtime-stabilization/01-CONTEXT.md</read_first>
  <action>把物理表预览契约补齐为固定返回 `preview_limit=20`、`sample_rows`、`sample_columns`、`fields`、`statistics`、`table_info`。`PreviewDatasetHandler.handle()` 必须复用现有表预览能力获取真实样本行，而不是只返回 Schema。扩展 `app/interfaces/api/v1/files.py` 支持 `csv/xls/xlsx`，新增按扩展名分派到 `pandas.read_csv` / `pandas.read_excel` 的解析函数，且 preview 始终限制在 20 行。保留 SQL 预览现有主结构，但让 `ExecuteSQLPreviewHandler` 的失败路径能附带稳定的 `error_code/details`，以便虚拟数据集注册流程也能归入同一错误分类体系。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/dataset/test_handler_coverage.py</verify>
  <acceptance_criteria>
    - app/application/dataset/handlers/preview_dataset_handler.py contains `preview_limit`
    - app/application/dataset/handlers/preview_dataset_handler.py contains `sample_rows`
    - app/interfaces/api/v1/files.py contains `xlsx`
    - app/interfaces/api/v1/files.py contains `read_excel`
  </acceptance_criteria>
  <done>物理表和文件数据集都能返回 `LIMIT 20` 样本预览，虚拟数据集失败路径也能输出稳定错误分类。</done>
</task>

<task type="auto">
  <name>Task 2: 把数据集同步扩展为三类型统一的后台任务</name>
  <files>app/application/dataset/services/dataset_metadata_refresh_service.py, app/infrastructure/tasks/jobs/dataset_sync_job.py, app/application/dataset/handlers/sync_schema_handler.py, app/interfaces/api/v1/datasets.py</files>
  <read_first>app/application/dataset/handlers/sync_schema_handler.py, app/application/dataset/handlers/create_dataset_handler.py, app/domain/entities/dataset.py, app/interfaces/api/v1/datasets.py, app/infrastructure/tasks/task_queue.py, frontend/src/components/business/SaveAsDatasetDialog.tsx, frontend/src/pages/FileDatasetRegister.tsx</read_first>
  <action>新增 `DatasetMetadataRefreshService`，把刷新策略按 `dataset.dataset_type` 精确分派：`physical` 走表 schema + FieldIdentifier；`virtual` 走 SQL 预览字段重解析；`file` 走已保存 `file_metadata.file_path` 的重新解析。新增 `app/infrastructure/tasks/jobs/dataset_sync_job.py`，定义 `execute_dataset_sync_job(dataset_id)`，统一负责 `start_sync()` -> 执行刷新 -> `complete_sync()` / `fail_sync()`。把 `POST /api/v1/data-center/datasets/<id>/sync-schema` 改成后台入队接口，返回 `success(data={'job_id': ..., 'status': 'queued'}, message='元数据同步已触发')`，不再在请求线程里直接执行刷新。保留 `dataset.sync_status / last_sync_at / sync_error` 为唯一数据集同步真相。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/dataset/test_handler_coverage.py tests/integration/test_dataset_api.py</verify>
  <acceptance_criteria>
    - app/application/dataset/services/dataset_metadata_refresh_service.py contains `physical`
    - app/application/dataset/services/dataset_metadata_refresh_service.py contains `virtual`
    - app/application/dataset/services/dataset_metadata_refresh_service.py contains `file`
    - app/infrastructure/tasks/jobs/dataset_sync_job.py contains `execute_dataset_sync_job`
    - app/interfaces/api/v1/datasets.py contains `job_id`
  </acceptance_criteria>
  <done>三类数据集都拥有统一的后台刷新入口和一致的同步状态回写语义。</done>
</task>

<task type="auto">
  <name>Task 3: 固化失败分类并扩展后端回归覆盖</name>
  <files>app/interfaces/api/middleware/error_handler.py, tests/unit/application/dataset/test_handler_coverage.py, tests/integration/test_dataset_api.py, tests/integration/test_api_routes_smoke.py</files>
  <read_first>app/interfaces/api/middleware/error_handler.py, app/shared/exceptions.py, tests/unit/application/dataset/test_handler_coverage.py, tests/integration/test_dataset_api.py, tests/integration/test_api_routes_smoke.py, docs/quality/testing.md</read_first>
  <action>让 preview / sync 相关错误路径稳定返回 `error_code` 与 `details.reason_code`。需要时扩展 `handle_application_exception()`，确保 `details` 不再在 500 路径被吞掉；并在 dataset preview / sync / file parse 失败时统一使用明确的 `reason_code`，至少覆盖 `connection_failed`、`object_not_found`、`schema_fetch_failed`、`query_timeout`、`file_parse_failed`。同步扩展 unit / integration / smoke 测试，确保新 sync 路由、Excel 上传和失败分类响应不退回到“只有 message、没有可区分原因”的旧行为。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/dataset/test_handler_coverage.py tests/integration/test_dataset_api.py tests/integration/test_api_routes_smoke.py</verify>
  <acceptance_criteria>
    - app/interfaces/api/middleware/error_handler.py contains `details`
    - tests/integration/test_dataset_api.py contains `sync-schema`
    - tests/integration/test_api_routes_smoke.py contains `/api/v1/data-center/datasets`
    - tests/unit/application/dataset/test_handler_coverage.py contains `file` and `virtual`
  </acceptance_criteria>
  <done>前端已经可以稳定拿到可分类失败信息，且后端回归覆盖住新 sync / preview 契约。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/dataset/test_handler_coverage.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/integration/test_dataset_api.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/integration/test_api_routes_smoke.py` passes
</verification>

<success_criteria>

- All tasks completed
- 物理表、SQL 虚拟、文件数据集都可以走同一套后台同步状态语义
- 物理表与文件注册流程都具备 `LIMIT 20` 样本预览
- 错误响应至少能让前端区分五类失败原因
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-runtime-stabilization/01-02-SUMMARY.md`
</output>
