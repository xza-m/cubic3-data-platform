---
phase: 03-semantic-runtime-and-query-trust
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - frontend/tests/e2e-node/devtools-browse.spec.ts
  - frontend/tests/e2e-node/semantic.visual.spec.ts
  - docs/semantic_verification.md
  - docs/TECH_STACK_AND_ARCHITECTURE.md
  - docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md
  - docs/prd/semantic_layer_prd.md
autonomous: true
requirements: [RUN-05, QRY-02, QRY-03, QRY-04]
must_haves:
  truths:
    - 语义专项回归必须覆盖 `DevTools` 运行证据、历史回放和详情页跳转，不只停留在静态视觉截图。
    - 文档基线要明确 `DevTools` 是唯一正式运行入口，运行只服务调试与验证，真实消费发生在应用层。
    - Phase 3 不把语义中心表述成新的查询产品，也不把本地调试历史包装成平台级历史治理能力。
  artifacts:
    - frontend/tests/e2e-node/devtools-browse.spec.ts
    - frontend/tests/e2e-node/semantic.visual.spec.ts
    - docs/semantic_verification.md
    - docs/TECH_STACK_AND_ARCHITECTURE.md
    - docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md
    - docs/prd/semantic_layer_prd.md
  key_links:
    - 回归门槛和文档基线必须一起更新，否则 Phase 3 只会留下代码行为，没有稳定交付口径。
---

<objective>
把 Phase 3 的运行闭环收口成可回归、可说明、可交付的一套基线。

Purpose: 如果只完成实现而不补浏览器回归与文档回写，`DevTools` 的唯一入口边界很快会被后续页面演进重新打散。
Output: 语义专项浏览器回归、验证文档、架构 / PRD 回写，以及最终交付门槛。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-RESEARCH.md
@docs/semantic_verification.md
@docs/TECH_STACK_AND_ARCHITECTURE.md
@docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md
@docs/prd/semantic_layer_prd.md
@frontend/tests/e2e-node/devtools-browse.spec.ts
@frontend/tests/e2e-node/semantic.visual.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展语义专项浏览器回归，覆盖 DevTools 运行闭环与详情跳转</name>
  <files>frontend/tests/e2e-node/devtools-browse.spec.ts, frontend/tests/e2e-node/semantic.visual.spec.ts</files>
  <read_first>frontend/tests/e2e-node/devtools-browse.spec.ts, frontend/tests/e2e-node/semantic.visual.spec.ts, docs/semantic_verification.md</read_first>
  <action>扩展现有语义专项 Playwright 资产，不新造第二套 smoke。`devtools-browse.spec.ts` 至少新增五类断言：1) 进入编译调试页后能看到运行证据包关键区域；2) 执行一次调试后可见历史记录区；3) 历史记录存在回放入口；4) 若历史记录的 `definition_hash` 与当前对象 live 定义不同，页面会出现 stale 提示；5) 从 `View` 或 `Cube` 详情跳转到 `DevTools` 后，当前对象上下文和目标 `tab` 都仍然可见。`semantic.visual.spec.ts` 则继续固定 `DevTools`、`ViewDetail`、`CubeDetail` 的首屏视觉基线，避免运行摘要区和跳转 CTA 被后续改坏。</action>
  <verify>cd frontend && npm exec -- playwright test tests/e2e-node/devtools-browse.spec.ts tests/e2e-node/semantic.visual.spec.ts</verify>
  <acceptance_criteria>
    - frontend/tests/e2e-node/devtools-browse.spec.ts contains `devtools-tab-compiler`
    - frontend/tests/e2e-node/devtools-browse.spec.ts contains `回放` or `history`
    - frontend/tests/e2e-node/devtools-browse.spec.ts contains `stale` or `定义已变化`
    - frontend/tests/e2e-node/devtools-browse.spec.ts contains `tab=compiler` or `tab=sync`
    - frontend/tests/e2e-node/semantic.visual.spec.ts contains `semantic-devtools`
    - frontend/tests/e2e-node/semantic.visual.spec.ts contains `semantic-view-detail`
  </acceptance_criteria>
  <done>Phase 3 的运行闭环已经进入浏览器级回归门槛，不再只靠局部单测保证。</done>
</task>

<task type="auto">
  <name>Task 2: 回写语义验证、架构与 PRD 基线</name>
  <files>docs/semantic_verification.md, docs/TECH_STACK_AND_ARCHITECTURE.md, docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md, docs/prd/semantic_layer_prd.md</files>
  <read_first>docs/semantic_verification.md, docs/TECH_STACK_AND_ARCHITECTURE.md, docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md, docs/prd/semantic_layer_prd.md, .planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md</read_first>
  <action>同步更新四份文档，明确写出当前 Phase 3 基线：1) `DevTools` 是语义中心唯一正式运行入口；2) 运行能力只服务调试与验证，不是平台内真实消费入口；3) `ViewDetail / CubeDetail` 只展示最近运行摘要和跳转；4) 轻量调试历史只在 `DevTools` 内保留，不扩成平台级查询历史产品；5) `make verify-semantic` 与 `make test-regression-semantic` 的语义专项回归要覆盖 `DevTools` 运行证据与详情页跳转。不要把已延期的“平台级查询历史”写成当前能力。</action>
  <verify>make verify-docs</verify>
  <acceptance_criteria>
    - docs/semantic_verification.md contains `DevTools`
    - docs/semantic_verification.md contains `make test-regression-semantic`
    - docs/TECH_STACK_AND_ARCHITECTURE.md contains `运行只服务调试` or `DevTools`
    - docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md contains `唯一正式运行入口`
    - docs/prd/semantic_layer_prd.md contains `真实消费发生在应用层` or `DevTools`
  </acceptance_criteria>
  <done>实现、回归和文档基线已经对齐，Phase 3 的产品边界不再依赖口头解释。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `make test-regression-semantic` passes
- [ ] `make verify-semantic` passes
- [ ] `make verify-docs` passes
</verification>

<success_criteria>

- All tasks completed
- 浏览器回归能够覆盖 `DevTools` 运行证据、历史回放和详情页跳转
- 文档基线已经明确 Phase 3 的运行边界和唯一入口
- Phase 3 的交付口径不再依赖额外口头解释
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-runtime-and-query-trust/03-04-SUMMARY.md`
</output>
