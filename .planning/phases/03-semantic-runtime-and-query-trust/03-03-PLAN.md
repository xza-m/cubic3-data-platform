---
phase: 03-semantic-runtime-and-query-trust
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx
  - frontend/src/pages/Semantic/ViewDetail.tsx
  - frontend/src/pages/Semantic/CubeDetail.tsx
  - frontend/src/pages/Semantic/ViewDetail.page.test.tsx
  - frontend/src/pages/Semantic/CubeDetail.page.test.tsx
autonomous: true
requirements: [RUN-03, RUN-04, RUN-05, QRY-04]
must_haves:
  truths:
    - 物化 / 重新发布与漂移检测的正式入口全部收敛到 `DevTools`，详情页只保留最近状态摘要和跳转。
    - `ViewDetail` 与 `CubeDetail` 仍需展示最近发布、最近漂移、定义哈希等摘要，但不再承载正式运行动作。
    - `SchemaSyncTab` 负责承接当前对象的运行聚焦，不扩成独立运营看板。
    - 详情页跳转到 `DevTools` 时必须使用稳定深链参数，而不是仅跳到 `/semantic/tools` 首页；最小深链集合固定为 `kind`、`resource`、`file`（若适用）和 `tab`。
  artifacts:
    - frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx
    - frontend/src/pages/Semantic/ViewDetail.tsx
    - frontend/src/pages/Semantic/CubeDetail.tsx
    - frontend/src/pages/Semantic/ViewDetail.page.test.tsx
    - frontend/src/pages/Semantic/CubeDetail.page.test.tsx
  key_links:
    - Phase 3 的入口统一不是“删除摘要”，而是“让摘要只承担摘要职责，把正式动作送回 `DevTools`”。
    - 漂移检测和物化要继续复用现有 API 与状态字段，不新造第二套状态模型。
---

<objective>
把物化 / 漂移检测的操作入口收敛回 `DevTools`，同时保留详情页的状态可见性。

Purpose: 如果详情页继续保留直接运行动作，用户会持续在“对象详情页”和 `DevTools` 之间摇摆，Phase 3 的“唯一正式入口”就失效了。
Output: `SchemaSyncTab` 的对象聚焦、`ViewDetail / CubeDetail` 的摘要与跳转收敛，以及对应页面回归。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-RESEARCH.md
@frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx
@frontend/src/pages/Semantic/ViewDetail.tsx
@frontend/src/pages/Semantic/CubeDetail.tsx
@frontend/src/pages/Semantic/ViewDetail.page.test.tsx
@frontend/src/pages/Semantic/CubeDetail.page.test.tsx
@frontend/src/api/semantic.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 让 SchemaSyncTab 承接当前对象的漂移聚焦与 View 物化动作</name>
  <files>frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx</files>
  <read_first>frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx, frontend/src/api/semantic.ts, frontend/src/pages/Semantic/DevTools.tsx, .planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md</read_first>
  <action>扩展 `SchemaSyncTab.tsx`，让它在不依赖 `DevTools.tsx` 额外重构的前提下，能够基于当前 URL 或最小 props 感知所选对象，并把“当前对象运行摘要”放在顶部。最低要求：1) 对 `Cube / View` 继续高亮对应漂移项；2) 当当前对象是 `View` 时，顶部必须显示最近物化 / 发布摘要、定义哈希和执行入口，并复用现有 `materializeView` / `getMaterializeStatus` API；3) 保留全局 `立即检测` 按钮，但新增聚焦对象摘要区域，不把整个页面重做成运营看板；4) `SchemaSyncTab` 要与详情页深链 schema 对齐，至少支持 `kind=view|cube`、`resource=<object>`、`tab=sync` 这种进入方式。请显式新增 `data-testid`，例如 `schema-sync-focus-summary`、`view-materialize-trigger`、`view-materialize-status`，供后续组件测试使用。</action>
  <verify>cd frontend && npm run test:unit -- src/components/Semantic/DevTools/SchemaSyncTab.test.tsx</verify>
  <acceptance_criteria>
    - frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx contains `schema-sync-focus-summary`
    - frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx contains `view-materialize-trigger`
    - frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx contains `view-materialize-status`
    - frontend/src/components/Semantic/DevTools/SchemaSyncTab.tsx contains `definition_hash`
  </acceptance_criteria>
  <done>`SchemaSyncTab` 已经成为物化 / 漂移检测的正式工作面，用户不需要再回详情页执行运行操作。</done>
</task>

<task type="auto">
  <name>Task 2: 收敛 ViewDetail / CubeDetail 为“摘要 + 跳转”模式</name>
  <files>frontend/src/pages/Semantic/ViewDetail.tsx, frontend/src/pages/Semantic/CubeDetail.tsx, frontend/src/pages/Semantic/ViewDetail.page.test.tsx, frontend/src/pages/Semantic/CubeDetail.page.test.tsx</files>
  <read_first>frontend/src/pages/Semantic/ViewDetail.tsx, frontend/src/pages/Semantic/CubeDetail.tsx, frontend/src/pages/Semantic/ViewDetail.page.test.tsx, frontend/src/pages/Semantic/CubeDetail.page.test.tsx, .planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md</read_first>
  <action>把 `ViewDetail` 和 `CubeDetail` 收敛成摘要页，而不是正式运行页。最低要求：1) `ViewDetail` 不再保留直接物化 / 重新发布按钮，改为跳转到 `DevTools` 深链的明确 CTA，URL 形状至少包含 `?kind=view&resource=<view_name>&file=<view_name>&tab=sync`，并继续展示最近发布状态、最近漂移状态、最近检测时间与 `definition_hash`；2) `CubeDetail` 继续展示最近漂移摘要和定义哈希，但主要 CTA 改成跳转到 `DevTools` 深链，URL 形状至少覆盖 `?resource=<cube_name>&file=<cube_name>&tab=compiler` 或 `tab=sync`；3) 两个页面都要显式说明“完整运行证据请到 DevTools 查看”。更新页面测试，直接断言新的跳转按钮、深链参数和摘要文案存在。</action>
  <verify>cd frontend && npm run test:unit -- src/pages/Semantic/ViewDetail.page.test.tsx src/pages/Semantic/CubeDetail.page.test.tsx</verify>
  <acceptance_criteria>
    - frontend/src/pages/Semantic/ViewDetail.tsx contains `/semantic/tools?kind=view`
    - frontend/src/pages/Semantic/ViewDetail.tsx contains `tab=sync`
    - frontend/src/pages/Semantic/ViewDetail.tsx contains `完整运行证据`
    - frontend/src/pages/Semantic/CubeDetail.tsx contains `/semantic/tools`
    - frontend/src/pages/Semantic/CubeDetail.tsx contains `tab=compiler` or `tab=sync`
    - frontend/src/pages/Semantic/CubeDetail.tsx contains `完整运行证据`
    - frontend/src/pages/Semantic/ViewDetail.page.test.tsx contains `kind=view`
    - frontend/src/pages/Semantic/ViewDetail.page.test.tsx contains `tab=sync`
    - frontend/src/pages/Semantic/CubeDetail.page.test.tsx contains `/semantic/tools`
  </acceptance_criteria>
  <done>详情页已经回到“可见摘要 + 可跳转”角色，正式运行入口只剩 `DevTools`。</done>
</task>

<task type="auto">
  <name>Task 3: 锁定运行摘要与跳转的页面回归</name>
  <files>frontend/src/pages/Semantic/ViewDetail.page.test.tsx, frontend/src/pages/Semantic/CubeDetail.page.test.tsx</files>
  <read_first>frontend/src/pages/Semantic/ViewDetail.page.test.tsx, frontend/src/pages/Semantic/CubeDetail.page.test.tsx, frontend/src/pages/Semantic/ViewDetail.tsx, frontend/src/pages/Semantic/CubeDetail.tsx</read_first>
  <action>扩展 `ViewDetail.page.test.tsx` 与 `CubeDetail.page.test.tsx`，至少锁定三类行为：1) 最近发布 / 漂移 / 定义哈希摘要仍然可见；2) 正式运行 CTA 指向带完整 query params 的 `DevTools` 深链；3) 页面中不再暴露会误导用户的直接运行动作。不要只检查按钮数量，直接断言目标链接、`kind/resource/file/tab` 参数、摘要区文案和关键 `data-testid`。</action>
  <verify>cd frontend && npm run test:unit -- src/pages/Semantic/ViewDetail.page.test.tsx src/pages/Semantic/CubeDetail.page.test.tsx</verify>
  <acceptance_criteria>
    - frontend/src/pages/Semantic/ViewDetail.page.test.tsx contains `完整运行证据`
    - frontend/src/pages/Semantic/ViewDetail.page.test.tsx contains `definition_hash` or `最近发布`
    - frontend/src/pages/Semantic/CubeDetail.page.test.tsx contains `完整运行证据`
    - frontend/src/pages/Semantic/CubeDetail.page.test.tsx contains `drift` or `最近检测`
  </acceptance_criteria>
  <done>Phase 3 的入口统一有了页面级回归门槛，不会在后续 UI 调整里重新散掉。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test:unit -- src/components/Semantic/DevTools/SchemaSyncTab.test.tsx` passes
- [ ] `cd frontend && npm run test:unit -- src/pages/Semantic/ViewDetail.page.test.tsx src/pages/Semantic/CubeDetail.page.test.tsx` passes
- [ ] `cd frontend && npm exec -- playwright test tests/e2e-node/devtools-browse.spec.ts tests/e2e-node/semantic.visual.spec.ts` passes
</verification>

<success_criteria>

- All tasks completed
- 物化 / 漂移检测的正式入口全部收敛到 `DevTools`
- `ViewDetail / CubeDetail` 只保留摘要和跳转，不再承担正式运行职责
- 运行摘要和跳转链路已经被页面测试与浏览器回归锁住
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-runtime-and-query-trust/03-03-SUMMARY.md`
</output>
