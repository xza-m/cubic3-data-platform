---
phase: 03-semantic-runtime-and-query-trust
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/api/semantic.ts
  - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx
  - frontend/src/pages/Semantic/DevTools.tsx
  - frontend/src/components/Semantic/DevTools/CompileDebugTab.test.tsx
  - frontend/src/pages/Semantic/DevTools.page.test.tsx
autonomous: true
requirements: [RUN-01, RUN-02, RUN-05, QRY-01, QRY-02, QRY-03]
must_haves:
  truths:
    - `DevTools` 是 Phase 3 唯一正式运行入口；编译、执行和回放都收敛在这里，不新开第二个查询工作台。
    - 调试历史保持轻量、本地、面向回放；不调用新的后端历史接口，也不把历史做成全局查询产品。
    - 历史回放若发现历史记录的 `definition_hash` 与当前 live 对象不一致，前端必须显式提示该记录已 stale，而不是静默覆盖或假装结果必然一致。
    - 运行结果展示必须完整消费 Phase 3 证据包：SQL、对象摘要、样本结果、行数、耗时、错误分类 / hint、定义版本标识。
  artifacts:
    - frontend/src/api/semantic.ts
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx
    - frontend/src/pages/Semantic/DevTools.tsx
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.test.tsx
    - frontend/src/pages/Semantic/DevTools.page.test.tsx
  key_links:
    - `CompileDebugTab` 是 Phase 3 运行闭环的主交互面，`DevTools` 页面负责把当前对象上下文传给它。
    - `PlaygroundTab` 里的已有执行逻辑可复用思路，但不应继续形成第二套交互入口。
---

<objective>
把 `DevTools` 的编译 / 执行 / 历史回放收敛成一条可解释、可回放、可复现的调试链。

Purpose: 后端契约就绪后，真正决定用户能否“信任这次调试”的，是 `DevTools` 是否能把证据包、历史和回放组织成一条稳定路径，而不是只显示一段 SQL。
Output: 前端运行类型对齐、`CompileDebugTab` 扩展、轻量本地历史与组件 / 页面回归。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-RESEARCH.md
@frontend/src/api/semantic.ts
@frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx
@frontend/src/components/Semantic/DevTools/PlaygroundTab.tsx
@frontend/src/pages/Semantic/DevTools.tsx
@frontend/src/pages/Semantic/DevTools.page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 对齐前端运行类型，完整消费 Phase 3 证据包</name>
  <files>frontend/src/api/semantic.ts</files>
  <read_first>frontend/src/api/semantic.ts, app/interfaces/api/v1/semantic.py, .planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md</read_first>
  <action>更新 `frontend/src/api/semantic.ts` 中编译 / 查询相关类型，至少补齐 `definition_hash`、`sample_rows`、`error_code`、`hint`、`execution_time_ms`、`row_count` 等字段，并保留现有兼容字段。需要为 `CompileDebugTab` 准备一个显式的历史记录类型，例如 `SemanticDebugHistoryItem`，其中最少包含对象标识、DSL 快照、SQL、样本摘要、执行时间、错误信息和定义哈希；同时为“当前 live 定义哈希”和“历史记录是否 stale”预留类型位。不要为了 Phase 3 在 API 层新增“查询历史”调用函数。</action>
  <verify>cd frontend && npm run test:unit -- src/pages/Semantic/DevTools.page.test.tsx</verify>
  <acceptance_criteria>
    - frontend/src/api/semantic.ts contains `definition_hash`
    - frontend/src/api/semantic.ts contains `sample_rows`
    - frontend/src/api/semantic.ts contains `error_code`
    - frontend/src/api/semantic.ts contains `SemanticDebugHistoryItem`
  </acceptance_criteria>
  <done>前端已经有稳定类型可消费运行证据包，不再依赖临时 `any` 或页面内自行猜字段。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 CompileDebugTab，补齐执行证据包、历史与回放</name>
  <files>frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx, frontend/src/components/Semantic/DevTools/CompileDebugTab.test.tsx</files>
  <read_first>frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx, frontend/src/components/Semantic/DevTools/PlaygroundTab.tsx, frontend/src/api/semantic.ts, .planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md</read_first>
  <action>把 `CompileDebugTab` 从“只编译”升级为“编译 + 执行 + 证据包 + 历史回放”工作面，但不新建第二个页面。最低要求：1) 在现有“编译”之外新增“执行”动作，直接基于当前 DSL 调用语义查询；2) 结果区必须稳定展示 SQL、主对象 / 关联对象、`sample_rows` 样本、`row_count`、`execution_time_ms`、`error_code` / `hint`、`definition_hash`；3) 使用浏览器本地存储保留最近 10 条以内调试历史，键名自定但必须显式常量化，并提供明确的 `清空历史` 动作；4) 每条历史记录都能“一键回放”回当前 DSL 编辑区并恢复对象摘要；5) 回放前若历史记录的 `definition_hash` 与当前 live 定义不一致，必须显示 stale 提示，但允许用户继续回放；6) 失败记录也要进入历史。请显式新增 `frontend/src/components/Semantic/DevTools/CompileDebugTab.test.tsx`，覆盖成功执行、失败执行、历史回放、stale 提示以及本地存储上限 / 清空行为。</action>
  <verify>cd frontend && npm run test:unit -- src/components/Semantic/DevTools/CompileDebugTab.test.tsx</verify>
  <acceptance_criteria>
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx contains `sample_rows`
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx contains `definition_hash`
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx contains `error_code`
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx contains `localStorage`
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx contains `回放`
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx contains `清空历史`
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx contains `stale` or `定义已变化`
    - frontend/src/components/Semantic/DevTools/CompileDebugTab.test.tsx exists
  </acceptance_criteria>
  <done>`DevTools` 已经形成完整的调试闭环，用户可以在同一工作面编译、执行、查看证据并回放历史。</done>
</task>

<task type="auto">
  <name>Task 3: 让 DevTools 页面显式传递当前对象上下文，并锁定壳层行为</name>
  <files>frontend/src/pages/Semantic/DevTools.tsx, frontend/src/pages/Semantic/DevTools.page.test.tsx</files>
  <read_first>frontend/src/pages/Semantic/DevTools.tsx, frontend/src/pages/Semantic/DevTools.page.test.tsx, frontend/src/components/Semantic/DevTools/CompileDebugTab.tsx</read_first>
  <action>更新 `DevTools.tsx`，把当前选中的对象类型、对象代码以及当前 live `definition_hash` 等最小上下文显式传入 `CompileDebugTab`，避免该组件依赖隐式全局状态。页面层要继续保证：切换资源后当前 tab 不丢失；历史和执行状态只跟当前工作区展示相关，不把 `DevTools` 重新做成独立列表页。更新 `DevTools.page.test.tsx`，显式断言编译调试标签可挂载真实 `CompileDebugTab` 关键占位，并覆盖“切换对象仍保留 compiler tab”的行为。</action>
  <verify>cd frontend && npm run test:unit -- src/pages/Semantic/DevTools.page.test.tsx src/components/Semantic/DevTools/CompileDebugTab.test.tsx</verify>
  <acceptance_criteria>
    - frontend/src/pages/Semantic/DevTools.tsx contains `selectedResource`
    - frontend/src/pages/Semantic/DevTools.tsx contains `devtools-tab-compiler`
    - frontend/src/pages/Semantic/DevTools.page.test.tsx contains `devtools-tab-compiler`
    - frontend/src/pages/Semantic/DevTools.page.test.tsx contains `CompileDebugTab` or `编译调试`
  </acceptance_criteria>
  <done>`DevTools` 页面的壳层职责和运行标签职责已经明确分离，历史回放不再依赖页面隐式状态。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test:unit -- src/components/Semantic/DevTools/CompileDebugTab.test.tsx` passes
- [ ] `cd frontend && npm run test:unit -- src/pages/Semantic/DevTools.page.test.tsx` passes
- [ ] `cd frontend && npm run test:unit -- src/components/Semantic/DevTools/CompileDebugTab.test.tsx src/pages/Semantic/DevTools.page.test.tsx` passes
</verification>

<success_criteria>

- All tasks completed
- `DevTools` 可以在单一工作面完成编译、执行、查看证据包与历史回放
- 调试历史不依赖后端持久化，也不依赖当前页面的隐式局部状态
- `DevTools` 继续是运行入口，而不是新长出一个查询产品
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-runtime-and-query-trust/03-02-SUMMARY.md`
</output>
