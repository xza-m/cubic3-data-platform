---
phase: 03-semantic-runtime-and-query-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/application/semantic/semantic_query_service.py
  - app/interfaces/api/v1/semantic.py
  - tests/unit/application/semantic/test_semantic_query_service.py
  - tests/integration/test_semantic_api.py
autonomous: true
requirements: [RUN-01, RUN-02, RUN-05, QRY-01, QRY-02]
must_haves:
  truths:
    - Phase 3 的运行 API 只服务 `DevTools` 调试，不扩成第二个查询产品，也不新增查询历史后端。
    - 编译与查询结果必须暴露稳定的调试证据字段：`sql`、`primary_cube`、`joined_cubes`、`definition_hash`、`error_code`、`hint`；查询成功时还要给出 `sample_rows`、`row_count`、`execution_time_ms`。
    - `error_code` 必须来自固定的最小错误目录，而不是自由字符串；至少要区分“用户可修复 DSL / 校验问题”“上游数据源问题”“平台内部编译 / 运行问题”三类。
    - 兼容性优先：保留现有 `columns`、`data`、`retryable` 等字段，新增字段只做补充，不破坏已有调用方。
  artifacts:
    - app/application/semantic/semantic_query_service.py
    - app/interfaces/api/v1/semantic.py
    - tests/unit/application/semantic/test_semantic_query_service.py
    - tests/integration/test_semantic_api.py
  key_links:
    - `SemanticQueryService` 负责把编译/查询结果正规化成证据包，接口层只做薄包装。
    - Phase 3 的“查询可信”建立在可解释字段和版本标识上，而不是建立在新页面或新存储上。
---

<objective>
把已有编译 / 查询后端输出收敛成稳定的语义调试证据包。

Purpose: 如果后端返回值继续只有“能跑通”的松散结构，前端即使补了历史和回放，也仍然缺乏错误分类、定义版本和稳定样本，无法支撑真正的查询可信。
Output: 标准证据包字段、兼容 API 输出与对应 unit / integration 回归。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md
@.planning/phases/03-semantic-runtime-and-query-trust/03-RESEARCH.md
@app/application/semantic/semantic_query_service.py
@app/interfaces/api/v1/semantic.py
@app/application/semantic/semantic_definition_service.py
@tests/unit/application/semantic/test_semantic_query_service.py
@tests/integration/test_semantic_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把编译与查询输出正规化成稳定的调试证据包</name>
  <files>app/application/semantic/semantic_query_service.py, tests/unit/application/semantic/test_semantic_query_service.py</files>
  <read_first>app/application/semantic/semantic_query_service.py, app/application/semantic/semantic_definition_service.py, .planning/phases/03-semantic-runtime-and-query-trust/03-CONTEXT.md, tests/unit/application/semantic/test_semantic_query_service.py</read_first>
  <action>在 `SemanticQueryService` 中新增统一的运行结果构造 helper（命名可自定，但必须是显式 helper，而不是把字段零散散落在主流程里），让 `compile_query()` 与 `query()` 输出统一调试证据字段。最低要求：1) 编译成功时返回 `sql`、`primary_cube`、`joined_cubes`、`definition_hash`；2) 查询成功时在保留 `columns`、`data` 的同时，新增 `sample_rows`（固定前 20 行样本）、`row_count`、`execution_time_ms`、`definition_hash`；3) 编译失败和执行失败都要给出稳定的 `error_code`，且该字段必须来自固定 helper / 常量目录，而不是自由拼接字符串；最小分类至少覆盖 `dsl_parse_error`、`dsl_validation_error`、`upstream_query_error`、`runtime_execute_error` 或等价集合，并能稳定映射为“用户可修复 / 上游问题 / 平台问题”三类；4) 如果失败发生在 SQL 已生成之后，响应中仍需保留 `sql` 以便前端排查。版本标识优先复用现有 `definition_hash` 计算或摘要逻辑，不新建数据库表或查询历史模型。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_semantic_query_service.py</verify>
  <acceptance_criteria>
    - app/application/semantic/semantic_query_service.py contains `sample_rows`
    - app/application/semantic/semantic_query_service.py contains `definition_hash`
    - app/application/semantic/semantic_query_service.py contains `error_code`
    - app/application/semantic/semantic_query_service.py contains `dsl_parse_error` or `compile_error`
    - app/application/semantic/semantic_query_service.py contains `upstream_query_error` or `execute_error`
    - tests/unit/application/semantic/test_semantic_query_service.py contains `upstream_query_error` or `dsl_validation_error`
    - tests/unit/application/semantic/test_semantic_query_service.py contains `sample_rows`
  </acceptance_criteria>
  <done>后端已经稳定输出足以支撑调试证据包的字段集合，前端不需要再从松散响应里拼凑事实。</done>
</task>

<task type="auto">
  <name>Task 2: 在语义 API 中保持兼容并暴露统一失败细节</name>
  <files>app/interfaces/api/v1/semantic.py, tests/integration/test_semantic_api.py</files>
  <read_first>app/interfaces/api/v1/semantic.py, app/application/semantic/semantic_query_service.py, tests/integration/test_semantic_api.py</read_first>
  <action>收敛 `POST /api/v1/semantic/compile` 与 `POST /api/v1/semantic/query` 的输出约定：成功时透传新的证据字段；失败时继续使用现有错误响应封装，但 `details` 中必须含有 `error_code`、`hint`、`definition_hash`（若可得）与 `sql`（若已生成）。同时在接口层或服务层测试中锁定“错误码来自固定目录而不是自由字符串”的约束。不要为 Phase 3 新增独立的“运行历史”或“调试回放” API；`DevTools` 的历史仍应建立在前端侧轻量存储之上。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/integration/test_semantic_api.py</verify>
  <acceptance_criteria>
    - app/interfaces/api/v1/semantic.py contains `error_code`
    - app/interfaces/api/v1/semantic.py contains `definition_hash`
    - tests/integration/test_semantic_api.py contains `sample_rows`
    - tests/integration/test_semantic_api.py contains `error_code`
  </acceptance_criteria>
  <done>接口层已经把调试证据包和失败分类稳定暴露出来，前后端对契约的理解一致。</done>
</task>

<task type="auto">
  <name>Task 3: 用回归锁定成功 / 失败 / 兼容场景</name>
  <files>tests/unit/application/semantic/test_semantic_query_service.py, tests/integration/test_semantic_api.py</files>
  <read_first>tests/unit/application/semantic/test_semantic_query_service.py, tests/integration/test_semantic_api.py, app/application/semantic/semantic_query_service.py, app/interfaces/api/v1/semantic.py</read_first>
  <action>补齐三类断言：1) 成功查询会返回 `sample_rows`、`row_count`、`execution_time_ms`、`definition_hash`；2) 编译或执行失败会返回稳定的 `error_code` 与 `hint`；3) 兼容字段 `columns`、`data`、`sql` 仍然保留。不要只断言 truthy，直接断言字段名和关键值。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_semantic_query_service.py tests/integration/test_semantic_api.py</verify>
  <acceptance_criteria>
    - tests/unit/application/semantic/test_semantic_query_service.py contains `definition_hash`
    - tests/unit/application/semantic/test_semantic_query_service.py contains `error_code`
    - tests/integration/test_semantic_api.py contains `columns`
    - tests/integration/test_semantic_api.py contains `data`
  </acceptance_criteria>
  <done>Phase 3 的运行证据包已经有直接回归门槛，不会在后续修改里悄悄退化。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_semantic_query_service.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/integration/test_semantic_api.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_semantic_query_service.py tests/integration/test_semantic_api.py` passes
</verification>

<success_criteria>

- All tasks completed
- 编译与查询都有稳定的调试证据字段，不再依赖页面侧临时推断
- 错误分类、hint 与定义版本标识可以直接支撑 `DevTools` 的可信展示
- Phase 3 没有引入新的后端查询历史或运行持久化中心
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-runtime-and-query-trust/03-01-SUMMARY.md`
</output>
