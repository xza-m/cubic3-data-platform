---
phase: 02-semantic-object-lifecycle-and-domain-directory
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - frontend/tests/e2e-node/semantic.visual.spec.ts
  - frontend/tests/e2e-node/domain-catalog.spec.ts
  - frontend/tests/e2e-node/domain-publish.spec.ts
  - frontend/tests/e2e-node/cube-browse.spec.ts
  - docs/semantic_verification.md
  - docs/TECH_STACK_AND_ARCHITECTURE.md
  - docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md
  - docs/prd/semantic_layer_prd.md
autonomous: true
requirements: [SEM-05, DOM-01, DOM-03, DOM-04]
must_haves:
  truths:
    - 语义专项回归与浏览器 smoke 会覆盖 Phase 2 新增的治理摘要和跨页导航，不只停留在单元测试绿色。
    - 文档会明确 `Domain.cubes[]` 是关系真相、`View` 是特殊 `Cube` 的展示定位、`Recipe` 是轻量消费对象。
    - Phase 2 仍然不引入“同一领域内重复实例化同一个 Cube”的高级建模能力。
  artifacts:
    - frontend/tests/e2e-node/semantic.visual.spec.ts
    - frontend/tests/e2e-node/domain-catalog.spec.ts
    - frontend/tests/e2e-node/cube-browse.spec.ts
    - docs/semantic_verification.md
    - docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md
  key_links:
    - 回归门槛和文档基线必须一起更新，否则 Phase 2 只会留下代码行为，缺少稳定交付口径。
---

<objective>
把 Phase 2 的对象治理能力收口成可回归、可说明、可交付的一套基线。

Purpose: 如果只完成后端和页面实现，而不补浏览器回归与文档基线，语义中心很快会回到“每页各自演进、没人知道哪套是当前标准”的状态。
Output: 语义专项 e2e / visual 回归、验证文档、架构/PRD 回写，以及最终交付门槛。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-CONTEXT.md
@.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-RESEARCH.md
@docs/semantic_verification.md
@docs/TECH_STACK_AND_ARCHITECTURE.md
@docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md
@docs/prd/semantic_layer_prd.md
@frontend/tests/e2e-node/semantic.visual.spec.ts
@frontend/tests/e2e-node/domain-catalog.spec.ts
@frontend/tests/e2e-node/domain-publish.spec.ts
@frontend/tests/e2e-node/cube-browse.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展语义专项浏览器回归以覆盖 Phase 2 的治理摘要与导航</name>
  <files>frontend/tests/e2e-node/semantic.visual.spec.ts, frontend/tests/e2e-node/domain-catalog.spec.ts, frontend/tests/e2e-node/domain-publish.spec.ts, frontend/tests/e2e-node/cube-browse.spec.ts</files>
  <read_first>frontend/tests/e2e-node/semantic.visual.spec.ts, frontend/tests/e2e-node/domain-catalog.spec.ts, frontend/tests/e2e-node/domain-publish.spec.ts, frontend/tests/e2e-node/cube-browse.spec.ts, docs/semantic_verification.md</read_first>
  <action>扩展现有语义 e2e / visual 回归，不新造第二套 smoke。`domain-catalog.spec.ts` 必须断言领域目录仍以治理看板方式工作，并可进入建模入口；`cube-browse.spec.ts` 必须断言当 `domain_count > 1` 时页面出现 `多领域引用` 或等价多领域摘要；`semantic.visual.spec.ts` 需要覆盖 `DomainList`、`CubeDetail`、`ViewDetail`、`DevTools` 的最新治理展示；`domain-publish.spec.ts` 继续保证领域发布流程不被 Phase 2 回归破坏。</action>
  <verify>cd frontend && npm exec -- playwright test tests/e2e-node/semantic.visual.spec.ts tests/e2e-node/domain-catalog.spec.ts tests/e2e-node/domain-publish.spec.ts tests/e2e-node/cube-browse.spec.ts</verify>
  <acceptance_criteria>
    - frontend/tests/e2e-node/domain-catalog.spec.ts contains `domain-create-trigger`
    - frontend/tests/e2e-node/cube-browse.spec.ts contains `多领域引用` or `cube-domain-links`
    - frontend/tests/e2e-node/semantic.visual.spec.ts contains `ViewDetail` or `view-related-cubes`
    - frontend/tests/e2e-node/domain-publish.spec.ts contains `publish-domain-button`
  </acceptance_criteria>
  <done>Phase 2 的治理摘要和跨页导航已经进入浏览器级回归门槛，不再只依赖局部单测。</done>
</task>

<task type="auto">
  <name>Task 2: 回写语义验证与架构文档基线</name>
  <files>docs/semantic_verification.md, docs/TECH_STACK_AND_ARCHITECTURE.md, docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md, docs/prd/semantic_layer_prd.md</files>
  <read_first>docs/semantic_verification.md, docs/TECH_STACK_AND_ARCHITECTURE.md, docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md, docs/prd/semantic_layer_prd.md, .planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-CONTEXT.md</read_first>
  <action>同步更新四份文档，明确写出以下当前基线：1) `Domain.cubes[]` / 领域画布是 Cube 与领域关系的唯一真相；2) `Cube.domain_id` 仅保留为兼容投影字段；3) `View` 在 Phase 2 作为特殊 `Cube` 于展示层收敛；4) `Recipe` 继续保持轻量消费对象；5) 语义验证入口仍以 `make verify-semantic` 为主，并补充 Phase 2 的浏览器回归重点。不要把已延期的“同一领域重复实例化同一个 Cube”写成当前能力。</action>
  <verify>make verify-docs</verify>
  <acceptance_criteria>
    - docs/semantic_verification.md contains `make verify-semantic`
    - docs/TECH_STACK_AND_ARCHITECTURE.md contains `Domain.cubes[]`
    - docs/architecture/decisions/ADR-004-semantic-workbench-page-model.md contains `特殊 Cube` or `View`
    - docs/prd/semantic_layer_prd.md contains `Recipe`
  </acceptance_criteria>
  <done>实现、验证和文档三者已经对齐，后续不会再靠口头记忆解释 Phase 2 的对象边界。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `make test-regression-semantic` passes
- [ ] `make verify-semantic` passes
- [ ] `make verify-docs` passes
</verification>

<success_criteria>

- All tasks completed
- 浏览器回归能够覆盖 Phase 2 的治理摘要与工作台导航
- 文档基线已经写清对象关系真相与 Phase 2 边界
- Phase 2 交付口径不依赖额外口头解释
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-04-SUMMARY.md`
</output>
