---
phase: 02-semantic-object-lifecycle-and-domain-directory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/application/semantic/domain_modeling_service.py
  - app/application/semantic/domain_canvas_service.py
  - tests/unit/application/semantic/test_domain_modeling_service.py
  - tests/unit/application/semantic/test_domain_canvas_service.py
autonomous: true
requirements: [SEM-03, SEM-05, DOM-02]
must_haves:
  truths:
    - `Domain.cubes[]` 与领域画布继续是 Cube 归属关系的唯一真相；Phase 2 不通过 `Cube.domain_id` 反向写回领域关系。
    - 同一个 Cube 可以属于多个领域，但同一领域内不能重复出现同一个 Cube；发布和更新路径会统一去重并保留原始顺序。
    - 领域详情与领域画布会输出统一的 `governance_summary`，并给库中 Cube 暴露 `related_domain_ids`、`related_domain_names`、`domain_count`。
  artifacts:
    - app/application/semantic/domain_modeling_service.py
    - app/application/semantic/domain_canvas_service.py
    - tests/unit/application/semantic/test_domain_modeling_service.py
    - tests/unit/application/semantic/test_domain_canvas_service.py
  key_links:
    - `DomainModelingService` 负责同领域内去重和发布前校验，`DomainCanvasService` 负责把该真相投影成前端可消费的治理数据。
    - Phase 2 允许跨领域复用同一个 Cube，但不引入“同一领域重复实例化同一个 Cube”能力。
---

<objective>
把领域关系从“隐式的一对一归属”推进到“明确的多对多真相 + 治理摘要”。

Purpose: Phase 2 先把 `Domain.cubes[]` / 领域画布这条真相链做稳，否则前端即使补了目录和详情页，也还是会建立在错误的一对一心智上。
Output: 域内去重规则、领域治理摘要、画布库 Cube 的多领域投影字段，以及对应 unit 回归。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-CONTEXT.md
@.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-RESEARCH.md
@app/application/semantic/domain_modeling_service.py
@app/application/semantic/domain_canvas_service.py
@tests/unit/application/semantic/test_domain_modeling_service.py
@tests/unit/application/semantic/test_domain_canvas_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在领域建模服务中固化“跨领域允许、域内禁止重复”的关系规则</name>
  <files>app/application/semantic/domain_modeling_service.py, tests/unit/application/semantic/test_domain_modeling_service.py</files>
  <read_first>app/application/semantic/domain_modeling_service.py, app/domain/semantic/entities.py, .planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-CONTEXT.md, tests/unit/application/semantic/test_domain_modeling_service.py</read_first>
  <action>在 `DomainModelingService` 新增显式 helper `def _normalize_domain_cubes(self, cubes: list[str]) -> list[str]`，固定使用“保序去重”规则清理 `DomainDefinition.cubes`；在 `update_domain()`、`publish_domain()`、`add_cube()` 统一调用该 helper。具体目标状态是：同一 `domain.cubes` 内相同 `cube_name` 最终只保留一次，跨不同 `Domain` 复用同一 `Cube` 不报错，不写回或修改任何 `Cube.domain_id`。如果传入 payload 含重复 cube，发布前由 `validate_domain()` 或等价校验逻辑给出明确诊断消息，文案包含 `同一领域内不能重复引用同一个 Cube`。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_domain_modeling_service.py</verify>
  <acceptance_criteria>
    - app/application/semantic/domain_modeling_service.py contains `def _normalize_domain_cubes`
    - app/application/semantic/domain_modeling_service.py contains `同一领域内不能重复引用同一个 Cube`
    - app/application/semantic/domain_modeling_service.py contains `_normalize_domain_cubes(` at least 3 times
    - tests/unit/application/semantic/test_domain_modeling_service.py contains `duplicate` or `重复`
  </acceptance_criteria>
  <done>领域关系规则已经从“默认假设”变成显式代码与测试约束，后续前端可以安全建立多对多心智。</done>
</task>

<task type="auto">
  <name>Task 2: 在领域详情和画布库中补齐治理摘要与多领域投影</name>
  <files>app/application/semantic/domain_modeling_service.py, app/application/semantic/domain_canvas_service.py, tests/unit/application/semantic/test_domain_canvas_service.py</files>
  <read_first>app/application/semantic/domain_modeling_service.py, app/application/semantic/domain_canvas_service.py, app/application/semantic/semantic_definition_service.py, tests/unit/application/semantic/test_domain_canvas_service.py</read_first>
  <action>在 `DomainModelingService.get_domain_detail()` 和 `DomainCanvasService.get_canvas()` 增加统一 `governance_summary`，精确包含 `cube_count`、`active_cube_count`、`draft_cube_count`、`deprecated_cube_count`、`join_count`、`dangling_cube_count`。同时让 `DomainCanvasService` 在 `library_cubes` 和当前 `nodes` payload 中增加 `related_domain_ids`、`related_domain_names`、`domain_count` 三个字段：遍历全部领域仓储，只要某个 `Domain.cubes` 包含该 cube 名称，就把该领域的 `id/code` 与 `name` 收集进来；`in_domain` 仍然只根据当前 domain 的 `cubes` 集判断。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_domain_canvas_service.py</verify>
  <acceptance_criteria>
    - app/application/semantic/domain_modeling_service.py contains `governance_summary`
    - app/application/semantic/domain_canvas_service.py contains `related_domain_ids`
    - app/application/semantic/domain_canvas_service.py contains `related_domain_names`
    - app/application/semantic/domain_canvas_service.py contains `dangling_cube_count`
  </acceptance_criteria>
  <done>领域详情和画布库都能直接表达治理健康度与多领域引用关系，不再只暴露单领域错觉。</done>
</task>

<task type="auto">
  <name>Task 3: 用单元测试锁定关系真相与治理摘要输出</name>
  <files>tests/unit/application/semantic/test_domain_modeling_service.py, tests/unit/application/semantic/test_domain_canvas_service.py</files>
  <read_first>tests/unit/application/semantic/test_domain_modeling_service.py, tests/unit/application/semantic/test_domain_canvas_service.py, app/application/semantic/domain_modeling_service.py, app/application/semantic/domain_canvas_service.py</read_first>
  <action>扩展 `test_domain_modeling_service.py` 与 `test_domain_canvas_service.py`，至少覆盖三类场景：1) 同领域重复 cube 会被拒绝或归一化；2) 不同领域引用同一 cube 时不会互相污染；3) `governance_summary`、`related_domain_ids`、`related_domain_names`、`domain_count` 都会出现在服务输出中。新增断言时直接使用这些键名，不要只检查长度或 truthy。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_domain_modeling_service.py tests/unit/application/semantic/test_domain_canvas_service.py</verify>
  <acceptance_criteria>
    - tests/unit/application/semantic/test_domain_modeling_service.py contains `governance_summary` or `重复引用`
    - tests/unit/application/semantic/test_domain_canvas_service.py contains `related_domain_ids`
    - tests/unit/application/semantic/test_domain_canvas_service.py contains `domain_count`
    - tests/unit/application/semantic/test_domain_canvas_service.py contains `dangling_cube_count`
  </acceptance_criteria>
  <done>Phase 2 的关系真相和领域治理摘要有了直接回归门槛，不依赖人工记忆。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_domain_modeling_service.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_domain_canvas_service.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_domain_modeling_service.py tests/unit/application/semantic/test_domain_canvas_service.py` passes
</verification>

<success_criteria>

- All tasks completed
- 同一个 Cube 可以被多个领域引用，但同一领域内不会重复实例化
- 领域详情和领域画布都能稳定暴露治理摘要与多领域投影
- 后续前端不再需要把 `Cube.domain_id` 当作唯一归属真相
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-01-SUMMARY.md`
</output>
