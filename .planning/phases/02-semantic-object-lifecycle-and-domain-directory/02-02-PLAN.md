---
phase: 02-semantic-object-lifecycle-and-domain-directory
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/application/semantic/semantic_definition_service.py
  - app/interfaces/api/v1/semantic.py
  - tests/unit/application/semantic/test_semantic_definition_service.py
  - tests/unit/application/semantic/test_view_publish_service.py
  - tests/integration/test_semantic_api.py
autonomous: true
requirements: [SEM-01, SEM-02, SEM-04, SEM-05, DOM-01, DOM-03, DOM-04]
must_haves:
  truths:
    - `Cube` 摘要与详情保留 `domain_id / domain_name` 兼容字段，同时新增多领域投影字段用于治理视图消费。
    - `domain_id` 投影字段必须是确定性的：若 `cube.domain_id` 可解析则优先使用；否则从所有相关领域中按 `domain.code` 升序选第一个作为兼容投影。
    - 多领域投影字段的生成必须采用单次批量聚合策略，不能在 `list_cubes()` 或等价列表路径中为每个 cube 重复扫描全部领域。
    - `View` 在列表与详情层具备与 `Cube` 接近的状态摘要，但仍保留自己的 `publish_summary / drift_summary`。
    - `Recipe` 继续保持轻量对象，只在列表与定义浏览入口补最小状态感知和关联 Cube 摘要。
  artifacts:
    - app/application/semantic/semantic_definition_service.py
    - app/interfaces/api/v1/semantic.py
    - tests/unit/application/semantic/test_semantic_definition_service.py
    - tests/integration/test_semantic_api.py
  key_links:
    - `semantic_definition_service` 负责生成统一对象摘要，`semantic.py` 负责把这些字段稳定暴露给前端。
    - `02-02` 依赖 `02-01` 已经固化的领域关系真相，再把它投影成统一对象摘要契约。
    - `View` 与 `Recipe` 的轻量治理能力在 Phase 2 通过摘要和导航完成，不新增重型编辑流程。
---

<objective>
把 `Cube / View / Recipe` 从“各自能读”推进到“有统一摘要、状态和治理导航入口”。

Purpose: 如果后端摘要契约不先收敛，前端工作台只能继续为每类对象写一套分裂逻辑，无法真正形成统一的语义治理心智。
Output: `Cube` 多领域投影字段、`View` 统一摘要、`Recipe` 轻量状态与关联摘要，以及对应 API / unit / integration 回归。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-CONTEXT.md
@.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-RESEARCH.md
@app/application/semantic/semantic_definition_service.py
@app/interfaces/api/v1/semantic.py
@tests/unit/application/semantic/test_semantic_definition_service.py
@tests/unit/application/semantic/test_view_publish_service.py
@tests/integration/test_semantic_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在语义定义服务中补齐 Cube 的多领域投影摘要</name>
  <files>app/application/semantic/semantic_definition_service.py, tests/unit/application/semantic/test_semantic_definition_service.py</files>
  <read_first>app/application/semantic/semantic_definition_service.py, app/application/semantic/domain_canvas_service.py, .planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-CONTEXT.md, tests/unit/application/semantic/test_semantic_definition_service.py</read_first>
  <action>在 `SemanticDefinitionService` 新增两个 helper：`def _build_cube_domain_projection_index(self, cubes: list[CubeDefinition]) -> dict[str, dict[str, Any]]` 和 `def _select_primary_domain(self, cube: CubeDefinition, domains: list[dict[str, Any]]) -> tuple[str | None, str | None]`。前者必须只遍历一次 `domain_repo.list_all()`，按 cube 名称批量生成 `domain_ids`、`domains`、`domain_count`；后者固定使用确定性投影规则：若 `cube.domain_id` 能解析到真实领域，则用它作为 `domain_id / domain_name`，否则对相关领域按 `domain.code` 升序排序并取第一个。`list_cubes()` 必须先批量构建 projection index，再逐 cube 读取；`describe_cube()` 可以复用同一 helper 对单 cube 包装，不写回 YAML。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_semantic_definition_service.py</verify>
  <acceptance_criteria>
    - app/application/semantic/semantic_definition_service.py contains `def _build_cube_domain_projection_index`
    - app/application/semantic/semantic_definition_service.py contains `def _select_primary_domain`
    - app/application/semantic/semantic_definition_service.py contains `domain_ids`
    - app/application/semantic/semantic_definition_service.py contains `domain_count`
    - app/application/semantic/semantic_definition_service.py contains `sorted(`
    - tests/unit/application/semantic/test_semantic_definition_service.py contains `domain_ids`
  </acceptance_criteria>
  <done>`Cube` 摘要不再只剩单个 `domain_id`，前端可以在不破坏兼容性的前提下表达多领域引用。</done>
</task>

<task type="auto">
  <name>Task 2: 统一 View 与 Recipe 的轻量治理摘要接口</name>
  <files>app/application/semantic/semantic_definition_service.py, app/interfaces/api/v1/semantic.py, tests/unit/application/semantic/test_view_publish_service.py, tests/integration/test_semantic_api.py</files>
  <read_first>app/interfaces/api/v1/semantic.py, app/application/semantic/semantic_definition_service.py, app/application/semantic/view_publish_service.py, tests/integration/test_semantic_api.py, tests/unit/application/semantic/test_view_publish_service.py</read_first>
  <action>把 `GET /semantic/views` 的列表输出固定为治理摘要对象，而不是直接透传仓储模型；每个 view 至少返回 `name`、`title`、`description`、`public`、`cube_count`、`cubes`、`status`、`state_summary`、`publish_summary.publish_status`、`publish_summary.last_published_at`。同时把 `GET /semantic/recipes` 扩展为轻量治理摘要：每个 recipe 必须返回 `name`、`title`、`tags`、`example_count`、`related_cubes`、`state_summary`，其中 `state_summary.object_type='recipe'`，`state_summary.status='active'` 当 `example_count > 0`，否则为 `draft`。`tests/unit/application/semantic/test_view_publish_service.py` 这次不仅作为 verify 命令运行，还要补断言覆盖 `publish_status` / `last_published_at` 的摘要形状。不要新增 Recipe 编辑 API，只做摘要和导航契约收敛。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_view_publish_service.py tests/integration/test_semantic_api.py</verify>
  <acceptance_criteria>
    - app/interfaces/api/v1/semantic.py contains `publish_summary`
    - app/interfaces/api/v1/semantic.py contains `state_summary`
    - app/interfaces/api/v1/semantic.py contains `related_cubes`
    - tests/integration/test_semantic_api.py contains `domain_ids`
    - tests/integration/test_semantic_api.py contains `publish_status`
  </acceptance_criteria>
  <done>`View` 和 `Recipe` 都有了可稳定消费的摘要接口，前端不必自己拼接对象状态。</done>
</task>

<task type="auto">
  <name>Task 3: 用 unit 与 integration 测试锁定对象摘要契约</name>
  <files>tests/unit/application/semantic/test_semantic_definition_service.py, tests/unit/application/semantic/test_view_publish_service.py, tests/integration/test_semantic_api.py</files>
  <read_first>tests/unit/application/semantic/test_semantic_definition_service.py, tests/unit/application/semantic/test_view_publish_service.py, tests/integration/test_semantic_api.py, app/application/semantic/semantic_definition_service.py, app/interfaces/api/v1/semantic.py</read_first>
  <action>扩展三类回归：1) `test_semantic_definition_service.py` 断言 `domain_ids`、`domains`、`domain_count` 会出现在 cube list/detail；2) `test_view_publish_service.py` 或等价定义服务回归覆盖 `View` 摘要中的 `publish_status` 与 `last_published_at`；3) `test_semantic_api.py` 直接断言 `/semantic/views` 与 `/semantic/recipes` 返回上述字段，而不是只检查 HTTP 200。</action>
  <verify>PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_semantic_definition_service.py tests/unit/application/semantic/test_view_publish_service.py tests/integration/test_semantic_api.py</verify>
  <acceptance_criteria>
    - tests/unit/application/semantic/test_semantic_definition_service.py contains `domain_count`
    - tests/unit/application/semantic/test_semantic_definition_service.py contains `sorted` or `primary domain`
    - tests/unit/application/semantic/test_view_publish_service.py contains `publish_status`
    - tests/integration/test_semantic_api.py contains `related_cubes`
    - tests/integration/test_semantic_api.py contains `state_summary`
  </acceptance_criteria>
  <done>后端对象摘要已经形成契约化回归，后续前端和执行阶段可以基于这些字段稳定开发。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_semantic_definition_service.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/unit/application/semantic/test_view_publish_service.py` passes
- [ ] `PYTHONPATH=. python -m pytest --no-cov tests/integration/test_semantic_api.py` passes
</verification>

<success_criteria>

- All tasks completed
- `Cube` 摘要同时具备兼容投影字段和多领域治理字段
- `View` 与 `Recipe` 都能以统一对象摘要被前端消费
- API 层不新增重型对象模型或额外持久化机制
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-object-lifecycle-and-domain-directory/02-02-SUMMARY.md`
</output>
