---
phase: 08.1-consume-04
plan: 03
type: execute
wave: 3
depends_on: ["08.1-02"]
files_modified:
  - frontend/src/v2/pages/chat/DataChat.tsx
  - frontend/src/v2/i18n/zh.json
  - docs/runbooks/production-acceptance.md
  - .planning/REQUIREMENTS.md
  - tests/integration/semantic/test_datachat_official_consume.py
requirements: [CONSUME-04]
autonomous: false

must_haves:
  truths:
    - "DataChat 对未登录用户引导登录（拒匿名后前端不再静默失败）"
    - "跨入口一致性冒烟：同主体同问，DataChat 与 /agent/semantic/plan 的 policy_decision.decision 一致（集成断言已绿）"
    - "production-acceptance.md:80 区域 legacy 验收行更新为诚实兜底口径"
    - "REQUIREMENTS.md Traceability 登记 CONSUME-04 → Phase 8.1 → In Progress（台账完整）"
    - "全平台 make verify 回归通过，p32/p34 不回归"
  artifacts:
    - path: "frontend/src/v2/pages/chat/DataChat.tsx"
      provides: "未登录引导登录交互"
      contains: "login"
    - path: "docs/runbooks/production-acceptance.md"
      provides: "验收行更新为诚实兜底（非 legacy 物理出数）"
      contains: "fallback"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONSUME-04 Traceability 登记行"
      contains: "CONSUME-04"
  key_links:
    - from: "DataChat.tsx"
      to: "登录态判断"
      via: "未登录时引导登录而非发空请求"
      pattern: "login|isAuthenticated|未登录"
    - from: "test_datachat_official_consume.py governance_parity"
      to: "DataChat 与 agent 入口 policy_decision"
      via: "集成断言两入口 decision 一致"
      pattern: "policy_decision"
---

<objective>
Wave 3（合流收尾）：Wave 2 后端原子批次落地后，补齐前端登录引导（决策 3 的前端侧）、跨入口治理一致性冒烟、文档同步（含 REQUIREMENTS 台账）与全平台回归。本 plan 不改后端治理逻辑，只做用户可见收尾 + 验收门。

Purpose: 让"拒匿名"在前端有正确引导（而非静默 401），把治理一致性写成可回归断言，更新验收文档 + 补登需求台账，过全平台 verify 门。
Output: 前端登录引导 + 一致性冒烟绿 + 文档/台账更新 + make verify 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-consume-04/08.1-CONTEXT.md
@.planning/phases/08.1-consume-04/08.1-02-SUMMARY.md

<interfaces>
<!-- 执行者直接使用以下契约。 -->

DataChat dataset 选择器现状（文案已是"范围提示·可选"，无需改文案）— DataChat.tsx:
  :88 dataChat.ctx.datasetFirst = "直接提问；可在左侧选择某个数据集以限定范围（可选）。"
  :221 dataChat.dataset.label = "数据集范围（可选）"
  :232 dataChat.dataset.global = "全局（不限数据集）"
  → CONTEXT 要求的"范围提示（可选）"前端已落地；本 plan 前端只补「未登录引导登录」。

DataChat 现有登录态来源（执行者读 DataChat.tsx 顶部 import 与 v2 auth hook 确认）：
  v2 通常经 auth context / useAuth / token 判断；引导走既有登录路由（与 ProtectedRoute 同口径）。

production-acceptance.md 待更新行 — :80 区域:
  当前："legacy 回答带「未经语义层验证」前缀且 agent_query_log 全路径记录 | ✅（代码+测试）"
  → 更新为：DataChat 答不出统一诚实兜底（source='fallback'/status='unanswerable'），物理直表旁路已删除；
    + 治理对齐：DataChat 主链经 AccessPolicyDecisionService.pre_route/post_compile，与 /agent/semantic/plan 同裁决。

REQUIREMENTS.md Traceability 待补登 — .planning/REQUIREMENTS.md:109 区域（表 `| Requirement | Phase | Status |`）:
  当前表内无任何 CONSUME-0x 行；本 plan 补一行 `| CONSUME-04 | Phase 8.1 | In Progress |`（不动 Coverage 计数说明——CONSUME 系列原不在 v1 35 条内，仅补台账可追溯性；如表底 Coverage 数有口径冲突，标注 CONSUME 为 v1 后增量、不并入 35 计数）。

i18n 新增 key（zh.json，中文单语）：
  "dataChat.auth.required": "请先登录后再发起数据问答"（或等价），供未登录引导使用。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataChat 未登录引导登录 + 跨入口一致性冒烟集成断言</name>
  <files>frontend/src/v2/pages/chat/DataChat.tsx, frontend/src/v2/i18n/zh.json, tests/integration/semantic/test_datachat_official_consume.py</files>
  <read_first>
    - frontend/src/v2/pages/chat/DataChat.tsx（被改文件，:40-135 会话/发送逻辑，确认登录态来源 hook）
    - frontend/src/v2/i18n/zh.json（:965-994 dataChat.* 命名空间，加 auth.required key）
    - frontend/src/v2/layout/AppShell.tsx 或 v2 auth context（确认 v2 登录态判断口径与 ProtectedRoute 同源）
    - tests/integration/semantic/test_datachat_official_consume.py（Wave 2 已加 governance_parity，本 task 确认其断言两入口 decision 一致）
    - app/interfaces/api/v1/agent.py:73-83（/agent/semantic/plan 入口，一致性冒烟的对照入口）
  </read_first>
  <action>
    1. frontend/src/v2/i18n/zh.json：在 dataChat.* 段加 `"dataChat.auth.required": "请先登录后再发起数据问答"`（保持中文单语，key 字母序就近插入）。
    2. frontend/src/v2/pages/chat/DataChat.tsx：在发送/新建会话入口前判断登录态（复用 v2 既有 auth hook / context，与 ProtectedRoute 同口径）；未登录时不发请求，展示 t('dataChat.auth.required') 并引导到登录（既有登录路由/动作）。不改 dataset 选择器文案（已合规）。
    3. tests/integration/semantic/test_datachat_official_consume.py：确认（或补强）Wave 2 落地的 test_two_entrances_policy_decision_parity 真实断言「同一 principal + 同一 question，DataChat 主链产出的 policy_decision.decision == /agent/semantic/plan（经 agent_plan_handler）产出的 decision」。若 Wave 2 仅 stub，本 task 用同一 AccessPolicyDecisionService 实例 + 同种子 policy 跑两入口断言相等。
  </action>
  <verify>
    <automated>pytest tests/integration/semantic/test_datachat_official_consume.py -k "parity" -x 2>&1 | grep -E "passed|PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dataChat.auth.required" frontend/src/v2/i18n/zh.json` >= 1
    - `grep -c "dataChat.auth.required" frontend/src/v2/pages/chat/DataChat.tsx` >= 1
    - `grep -c "test_two_entrances_policy_decision_parity" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `pytest tests/integration/semantic/test_datachat_official_consume.py -k "parity" -x` exits 0
    - `make typecheck-frontend`（或 `cd frontend && npx tsc --noEmit`）无新增类型错
  </acceptance_criteria>
  <done>
    DataChat 未登录引导登录已落地（不静默 401），跨入口一致性集成断言绿，前端类型检查通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 文档同步（production-acceptance + REQUIREMENTS 台账）+ 全平台 verify 回归</name>
  <files>docs/runbooks/production-acceptance.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/runbooks/production-acceptance.md（:80 区域 legacy 验收行）
    - .planning/REQUIREMENTS.md:105-150（Traceability 表 + Coverage 说明）
    - .planning/phases/08.1-consume-04/08.1-CONTEXT.md（specifics：:313 + production-acceptance.md:80 是阻断级遗漏）
    - .planning/phases/08.1-consume-04/08.1-02-SUMMARY.md（observe 纠偏归档结论，本 task 文档口径须与之一致）
    - CLAUDE.md（文档更新期望：影响 API 路径鉴权须查 production-acceptance.md；Makefile 统一验证入口）
  </read_first>
  <action>
    1. docs/runbooks/production-acceptance.md:80 区域：把"legacy 回答带「未经语义层验证」前缀"验收行更新为——DataChat 答不出统一诚实兜底（source='fallback'/status='unanswerable'），两条物理直表旁路（legacy 第3层 + agent 第2层全局会话）已删除；治理对齐：DataChat 主链经 AccessPolicyDecisionService.pre_route/post_compile，与 /agent/semantic/plan 同裁决；新增 D4 运维桥接前提：DataChat 主体须绑 data_m1_reader 方能出数。补一句口径说明（来自 RESEARCH Q1(b)）：本期不改 RLS 模式（observe），出数钥匙=主体绑 data_m1_reader——`data_policy_not_matched` deny 与 RLS 模式无关。
    2. .planning/REQUIREMENTS.md Traceability 表（:109 区域）补一行 `| CONSUME-04 | Phase 8.1 | In Progress |`（CONSUME 系列为 v1 后增量收口需求，不并入表底 v1 35 条 Coverage 计数；若表底 Coverage 文字与之冲突，加一句脚注标注 CONSUME 为后增量、不计入 35 mapped 统计）。
    3. 运行全平台回归：`make verify`（或最小路由 `make verify-changed` / `make verify-backend` + `make verify-docs` + 前端测试）。重点确认 p32/p34（comment demo 发布链路）不回归、conversations/datachat 相关测试绿。
    4. 若 verify 报无关存量 lint（历史已知，参考 STATE.md Phase 2 记录），记录在 SUMMARY 不阻断本期交付，但本期改动文件不得引入新 lint/typecheck 错。
  </action>
  <verify>
    <automated>make verify-backend 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fallback\|诚实兜底" docs/runbooks/production-acceptance.md` >= 1
    - `grep -c "未经语义层验证" docs/runbooks/production-acceptance.md` 行为已更新（该旧表述若保留须明确标注为历史/已废，新口径为诚实兜底）
    - `grep -c "CONSUME-04" .planning/REQUIREMENTS.md` >= 1（Traceability 已登记）
    - `make verify-backend` exits 0（或仅历史无关 lint，本期改动文件零新错）
    - `make verify-docs` exits 0
    - p34/p32 相关测试无回归：`pytest -k "p34 or modeling_agent_live or comment" 2>&1 | grep -E "passed|failed"` 无新增 failed
  </acceptance_criteria>
  <done>
    production-acceptance.md 验收行更新为诚实兜底 + 治理对齐 + observe 纠偏口径；REQUIREMENTS.md 登记 CONSUME-04；make verify-backend/verify-docs 绿；p32/p34 不回归。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 真实环境闭环验证（D4 运维桥接 checkpoint）</name>
  <files>（无文件改动——真实环境人工验证）</files>
  <action>
    Phase 8.1 完整落地后的真实环境闭环验证（D4 运维桥接，自动化无法替代）。执行者先确保 Task 1/2 自动化全绿，再引导用户在真实部署环境执行：
    1. 部署后给 DataChat 目标登录主体绑定 data_m1_reader（access API PUT role-bindings 或容器内 ensure_principal_role_bindings）。
    2. 真实 DataChat 登录态问「学生答题统计 总数」→ 期望经 semantic 主链 + 治理 allow → 真实出数（route_type=cube，有 execution_results），source='semantic'。
    3. 用一个无 data 角色的主体问同一问 → 期望治理 deny → 诚实兜底（source='fallback'，不出数、不报 500）。
    4. 未登录态打开 DataChat → 期望引导登录（不静默失败）。
    5. 同主体同问对比 DataChat 与 /agent/semantic/plan 的 policy_decision.decision 一致。
    6.（可选）确认无残留 running AgentQueryLog（全局会话不再建 agent log）。
    用户输入 "approved"（全部符合预期）或描述偏差后方可继续。
  </action>
  <verify>
    人工验证：步骤 2 真实出数（source='semantic' + 非空 execution_results）、步骤 3 deny 兜底（source='fallback'）、步骤 4 登录引导、步骤 5 两入口 decision 一致。用户回复 "approved" 或偏差描述。
  </verify>
  <done>
    用户确认：绑 data_m1_reader 后真实出数 + 无角色主体 deny 兜底 + 未登录引导 + 两入口 decision 一致，全部符合预期（"approved"）。
  </done>
</task>

</tasks>

<verification>
- 前端：DataChat 未登录引导登录，dataset 文案保持"范围（可选）"，typecheck-frontend 绿。
- 集成：跨入口一致性断言绿（DataChat 与 agent 入口 decision 一致）。
- 文档：production-acceptance.md:80 区域更新为诚实兜底 + 治理对齐 + observe 纠偏口径；REQUIREMENTS.md 登记 CONSUME-04。
- 回归：make verify-backend / verify-docs 绿，p32/p34 不回归。
- 真实闭环（checkpoint）：绑 data_m1_reader 后真实出数 + 无角色主体 deny 兜底 + 未登录引导。
</verification>

<success_criteria>
- DataChat 对未登录用户引导登录。
- 跨入口治理一致性可回归且绿。
- 验收文档与新口径对齐；REQUIREMENTS 台账登记 CONSUME-04。
- 全平台 verify 通过，无回归。
- 真实环境闭环经 checkpoint 人工确认（绑角色出数 / deny 兜底 / 登录引导）。
</success_criteria>

<output>
完成后创建 .planning/phases/08.1-consume-04/08.1-03-SUMMARY.md，须记录：
- 前端登录引导落点
- 跨入口一致性断言形态
- 文档更新行（production-acceptance + observe 纠偏口径）
- REQUIREMENTS.md CONSUME-04 登记
- make verify 结果（含历史无关 lint 说明）
- checkpoint 真实闭环结果（绑角色后出数 / deny 兜底证据）
</output>
</content>
</invoke>
