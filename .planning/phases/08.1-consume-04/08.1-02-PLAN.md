---
phase: 08.1-consume-04
plan: 02
type: tdd
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - app/application/conversation/commands/send_message.py
  - app/interfaces/api/v1/conversations.py
  - app/application/conversation/handlers/send_message_handler.py
  - tests/unit/application/conversation/test_send_message_handler.py
  - tests/integration/semantic/test_datachat_official_consume.py
requirements: [CONSUME-04]
autonomous: true
user_setup:
  - service: access_role_bindings
    why: "principal 透传后，治理引擎（runtime_service.execute→PrincipalResolver→post_compile，已在链路）按真实角色裁决。DataChat 登录主体若无 data_ 角色 → post_compile data_policy_not_matched → deny → 诚实兜底（答不出）。出数前提是目标主体持 data_m1_reader（答题 cube 物理表 dws_* → M1 → m1_aggregate_read，DB 已 seed active allow）。飞书用户 principal:feishu:140b0c11... 现成已持 data_m0/m1/m2_reader；internal admin 零 data_ 角色需运维授一下。这是数据动作（运维），非代码。"
    dashboard_config:
      - task: "给 DataChat 目标登录主体绑定 data_m1_reader 角色（PUT /access/principals/<id>/permission-packages {data_m1_reader} 或 AccessIdentityService.ensure_principal_role_bindings）；data_m1_reader 权限包已含 data_m0_reader。访问策略/执行档已 seed（m1_aggregate_read active allow / mc_m1_reader active），不需新配。"
        location: "access 管理 API / 容器内 DB access_role_bindings 表（部署后执行，归入 D4 运维桥接）"

must_haves:
  truths:
    - "send-message 入口拒匿名（@require_auth），无 Bearer 返回 401"
    - "principal 解析（碰 g）只在 interfaces 层（conversations.py），经 principal_context_from_bearer() 取治理主体上下文（access_role_bindings 权威源，非 JWT roles），经 SendMessageCommand 透传进 handler"
    - "handler 不再写死 viewer_roles=[]，把 command.principal_context/command.viewer_roles 透传给 semantic_router_service.execute_plan"
    - "治理裁决不在 handler 重复实现：execute_plan→runtime_service.execute→PrincipalResolver.resolve→post_compile 整链已就绪（runtime_service.py:82/113/126），handler 只补 principal 透传即可让真实角色到达治理引擎；不在 handler 内自建 pre_route/post_compile/compile_preview（会与 runtime_service 重复裁决，造冗余——CONTEXT 对账修订第 1 项）"
    - "两条物理直表旁路彻底删除：_handle_via_legacy_llm 物理分支 + _execute_query + LEGACY_DISCLAIMER 删；_handle_via_agent 对全局会话(dataset_id is None) return None 短路"
    - "三类答不出统一收敛 _build_unanswerable_fallback()（source='fallback'/status='unanswerable'），不产 SQL 不碰物理表；dataset_id is None 与 dataset 有值两条主链全失败路径均落 fallback"
    - "治理 deny（execute_plan 返回 blocked / 无 execution_results）→ 落诚实兜底（不伪造出数）"
  artifacts:
    - path: "app/application/conversation/commands/send_message.py"
      provides: "SendMessageCommand 末尾加 principal_context/viewer_roles 可选字段（默认 None，向后兼容）"
      contains: "principal_context"
    - path: "app/application/conversation/handlers/send_message_handler.py"
      provides: "principal 透传（替换写死 viewer_roles=[]）+ 统一兜底 + 物理路删除 + agent 全局短路"
      contains: "_build_unanswerable_fallback"
    - path: "app/interfaces/api/v1/conversations.py"
      provides: "send-message 拒匿名（require_auth）+ principal_context_from_bearer 透传"
      contains: "require_auth"
  key_links:
    - from: "conversations.py send_message"
      to: "SendMessageCommand"
      via: "principal_context_from_bearer() 解析治理主体（access_role_bindings）后填入 command"
      pattern: "principal_context_from_bearer"
    - from: "send_message_handler._handle_via_semantic_router"
      to: "semantic_router_service.execute_plan"
      via: "透传 principal_context=command.principal_context, viewer_roles=command.viewer_roles（不再 viewer_roles=[]）；治理由下游 runtime_service.execute 的 post_compile 完成"
      pattern: "principal_context=command.principal_context"
    - from: "send_message_handler 各回退分支"
      to: "_build_unanswerable_fallback"
      via: "deny(blocked) / 未命中 / agent 软失败统一收敛"
      pattern: "_build_unanswerable_fallback"
---

<objective>
Wave 2（GREEN 原子批次）：把 Wave 1 全部 RED 断言转绿。这是 Phase 8.1 的核心交付，**四件事必须同批落地（CONTEXT specifics + Pitfall：分开上线会制造"看似对齐实则未对齐"合规错觉）**：

1. **principal 透传（决策 4，唯一治理命门）**：`SendMessageCommand` 加可选字段；`conversations.py` `@optional_auth`→`@require_auth` + `principal_context_from_bearer()` 取治理主体上下文（`access_role_bindings` 权威源，非 JWT roles）填 command；`_handle_via_semantic_router` 把 `command.principal_context`/`command.viewer_roles` 透传给 `execute_plan`，不再写死 `viewer_roles=[]`。
   **不在 handler 内自建治理管线**——`execute_plan`→`runtime_service.execute`(`runtime_service.py:82` `PrincipalResolver.resolve` → `:113` `post_compile` → `:126` `decision!=allow→blocked`) 整链已就绪并强制 deny（CONTEXT 对账修订第 1 项实测坐实）。handler 只要把真实 `principal_context` 透下去，治理引擎就拿到真实角色裁决。**严禁在 `_handle_via_semantic_router` 引入 `pre_route`/`post_compile`/`compile_preview`/`PrincipalResolver()`——会与 runtime_service 重复裁决，造冗余。**
2. **拒匿名**：`conversations.py` send-message 收紧为 `@require_auth`；无 Bearer → `AuthenticationError` → 统一 401。
3. **删物理直表旁路①**：legacy 第 3 层 `_handle_via_legacy_llm` 物理分支 + `_execute_query` + `LEGACY_DISCLAIMER` 全删。
4. **删物理直表旁路② + 统一诚实兜底**：`_handle_via_agent` 对 `dataset_id is None` 全局会话 `return None` 短路（消除残留 running AgentQueryLog 死路径）；新增 `_build_unanswerable_fallback()`（`source='fallback'`/`status='unanswerable'`），三类答不出（execute_plan 返回 blocked / 未命中 / agent 软失败）统一收敛。

Purpose: 让 DataChat 真正符合 L1→L2 治理纪律——principal 透传让已在链路的治理引擎按真实角色裁决；堵死"绕 gateway / 直扫物理表产 SQL"红线。
Output: principal 透传 + 拒匿名 + 物理收口 + 统一兜底的 production 代码，Wave 1 RED/xfail 全部转绿。**不新增 DI 注入，不在 handler 自建治理裁决。**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-consume-04/08.1-CONTEXT.md
@.planning/phases/08.1-consume-04/08.1-RESEARCH.md
@.planning/phases/08.1-consume-04/08.1-01-SUMMARY.md

<interfaces>
<!-- 执行者直接使用以下契约，无需回查代码库。来自逐行核实（2026-06-26 对账实测）。 -->

【治理已在链路，handler 不重复实现】— runtime_service.py（execute_plan 下游）:
```python
# runtime_service.py:47 def execute(..., :59 viewer_roles, :60 principal_context, ...)
# :82  principal = self._principal_resolver.resolve(principal_context=principal_context, viewer_roles=viewer_roles)
# :113 policy_decision = self._access_policy_service.post_compile(principal=principal, compiled_targets=[preview], ...)
# :126 if policy_decision.decision != "allow": return {"status":"blocked", ...}
```
> 含义：execute_plan → runtime_service.execute 已用真实 principal 跑 post_compile 并强制 deny（status=blocked）。
> handler 唯一缺口是没把 principal_context 透传进去（写死 viewer_roles=[]、零 principal_context）。
> **不要在 handler 引入 pre_route/post_compile/compile_preview/PrincipalResolver()——纯冗余。**

execute_plan 已接受 principal 透传 — preview_service.py:582-601:
```python
def execute_plan(self, *, question, viewer_roles=None, principal_context=None, runtime_mode="official", ...):
    viewer_roles = self._resolve_roles(viewer_roles=viewer_roles, principal_context=principal_context)  # :601
    # → 透传至 runtime_service.execute(viewer_roles=..., principal_context=...) :613-614
```
> 含义：handler 只需 execute_plan(question=..., principal_context=command.principal_context, viewer_roles=command.viewer_roles or [], runtime_mode="official")。
> _resolve_roles 会从 principal_context 取真实角色；治理在下游 runtime_service 完成。

当前 SendMessageCommand（仅 3 字段，dataclass）— send_message.py:7-12:
```python
@dataclass
class SendMessageCommand:
    conversation_id: int
    user_id: str
    content: str
```

当前 _handle_via_semantic_router 治理缺口 — send_message_handler.py:99-103:
```python
plan_result = self.semantic_router_service.execute_plan(
    question=command.content,
    viewer_roles=[],            # ← 写死空角色 + 零 principal_context，治理拿不到真实角色，命门
    runtime_mode="official",
)
```

interfaces 层 principal 解析 — principal_context.py:20-55（与 agent.py 同口径）:
```python
def principal_context_from_bearer(*, source: str = "bearer") -> dict:
    # 内部 RoleBindingResolver(...).resolve_principal_context(...)——角色来自 access_role_bindings（权威源，非 JWT）
    # 无 principal → raise AuthenticationError("Missing authenticated principal")
    # 返回 dict 含: principal_id / roles / data_roles / platform_roles / ...
```
> handler 透传：principal_context=pc；viewer_roles 派生 pc.get("roles") or []（治理实际只读 principal_context.roles，viewer_roles 仅兼容旧位置）。

require_auth — auth.py（注入 g.user_id/g.principal_id/g.user_roles；无 Bearer raise AuthenticationError）。
当前 conversations.py send-message :158 @optional_auth、:179 user_id ... or 'anonymous'、:181 SendMessageCommand(...)（须改）。

当前诚实兜底块 — send_message_handler.py:313-323:
```python
if conversation.dataset_id is None:
    # 落 Message(source='legacy_llm')「未能在已发布的语义资产中找到…」——source 须改 'fallback'
```

物理出口（须删）— send_message_handler.py:
  :279 LEGACY_DISCLAIMER 常量
  :311 _handle_via_legacy_llm — :313 dataset_id is None 兜底块（改兜底）、:333+ else 物理体（find_by_id→:355 generate_sql→_execute_query，删）
  :454 _execute_query 方法、:456/:464 AdapterFactory（删）
  :311 _handle_via_legacy_llm 是 :84 handle() 终点（-> Dict 非 Optional）——删 else 物理体后两条路径（None/有值）都必须显式 return _build_unanswerable_fallback，不能落空。

当前 agent 全局死路径 — _handle_via_agent :202（开头须加 dataset_id is None → return None）。

Message.source / via_semantic_layer — conversation.py:168/192:
  source = Column(String(32))  # 'fallback'(8) 合法
  'via_semantic_layer': self.source == 'semantic' if self.source else None  → source='fallback' ⇒ is False
status='unanswerable'(12) 合法 — agent_query_log.py status=String(20)。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SendMessageCommand 加字段 + conversations.py 拒匿名 + principal 透传</name>
  <files>app/application/conversation/commands/send_message.py, app/interfaces/api/v1/conversations.py</files>
  <read_first>
    - app/application/conversation/commands/send_message.py（被改文件，当前仅 3 字段，:7-12）
    - app/interfaces/api/v1/conversations.py（被改文件，:13 import optional_auth、:158 @optional_auth、:159 def send_message、:179 user_id ... or 'anonymous'、:181 SendMessageCommand 构造）
    - app/interfaces/api/v1/agent.py（require_auth + principal_context_from_bearer + authenticated_user_from_g 对齐口径，参照）
    - app/interfaces/api/v1/principal_context.py:20-55（principal_context_from_bearer 签名与返回 dict 结构；roles 来自 access_role_bindings）
    - app/interfaces/api/middleware/auth.py（require_auth 注入 g 与异常）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q4 principal 透传 + 拒匿名）
  </read_first>
  <behavior>
    - SendMessageCommand 加 principal_context: dict | None = None、viewer_roles: list[str] | None = None（dataclass 默认值放末尾，不破坏位置参数，所有现有构造点零改动兼容）。
    - conversations.py send-message 的 @optional_auth → @require_auth；无 Bearer 经 AuthenticationError → error_handler 统一 401（不手写 if not user）。
    - command 构造处填 principal_context=principal_context_from_bearer(source="datachat_bearer")（治理主体上下文，access_role_bindings 权威源）、viewer_roles=pc.get("roles") or []。
    - test_send_message_requires_auth（集成，conversations API 集成测试坐实）：无 Bearer → 401。
  </behavior>
  <action>
    1. app/application/conversation/commands/send_message.py：在 SendMessageCommand 末尾加
       `principal_context: dict | None = None`
       `viewer_roles: list[str] | None = None`
       （保持 dataclass，默认 None 放末尾，向后兼容；现有 SendMessageCommand(conversation_id=, user_id=, content=) 调用零改动）。
    2. app/interfaces/api/v1/conversations.py：
       - import：`from app.interfaces.api.middleware.auth import require_auth`（新增，保留 optional_auth import 供其他路由）；`from app.interfaces.api.v1.principal_context import principal_context_from_bearer`。
       - send_message 路由（:158）装饰器 `@optional_auth` → `@require_auth`。
       - command 构造（:181）前：`pc = principal_context_from_bearer(source="datachat_bearer")`，构造时追加 `principal_context=pc`、`viewer_roles=pc.get("roles") or []`。
       - user_id 行（:179）改用 `g.user_id`（require_auth 已注入，去掉 `or 'anonymous'`）。
       - 仅改 send_message 路由；其余 GET/DELETE 路由本期不动（保持 optional_auth 现状，最小改动）。
    3. 不在本 task 改 handler（handler 在 Task 2 改）。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py -k "semantic_router"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "principal_context" app/application/conversation/commands/send_message.py` >= 1
    - `grep -c "viewer_roles" app/application/conversation/commands/send_message.py` >= 1
    - `grep -c "require_auth" app/interfaces/api/v1/conversations.py` >= 1
    - `grep -c "principal_context_from_bearer" app/interfaces/api/v1/conversations.py` >= 1
    - send_message 路由不再 optional_auth：`sed -n '158,160p' app/interfaces/api/v1/conversations.py | grep -c "@require_auth"` >= 1
    - `PYTHONPATH=. /Users/xuan/miniconda3/bin/python -c "from app.application.conversation.commands.send_message import SendMessageCommand; SendMessageCommand(conversation_id=1, user_id='u', content='q')"` exits 0（旧构造点零改动兼容）
    - `PYTHONPATH=. /Users/xuan/miniconda3/bin/python -c "import app.interfaces.api.v1.conversations"` 无导入/语法错
  </acceptance_criteria>
  <done>
    SendMessageCommand 带可选 principal 字段且旧构造兼容；send-message 拒匿名（require_auth）+ principal（access_role_bindings 治理主体）经 command 透传；handler 未动。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: principal 透传 + 统一兜底 + 删两条物理路 + agent 全局短路（核心原子批次，不自建治理管线）</name>
  <files>app/application/conversation/handlers/send_message_handler.py, tests/unit/application/conversation/test_send_message_handler.py, tests/integration/semantic/test_datachat_official_consume.py</files>
  <read_first>
    - app/application/conversation/handlers/send_message_handler.py（被改文件主体：:89 _handle_via_semantic_router、:99-103 写死 viewer_roles=[]、:176 _build_blocked_route_message、:202 _handle_via_agent、:279 LEGACY_DISCLAIMER、:281 _record_query_log、:311 _handle_via_legacy_llm、:313 dataset_id is None 块、:333+ else 物理体、:355 generate_sql、:454 _execute_query、:456/:464 AdapterFactory）
    - app/application/semantic_router/preview_service.py:582-614（execute_plan 接受 principal_context/viewer_roles 并透传至 runtime_service.execute——确认 handler 只需透传不需自建治理）
    - app/application/execution_compiler/runtime_service.py:47-130（execute 内 PrincipalResolver.resolve :82 → post_compile :113 → decision!=allow→blocked :126；治理已在此完成，handler 不复刻）
    - app/domain/entities/conversation.py:168/192 + app/domain/entities/agent_query_log.py（source/via_semantic_layer/status 列约束）
    - .planning/phases/08.1-consume-04/08.1-01-SUMMARY.md（Wave 1 RED 结论：principal 透传后角色到达治理引擎才会 allow）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q3/Q5 物理收口与统一兜底；Q1(b) observe 纠偏——本期不改 RLS 模式）
  </read_first>
  <behavior>
    - principal 透传（不自建治理）：_handle_via_semantic_router 的 execute_plan 调用把 `viewer_roles=[]` 改为 `principal_context=command.principal_context, viewer_roles=command.viewer_roles or []`。治理裁决由下游 runtime_service.execute 的 post_compile（已在链路）完成；**不在 handler 引入 pre_route/post_compile/compile_preview/PrincipalResolver()**。
    - deny 收敛：execute_plan 返回 status=='blocked'（治理 deny）或无 execution_results 且 _build_blocked_route_message 给不出"运行时未就绪"明确原因（纯未命中）→ 落 _build_unanswerable_fallback（不伪造出数）。保留"运行时未就绪"blocked→source=='semantic' 分支不动。
    - 统一诚实兜底：新增 _build_unanswerable_fallback(conversation, user_message, *, reason) → 落 Message(source='fallback')，_record_query_log(status='unanswerable')；三类答不出（未命中/软失败、agent 软失败、deny）统一收敛。ai_message.to_dict()['via_semantic_layer'] is False。
    - 删物理旁路①：删 _handle_via_legacy_llm 的物理 else 分支（find_by_id + generate_sql + _execute_query 调用）+ _execute_query 方法 + LEGACY_DISCLAIMER 常量；dataset_id is None 兜底块改调 _build_unanswerable_fallback（source 'legacy_llm' → 'fallback'）；删 else 物理体后 dataset 有值路径也必须显式 return self._build_unanswerable_fallback(...)（_handle_via_legacy_llm 是 :84 终点，-> Dict 非 Optional，不能落空）。两条路径都落 fallback。
    - 删物理旁路②：_handle_via_agent 开头 `if conversation.dataset_id is None: return None`（建 AgentQueryLog 前短路，消除残留 running log）。
    - 转绿 Wave 1：test_unanswerable_fallback_via_semantic_layer_false / test_agent_global_session_short_circuits / test_agent_failure_falls_back_to_honest_fallback 通过；删除 xfail 的 test_legacy_llm_success / test_execute_query_rejects_non_select（方法已删）；集成层去掉 :313/治理 deny 两处 xfail 转绿；新增「dataset_id 有值 + agent 失败 → source=='fallback'」断言。
  </behavior>
  <action>
    在 app/application/conversation/handlers/send_message_handler.py：
    1. _handle_via_semantic_router（:89）execute_plan 调用（:99-103）：把 `viewer_roles=[]` 改为
       `principal_context=command.principal_context,`
       `viewer_roles=command.viewer_roles or [],`
       （保持 question=command.content, runtime_mode="official"）。**不新增 pre_route/post_compile/compile_preview/PrincipalResolver——治理由下游 runtime_service.execute 完成（runtime_service.py:113 post_compile 已在链路）。**
    2. execute_plan 返回处理：若 plan_result.get("status") == "blocked" 或（无 execution_results 且 _build_blocked_route_message 返 None，即纯未命中）→ `return self._build_unanswerable_fallback(conversation, user_message, reason=plan_result.get("reason") or "未命中已发布语义资产")`。保留既有"运行时未就绪"blocked→source=='semantic' 落库分支（:111-121 不动）。allow 出数路径（有 execution_results）维持现状 source='semantic' 落库。
    3. 新增方法 _build_unanswerable_fallback(self, conversation, user_message, *, reason: str | None = None) -> Dict[str, Any]：落 Message(source='fallback', content=诚实文案如"未能在已发布的语义资产中找到可回答该问题的口径，请补充建模或换一种问法。")，调 self._record_query_log(..., status='unanswerable')，返回响应 dict。
    4. _handle_via_agent（:202）开头加 `if conversation.dataset_id is None: return None`。
    5. _handle_via_legacy_llm（:311）收口：
       - dataset_id is None 兜底块（:313-323）改调 `return self._build_unanswerable_fallback(conversation, user_message, reason="未命中已发布语义资产")`（source 'legacy_llm' → 'fallback'）。
       - 删 else 物理体（find_by_id :333+ / generate_sql :355 / _execute_query 调用 / AdapterFactory）；删后 dataset 有值路径也必须 `return self._build_unanswerable_fallback(conversation, user_message, reason="未命中已发布语义资产")`（终点非 Optional，不能落空）。
       - 删除 _execute_query（:454-464）方法、LEGACY_DISCLAIMER（:279）常量。
       - 收口后 _handle_via_legacy_llm 两条路径（None / 有值）均 return _build_unanswerable_fallback；若两路完全同形可整体合并为单一 return，但两路必须都有显式 return。
    在测试文件：
    6. test_send_message_handler.py：删除 xfail 的 test_legacy_llm_success、test_execute_query_rejects_non_select（对应方法已删）；新 RED 用例转绿。**fixture handler 构造不追加 access_policy_service/compiler_service/principal_resolver**（本期 handler 不自建治理、不新增依赖）；transparency 测试只需断言 execute_plan 被调用时 principal_context==command.principal_context（principal 透传到位）。
    7. test_datachat_official_consume.py：去掉 :313 与 test_governance_deny_falls_back 的 xfail 标记，使其真实通过——governance deny 测法改为「stub execute_plan 返回 status=='blocked'（模拟下游 post_compile deny）→ 断言 ai['source']=='fallback' 且无伪造出数」（不在集成层重建治理引擎，治理由 runtime_service 单测/真实链覆盖）；新增「dataset_id 有值 + 主链全失败（agent 软失败）→ source=='fallback'」断言。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "principal_context=command.principal_context" app/application/conversation/handlers/send_message_handler.py` >= 1（principal 透传到位）
    - `grep -c "viewer_roles=\[\]" app/application/conversation/handlers/send_message_handler.py` == 0（写死空角色已删）
    - `grep -cE "pre_route|post_compile|compile_preview|PrincipalResolver\(" app/application/conversation/handlers/send_message_handler.py` == 0（决策修订第 1 项：handler 不自建治理管线，避免与 runtime_service 重复裁决）
    - `grep -c "def _build_unanswerable_fallback" app/application/conversation/handlers/send_message_handler.py` == 1
    - `grep -c "return self._build_unanswerable_fallback" app/application/conversation/handlers/send_message_handler.py` >= 2（_handle_via_legacy_llm 的 None 与有值两条路径均不落空）
    - `grep -c "LEGACY_DISCLAIMER" app/application/conversation/handlers/send_message_handler.py` == 0
    - `grep -c "def _execute_query" app/application/conversation/handlers/send_message_handler.py` == 0
    - `grep -c "AdapterFactory" app/application/conversation/handlers/send_message_handler.py` == 0
    - `grep -c "generate_sql" app/application/conversation/handlers/send_message_handler.py` == 0（物理 SQL 生成路径净删）
    - `grep -c "dataset_id is None" app/application/conversation/handlers/send_message_handler.py` >= 1（agent 短路）
    - `grep -c "xfail" tests/integration/semantic/test_datachat_official_consume.py` == 0（GREEN 后无 xfail）
    - `grep -c "def test_legacy_llm_success" tests/unit/application/conversation/test_send_message_handler.py` == 0（已删）
    - `PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py` exits 0（全绿）
  </acceptance_criteria>
  <done>
    DataChat 主链把真实 principal_context 透传给 execute_plan（治理由已在链路的 runtime_service.execute post_compile 完成，handler 零自建治理管线——决策修订第 1 项）；两条物理直表路删尽（无 _execute_query/AdapterFactory/generate_sql/LEGACY_DISCLAIMER）；_handle_via_legacy_llm 两条路径（None / 有值）均 return _build_unanswerable_fallback 不落空；agent 全局会话短路；三类答不出统一 _build_unanswerable_fallback(source='fallback')；不新增 DI 注入；Wave 1 RED/xfail 全转绿，两测试文件全绿。
  </done>
</task>

</tasks>

<verification>
- `PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py` 全绿，无 xfail 残留。
- principal 透传：send_message_handler.py 含 `principal_context=command.principal_context`，无 `viewer_roles=[]`。
- 决策修订第 1 项合规：send_message_handler.py 不含 pre_route/post_compile/compile_preview/PrincipalResolver(（治理不重复实现，由下游 runtime_service.execute 完成）。
- 物理路净删：send_message_handler.py 中 _execute_query / AdapterFactory / LEGACY_DISCLAIMER / generate_sql 全部 0 计数。
- 兜底落点确定性：_handle_via_legacy_llm 的 None 与有值两条路径均 `return self._build_unanswerable_fallback`（>= 2 计数）。
- 拒匿名：conversations.py send-message 路由 @require_auth。
- 不新增 DI 注入：container.py send_message_handler Factory 段无新增依赖（本期 handler 不自建治理、不接 access_policy_service/compiler_service/principal_resolver）。
- 原子批次完整性：本 plan 的 4 件事在同一 wave 落地，无分批上线缺口。
</verification>

<success_criteria>
- DataChat 主链把真实 principal_context（access_role_bindings 治理主体）透传给 execute_plan；治理裁决由已在链路的 runtime_service.execute post_compile 完成，handler 零自建治理管线（决策修订第 1 项）。
- send-message 拒匿名，principal 经 command 透传，不写死空角色。
- 两条物理直表旁路彻底删除，无 env 灰度回退口子；_handle_via_legacy_llm 两条路径均落诚实兜底（不落空）。
- 三类答不出统一收敛 _build_unanswerable_fallback（source='fallback'/status='unanswerable'/via_semantic_layer is False）。
- 不新增 DI 注入（不接 access_policy_service/compiler_service/principal_resolver）。
- 两个受影响测试文件全绿无 xfail。
- 用户须在部署后给 DataChat 主体绑 data_m1_reader（user_setup，否则下游 post_compile 全 deny → 全兜底）。
</success_criteria>

<output>
完成后创建 .planning/phases/08.1-consume-04/08.1-02-SUMMARY.md，须记录：
- principal 透传落点（execute_plan 调用改 principal_context=command.principal_context + viewer_roles=command.viewer_roles，删 viewer_roles=[]）
- **关键归档（决策修订第 1 项）**：治理裁决未在 handler 重复实现——execute_plan→runtime_service.execute 的 post_compile（runtime_service.py:113）已在链路并强制 deny；handler 只补 principal 透传。避免后续读者误以为"DataChat 主链零治理"而重复引入 post_compile。
- 删除的物理路清单（_execute_query/AdapterFactory/LEGACY_DISCLAIMER/generate_sql 物理分支）
- _handle_via_legacy_llm 收口：None 与有值两条路径均 return _build_unanswerable_fallback（删 else 物理体后不落空）
- _build_unanswerable_fallback 文案与三处收敛点（deny/blocked、未命中、agent 软失败）
- **归档结论（observe 纠偏）**：CONTEXT 硬约束第 4 项「observe 在 router 路径阻断」经对账证伪——deny=`data_policy_not_matched`（access.py:693）发生在 row_scope 求值之前、与 `RLS_ENFORCEMENT_MODE` 无关；本期不改 RLS 模式，出数钥匙=主体绑 `data_m1_reader`。
- **归档结论（硬约束③）**：访问策略/执行档已 seed active（m1_aggregate_read allow / mc_m1_reader），本期不配新策略。
- D4 运维桥接待办：部署后绑 data_m1_reader + 真实 DataChat 问"学生答题统计 总数"断真实出数（否则下游 post_compile 全 deny）
</output>
</content>
</invoke>
