---
phase: 08.1-consume-04
plan: 02
type: tdd
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - app/application/conversation/commands/send_message.py
  - app/interfaces/api/v1/conversations.py
  - app/application/conversation/handlers/send_message_handler.py
  - app/di/container.py
  - tests/unit/application/conversation/test_send_message_handler.py
  - tests/integration/semantic/test_datachat_official_consume.py
requirements: [CONSUME-04]
autonomous: true
user_setup:
  - service: access_role_bindings
    why: "完整治理对齐后，DataChat 登录主体若无 data 角色 → post_compile 全 deny → 全走诚实兜底（答不出）。出数前提是给目标主体绑 data_m1_reader（答题 cube 物理表 dws_* → M1）。这是数据动作（运维），非代码，本期同批落地。"
    dashboard_config:
      - task: "给 DataChat 目标登录主体绑定 data_m1_reader 角色（经 access API PUT role-bindings 或 AccessIdentityService.ensure_principal_role_bindings）；data_m1_reader 权限包已含 data_m0_reader"
        location: "access 管理 API / 容器内 DB access_role_bindings 表（部署后执行，归入 D4 运维桥接）"

must_haves:
  truths:
    - "DataChat 主链经 AccessPolicyDecisionService.pre_route/post_compile，与 /agent/semantic/plan 同治理裁决"
    - "send-message 入口拒匿名（@require_auth），无 Bearer 返回 401"
    - "principal 解析（碰 g）只在 interfaces 层（conversations.py），经 SendMessageCommand 透传进 handler"
    - "handler 不再写死 viewer_roles=[]，透传真实 principal_context/viewer_roles"
    - "handler 内归一 principal 直接 PrincipalResolver().resolve()（纯函数实例化，不经 DI/不注入构造）——遵守决策 4 principal 注入瘦身"
    - "两条物理直表旁路彻底删除：_handle_via_legacy_llm 物理分支 + _execute_query + LEGACY_DISCLAIMER 删；_handle_via_agent 对全局会话(dataset_id is None) return None 短路"
    - "三类答不出统一收敛 _build_unanswerable_fallback()（source='fallback'/status='unanswerable'），不产 SQL 不碰物理表；dataset_id is None 与 dataset 有值两条主链全失败路径均落 fallback"
    - "治理 deny → 落诚实兜底（不伪造出数）"
  artifacts:
    - path: "app/application/conversation/commands/send_message.py"
      provides: "SendMessageCommand 加 principal_context/viewer_roles 可选字段"
      contains: "principal_context"
    - path: "app/application/conversation/handlers/send_message_handler.py"
      provides: "治理对齐（pre_route/post_compile + PrincipalResolver().resolve 纯函数归一）+ 统一兜底 + 物理路删除 + agent 全局短路"
      contains: "_build_unanswerable_fallback"
    - path: "app/interfaces/api/v1/conversations.py"
      provides: "send-message 拒匿名 + principal 透传"
      contains: "require_auth"
    - path: "app/di/container.py"
      provides: "send_message_handler 注入 access_policy_service + compiler_service（execution_compiler_preview_service）；不注入 principal_resolver（决策 4：handler 内用纯函数实例化）"
      contains: "access_policy_service"
  key_links:
    - from: "send_message_handler._handle_via_semantic_router"
      to: "AccessPolicyDecisionService.pre_route/post_compile"
      via: "DI 注入 access_policy_service + compiler_service，principal 经 PrincipalResolver().resolve 纯函数归一，compiled_targets 经 compile_preview 重编译"
      pattern: "pre_route|post_compile"
    - from: "conversations.py send_message"
      to: "SendMessageCommand"
      via: "principal_context_from_bearer() 解析后填入 command"
      pattern: "principal_context_from_bearer"
    - from: "send_message_handler 各回退分支"
      to: "_build_unanswerable_fallback"
      via: "deny / 未命中 / agent 软失败统一收敛"
      pattern: "_build_unanswerable_fallback"
---

<objective>
Wave 2（GREEN 原子批次）：把 Wave 1 全部 RED 断言转绿。这是 Phase 8.1 的核心交付，**五件事必须同批落地（CONTEXT specifics + Pitfall 2：分开上线会制造"看似对齐实则未对齐"合规错觉）**：

1. **治理对齐（方案 B）**：`_handle_via_semantic_router` 内引入 `AccessPolicyDecisionService.pre_route/post_compile`，principal 归一用 `PrincipalResolver().resolve(...)` 纯函数实例化（**不经 DI、不注入 handler 构造**——决策 4），compiled_targets 经 `compile_preview` 重编译（Wave 1 探查已坐实 execute_plan 输出顶层无 resource_set，不能直喂）。
2. **principal 透传 + 拒匿名**：`SendMessageCommand` 加可选字段；`conversations.py` `@optional_auth`→`@require_auth` + `principal_context_from_bearer()` 填 command；handler 透传不写死 `viewer_roles=[]`。
3. **删物理直表旁路①**：legacy 第 3 层 `_handle_via_legacy_llm` 物理分支 + `_execute_query` + `LEGACY_DISCLAIMER` 全删。
4. **删物理直表旁路②**：`_handle_via_agent` 对 `dataset_id is None` 全局会话 `return None` 短路（消除残留 running AgentQueryLog 死路径）。
5. **统一诚实兜底**：`_build_unanswerable_fallback()`（`source='fallback'`/`status='unanswerable'`），三类答不出统一收敛。

Purpose: 让 DataChat 真正符合 L1→L2 治理纪律，堵死"绕治理 / 绕 gateway / 直扫物理表产 SQL"红线。
Output: 治理对齐 + 拒匿名 + 物理收口 + 统一兜底的 production 代码 + DI 装配，Wave 1 RED/xfail 全部转绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-consume-04/08.1-CONTEXT.md
@.planning/phases/08.1-consume-04/08.1-RESEARCH.md
@.planning/phases/08.1-consume-04/08.1-01-SUMMARY.md

<interfaces>
<!-- 执行者直接使用以下契约，无需回查代码库。来自逐行核实。 -->

治理对齐范式（DataChat 须复刻）— agent_plan_handler.py:38-83:
```python
principal = self._principal_resolver.resolve(principal_context=..., viewer_roles=...)
pre_decision = self._access_policy_service.pre_route(principal=principal)
if pre_decision.decision == "deny": return <早退 with policy_decision=pre_decision>
# plan/编译 ...
compiled_targets = [self._compile_target(t, principal_context=..., viewer_roles=..., runtime_mode="official")
                    for t in plan.get("execution_targets", [])]
post_decision = self._access_policy_service.post_compile(principal=principal, compiled_targets=compiled_targets, ...)
```
> 注意：agent_plan_handler 经 DI 持有 `self._principal_resolver`（其构造已注入）。**DataChat handler 不复刻这一注入**——决策 4 明令 DataChat handler 不注入 PrincipalResolver，handler 内直接 `PrincipalResolver().resolve(...)` 纯函数实例化（见下）。

_compile_target 重编译（产出含 resource_set 的 target）— agent_plan_handler.py:85-110:
```python
preview = self._compiler_service.compile_preview(
    target_type=str(target.get("target_type") or ""), metric_name=target.get("metric_name"),
    question=target.get("question"), retrieval_query=target.get("retrieval_query"),
    retrieval_sources=target.get("retrieval_sources"), tool_name=target.get("tool_name"),
    tool_arguments=target.get("tool_arguments"), analysis_intent=target.get("analysis_intent"),
    query_dsl=target.get("query_dsl"), principal_context=principal_context,
    viewer_roles=viewer_roles, runtime_mode=runtime_mode)
return {"target": target, **preview}   # preview 含 resource_set/sql_hash/data_level
```

PrincipalResolver — access.py:185-192（**无 __init__，纯函数类，可直接实例化，不进 DI**）:
```python
class PrincipalResolver:
    def resolve(self, *, principal_context: dict | None, viewer_roles: list[str] | None = None,
                authenticated_user: dict | None = None) -> PrincipalContext: ...
```
  → DataChat handler 内归一写法（决策 4 合规）:
    `from app.application.governance.access import PrincipalResolver`
    `principal = PrincipalResolver().resolve(principal_context=command.principal_context or {}, viewer_roles=command.viewer_roles or [])`
  决策 4 禁的是「构造注入 PrincipalResolver 到 handler」，不禁用纯函数实例化（resolve 不碰 g）。

AccessPolicyDecisionService — access.py:476/489/509:
  pre_route(*, principal: PrincipalContext) -> PolicyDecisionResult
  post_compile(*, principal, compiled_targets: list[dict], approval_id=None,
               dimension_resolver: Optional[Callable]=None, row_scope_mode="evaluate") -> PolicyDecisionResult
  PolicyDecisionResult.decision ∈ {"allow","deny",...}；deny 根因 data_policy_not_matched (:693-714)
  → dimension_resolver 仅当 compiled_target 命中带 row_scope 的 policy 模板时才需要（未提供则对该模板 fail closed）。
    本期目标 cube 为 M1（m1_aggregate_read 无 row_scope，seed.py:602），正常路径**不触发** row_scope → 省 dimension_resolver 安全。
    若后续主体触及 M2 row_scope（m2_detail_read）需补 dimension_resolver（参照 agent 入口 _compile_target 的编译产物）。

DI 装配点 — container.py:
  :905 principal_resolver = Singleton(PrincipalResolver)   # ← agent 入口用；DataChat handler **不注入**它
  :907 access_policy_service = Singleton(AccessPolicyDecisionService, rls_enforcement_mode=config.rls_enforcement_mode)
  :923 execution_compiler_preview_service = Singleton(...)   # compile_preview 提供方
  :1521 send_message_handler = Factory(SendMessageHandler, ...)  # ← 在此追加注入 access_policy_service + compiler_service（不加 principal_resolver）

interfaces 层 principal 解析 — principal_context.py:12/20（与 agent.py:78-79 同口径）:
  authenticated_user_from_g() -> dict
  principal_context_from_bearer(*, source: str = "bearer") -> dict   # 无 principal raise AuthenticationError

require_auth — auth.py:26（注入 g.user_id/g.principal_id/g.user_roles；无 Bearer raise AuthenticationError("MISSING_TOKEN")）

列约束（兜底合法）— conversation.py:168 source=String(32)；agent_query_log.py:39 status=String(20)：
  source='fallback'(8)、status='unanswerable'(12) 均合法；via_semantic_layer = source=='semantic' → fallback ⇒ is False

当前写死治理缺口 — send_message_handler.py:101: viewer_roles=[]  （须改透传 command.viewer_roles）
当前 _handle_via_legacy_llm 终点结构 — :84 handle() 把 _handle_via_legacy_llm 作为必返回终点（-> Dict 非 Optional）；
  :311 _handle_via_legacy_llm 内：`if conversation.dataset_id is None:` 兜底 return（当前 source='legacy_llm'）
       `else:` find_by_id → generate_sql → _execute_query（物理出数，须删）
  → 删 else 物理体后，dataset 有值路径会落空——必须显式 `return self._build_unanswerable_fallback(...)`，否则函数无返回。
当前物理出口 — send_message_handler.py:355 generate_sql / :364 _execute_query / :454-464 _execute_query+AdapterFactory（须删）
当前 LEGACY_DISCLAIMER — send_message_handler.py:279（须删）
当前 agent 全局死路径 — _handle_via_agent :202（开头须加 dataset_id is None → return None）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SendMessageCommand 加字段 + conversations.py 拒匿名 + principal 透传</name>
  <files>app/application/conversation/commands/send_message.py, app/interfaces/api/v1/conversations.py</files>
  <read_first>
    - app/application/conversation/commands/send_message.py（被改文件，当前仅 3 字段）
    - app/interfaces/api/v1/conversations.py（被改文件，:19-20 @optional_auth、:157-188 send_message、:179-181 command 构造）
    - app/interfaces/api/v1/agent.py:13-14/73-79（require_auth + principal_context_from_bearer + authenticated_user_from_g 对齐口径，参照）
    - app/interfaces/api/v1/principal_context.py:12/20（principal 解析签名）
    - app/interfaces/api/middleware/auth.py:26（require_auth 注入 g 与异常）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q4 principal 透传 + 拒匿名）
  </read_first>
  <behavior>
    - SendMessageCommand 加 principal_context: dict | None = None、viewer_roles: list[str] | None = None（dataclass 默认值，所有现有构造点零改动兼容）。
    - conversations.py send-message 的 @optional_auth → @require_auth；无 Bearer 经 AuthenticationError → error_handler 统一 401（不手写 if not user）。
    - command 构造处填 principal_context=principal_context_from_bearer(source="datachat_bearer")、viewer_roles=（从 principal_context 或 authenticated_user 派生，与 agent 同口径）。
    - test_send_message_requires_auth（集成，本 task 在 conversations API 集成测试坐实）：无 Bearer → 401。
  </behavior>
  <action>
    1. app/application/conversation/commands/send_message.py：在 SendMessageCommand 末尾加
       `principal_context: dict | None = None`
       `viewer_roles: list[str] | None = None`
       （保持 dataclass，默认 None，向后兼容）。
    2. app/interfaces/api/v1/conversations.py：
       - import：`from app.interfaces.api.middleware.auth import require_auth`（替换/新增，保留其他装饰器现状）；`from app.interfaces.api.v1.principal_context import principal_context_from_bearer, authenticated_user_from_g`。
       - send_message 路由（:157-159）装饰器 `@optional_auth` → `@require_auth`。
       - command 构造（:181）：先 `pc = principal_context_from_bearer(source="datachat_bearer")`，再追加 `principal_context=pc`、`viewer_roles=pc.get("roles") or []`（避免重复调用）。
       - user_id 行（:179）改用 `g.user_id`（require_auth 已注入，不再 `or 'anonymous'`）。
       - 仅改 send_message 路由；其余 GET/DELETE 路由本期不动（保持 optional_auth 现状，最小改动）。
    3. 不在本 task 改 handler（handler 在 Task 2 改）。
  </action>
  <verify>
    <automated>pytest tests/unit/application/conversation/test_send_message_handler.py -k "semantic_router" -x 2>&1 | grep -E "passed|PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "principal_context" app/application/conversation/commands/send_message.py` >= 1
    - `grep -c "viewer_roles" app/application/conversation/commands/send_message.py` >= 1
    - `grep -c "require_auth" app/interfaces/api/v1/conversations.py` >= 1
    - `grep -c "principal_context_from_bearer" app/interfaces/api/v1/conversations.py` >= 1
    - send_message 路由不再 optional_auth：`sed -n '157,160p' app/interfaces/api/v1/conversations.py | grep -c "@require_auth"` >= 1
    - `python -c "from app.application.conversation.commands.send_message import SendMessageCommand; SendMessageCommand(conversation_id=1, user_id='u', content='q')"` exits 0（旧构造点零改动兼容）
    - `make typecheck-backend` 或 `python -c "import app.interfaces.api.v1.conversations"` 无导入/语法错
  </acceptance_criteria>
  <done>
    SendMessageCommand 带可选 principal 字段且旧构造兼容；send-message 拒匿名（require_auth）+ principal 经 command 透传；handler 未动。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 治理对齐 + 统一兜底 + 删两条物理路 + agent 全局短路（核心原子批次）+ DI 装配</name>
  <files>app/application/conversation/handlers/send_message_handler.py, app/di/container.py, tests/unit/application/conversation/test_send_message_handler.py, tests/integration/semantic/test_datachat_official_consume.py</files>
  <read_first>
    - app/application/conversation/handlers/send_message_handler.py（被改文件主体，全文：:25 __init__、:89 _handle_via_semantic_router、:99-102 写死 viewer_roles=[]、:108 _build_blocked_route_message、:202 _handle_via_agent、:279 LEGACY_DISCLAIMER、:281 _record_query_log、:311 _handle_via_legacy_llm、:313 dataset_id is None 块、:333+ else 物理体、:454 _execute_query）
    - app/application/agent/handlers/agent_plan_handler.py:38-110（pre_route/post_compile/_compile_target 范式——复刻治理调用与重编译，但**不复刻** principal_resolver 注入）
    - app/application/governance/access.py:185-192（PrincipalResolver.resolve，纯函数，直接实例化）/:476-509（pre_route/post_compile 签名，post_compile 含 dimension_resolver 可选参）/:693-714（deny 根因 data_policy_not_matched，发生在 row_scope 之前、与 RLS 模式无关）
    - app/application/execution_compiler/runtime_service.py:277-289（execution_results 主返回，确认顶层无 resource_set）
    - app/di/container.py:905-923/:1521-1527（DI 装配点；:905 principal_resolver 是 agent 入口用，本 handler 不注入）
    - app/domain/entities/conversation.py:168/192 + app/domain/entities/agent_query_log.py:39（列约束）
    - .planning/phases/08.1-consume-04/08.1-01-SUMMARY.md（Wave 1 探查结论：execution_results 顶层无 resource_set → 用 compile_preview 重编译）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q1/Q2/Q3/Q5 全部，含 Q1(b) observe 纠偏结论）
  </read_first>
  <behavior>
    - 治理对齐：_handle_via_semantic_router 透传 command.principal_context/command.viewer_roles 给 execute_plan（不再 viewer_roles=[]）；并经 `PrincipalResolver().resolve(principal_context=command.principal_context or {}, viewer_roles=command.viewer_roles or [])` **纯函数实例化**归一（决策 4：不注入 resolver 到 handler 构造）→ AccessPolicyDecisionService.pre_route；pre_route deny → 落 _build_unanswerable_fallback；命中出数前用 self._compiler_service.compile_preview 对 plan["execution_targets"] 重编译得 compiled_targets（含 resource_set），调 post_compile（本期 M1 无 row_scope，省 dimension_resolver 安全）；post_compile deny → 落 _build_unanswerable_fallback（不伪造出数）；allow → 维持现有出数 + Message(source='semantic') 落库，并把 policy_decision 落 trace。
    - 统一诚实兜底：新增 _build_unanswerable_fallback(conversation, user_message, *, reason) → 落 Message(source='fallback')，_record_query_log(status='unanswerable')；三类答不出（semantic 软失败/未命中、agent 软失败、deny）统一收敛。ai_message.to_dict()['via_semantic_layer'] is False。
    - 删物理旁路①：删 _handle_via_legacy_llm 的物理 else 分支（find_by_id + generate_sql + _execute_query 调用）+ _execute_query 方法 + LEGACY_DISCLAIMER 常量；**dataset_id is None 兜底块改调 _build_unanswerable_fallback（source 由 'legacy_llm' → 'fallback'）**；删 else 物理体后 **dataset 有值路径也必须 `return self._build_unanswerable_fallback(...)`**（_handle_via_legacy_llm 是 :84 终点，-> Dict 非 Optional，不能落空）。两条路径都落 fallback。
    - 删物理旁路②：_handle_via_agent 开头 `if conversation.dataset_id is None: return None`（建 AgentQueryLog 前短路，消除残留 running log）。
    - DI：container.py send_message_handler Factory 追加注入 access_policy_service / execution_compiler_preview_service(作 compiler_service)；**不注入 principal_resolver**（决策 4：handler 内用 PrincipalResolver() 纯函数实例化）。handler __init__ 接这两个依赖（access_policy_service + compiler_service）。
    - 转绿 Wave 1：test_unanswerable_fallback_via_semantic_layer_false / test_agent_global_session_short_circuits / test_agent_failure_falls_back_to_honest_fallback 通过；删除 xfail 的 test_legacy_llm_success / test_execute_query_rejects_non_select（方法已删）；集成层去掉 :313/治理/deny 三处 xfail 转绿；新增「dataset_id 有值 + agent 失败 → source=='fallback'」断言。
  </behavior>
  <action>
    在 app/application/conversation/handlers/send_message_handler.py：
    1. __init__（:25）追加参数 `access_policy_service`、`compiler_service`，存为 self._access_policy_service / self._compiler_service。**不加 principal_resolver 参数**——handler 内归一 principal 时直接 `from app.application.governance.access import PrincipalResolver`（模块顶部 import）然后 `PrincipalResolver().resolve(...)`（纯函数实例化，不碰 g，决策 4 合规）。
    2. _handle_via_semantic_router（:89）：
       - execute_plan 调用（:99-102）`viewer_roles=[]` → `viewer_roles=command.viewer_roles or [], principal_context=command.principal_context`。
       - 在出数落库前插入治理：
         `principal = PrincipalResolver().resolve(principal_context=command.principal_context or {}, viewer_roles=command.viewer_roles or [])`
         `pre = self._access_policy_service.pre_route(principal=principal)`
         `if pre.decision == "deny": return self._build_unanswerable_fallback(conversation, user_message, reason=pre.reason)`
         `compiled_targets = [{"target": t, **self._compiler_service.compile_preview(target_type=str(t.get("target_type") or ""), metric_name=t.get("metric_name"), question=t.get("question"), retrieval_query=t.get("retrieval_query"), retrieval_sources=t.get("retrieval_sources"), tool_name=t.get("tool_name"), tool_arguments=t.get("tool_arguments"), analysis_intent=t.get("analysis_intent"), query_dsl=t.get("query_dsl"), principal_context=command.principal_context, viewer_roles=command.viewer_roles or [], runtime_mode="official")} for t in (plan_result.get("plan", {}).get("execution_targets") or plan_result.get("execution_targets") or [])]`
         `post = self._access_policy_service.post_compile(principal=principal, compiled_targets=compiled_targets)`  # 本期 M1 无 row_scope，省 dimension_resolver 安全
         `if post.decision == "deny": return self._build_unanswerable_fallback(conversation, user_message, reason=post.reason)`
       - 成功路径维持现有 Message(source='semantic') 落库，把 post.to_dict() 写入 trace/context。
       - 无 execution_results（未命中/软失败）→ 改走 _build_unanswerable_fallback（替换原 _build_blocked_route_message 返回 None 后落 legacy 的链路；保留"运行时未就绪"blocked→source=='semantic' 分支不动）。
    3. 新增方法 _build_unanswerable_fallback(self, conversation, user_message, *, reason: str | None = None) -> Dict[str, Any]：落 Message(source='fallback', content=诚实文案如"未能在已发布的语义资产中找到可回答该问题的口径，请补充建模或换一种问法。")，调 self._record_query_log(..., status='unanswerable')，返回响应 dict。
    4. _handle_via_agent（:202）开头加 `if conversation.dataset_id is None: return None`。
    5. _handle_via_legacy_llm（:311）收口：
       - dataset_id is None 兜底块（:313-332）改调 `return self._build_unanswerable_fallback(conversation, user_message, reason="未命中已发布语义资产")`（source 'legacy_llm' → 'fallback'）。
       - 删 else 物理体（find_by_id :333+ / generate_sql :355 / _execute_query :364 / AdapterFactory）；删后 **dataset 有值路径也必须 `return self._build_unanswerable_fallback(conversation, user_message, reason="未命中已发布语义资产")`**（_handle_via_legacy_llm 是 :84 终点，非 Optional，不能落空）。
       - 删除 _execute_query（:454-464）方法、LEGACY_DISCLAIMER（:279）常量。
       - 收口后 _handle_via_legacy_llm 两条路径（dataset_id is None / 有值）均 return _build_unanswerable_fallback；若两路完全同形可整体合并为单一 return，但**两路必须都有显式 return**。
    在 app/di/container.py：
    6. send_message_handler Factory（:1521）追加 `access_policy_service=access_policy_service`、`compiler_service=execution_compiler_preview_service`；**不追加 principal_resolver=principal_resolver**（决策 4：handler 内 PrincipalResolver() 纯函数实例化）。
    在测试文件：
    7. test_send_message_handler.py：删除 xfail 的 test_legacy_llm_success、test_execute_query_rejects_non_select（对应方法已删）；新 RED 用例转绿（fixture handler 构造追加 mock access_policy_service / compiler_service；**不传 principal_resolver**——handler 内部 PrincipalResolver() 实例化，单测可对 PrincipalResolver 真实纯函数放行或 patch 其 resolve）。
    8. test_datachat_official_consume.py：去掉 :313/test_two_entrances_policy_decision_parity/test_governance_deny_falls_back 的 xfail 标记，使其真实通过（注入种子 policy + 主体角色 stub，或 stub access_policy_service 返回 allow/deny）；新增「dataset_id 有值 + 主链全失败（agent 软失败）→ source=='fallback'」断言（坐实删 else 物理体后有值路径落 fallback）。
  </action>
  <verify>
    <automated>pytest tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "return self._build_unanswerable_fallback" app/application/conversation/handlers/send_message_handler.py` >= 2（覆盖 _handle_via_legacy_llm 的 None 与有值两条路径，确保删 else 物理体后均不落空）
    - `grep -c "def _build_unanswerable_fallback" app/application/conversation/handlers/send_message_handler.py` == 1（方法定义）
    - `grep -c "PrincipalResolver()" app/application/conversation/handlers/send_message_handler.py` >= 1（纯函数实例化归一 principal）
    - `grep -c "principal_resolver" app/di/container.py | head -1` —— send_message_handler Factory 段**不含** principal_resolver=（决策 4）；核验：`sed -n '1521,1535p' app/di/container.py | grep -c "principal_resolver"` == 0
    - `grep -c "self._principal_resolver" app/application/conversation/handlers/send_message_handler.py` == 0（handler 不持注入的 resolver）
    - `grep -c "LEGACY_DISCLAIMER" app/application/conversation/handlers/send_message_handler.py` == 0
    - `grep -c "def _execute_query" app/application/conversation/handlers/send_message_handler.py` == 0
    - `grep -c "AdapterFactory" app/application/conversation/handlers/send_message_handler.py` == 0
    - `grep -c "viewer_roles=\[\]" app/application/conversation/handlers/send_message_handler.py` == 0
    - `grep -c "pre_route" app/application/conversation/handlers/send_message_handler.py` >= 1
    - `grep -c "post_compile" app/application/conversation/handlers/send_message_handler.py` >= 1
    - `grep -c "compile_preview" app/application/conversation/handlers/send_message_handler.py` >= 1
    - `grep -c "dataset_id is None" app/application/conversation/handlers/send_message_handler.py` >= 1（agent 短路）
    - `grep -c "access_policy_service=access_policy_service" app/di/container.py` >= 1
    - `grep -c "xfail" tests/integration/semantic/test_datachat_official_consume.py` == 0（GREEN 后无 xfail）
    - `grep -c "def test_legacy_llm_success" tests/unit/application/conversation/test_send_message_handler.py` == 0（已删）
    - `grep -c "generate_sql" app/application/conversation/handlers/send_message_handler.py` == 0（物理 SQL 生成路径净删）
    - `pytest tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py` exits 0（全绿）
  </acceptance_criteria>
  <done>
    DataChat 主链经 pre_route/post_compile 治理裁决（compiled_targets 经 compile_preview 重编译，principal 经 PrincipalResolver() 纯函数实例化归一、未注入 DI——决策 4）；两条物理直表路删尽（无 _execute_query/AdapterFactory/generate_sql/LEGACY_DISCLAIMER）；_handle_via_legacy_llm 两条路径（None / 有值）均 return _build_unanswerable_fallback 不落空；agent 全局会话短路；三类答不出统一 _build_unanswerable_fallback(source='fallback')；DI 注入 access_policy_service + compiler_service（不注入 principal_resolver）；Wave 1 RED/xfail 全转绿，两测试文件全绿。
  </done>
</task>

</tasks>

<verification>
- `pytest tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py` 全绿，无 xfail 残留。
- `grep` 核验物理路净删：send_message_handler.py 中 _execute_query / AdapterFactory / LEGACY_DISCLAIMER / generate_sql / viewer_roles=[] 全部 0 计数。
- 治理对齐：send_message_handler.py 含 pre_route + post_compile + compile_preview + PrincipalResolver()（纯函数实例化）。
- 决策 4 合规：container.py send_message_handler Factory 段不含 principal_resolver=；handler 不持 self._principal_resolver。
- DI 注入：container.py send_message_handler 含 access_policy_service / compiler_service（execution_compiler_preview_service），不含 principal_resolver。
- 兜底落点确定性：_handle_via_legacy_llm 的 None 与有值两条路径均 `return self._build_unanswerable_fallback`（>= 2 计数）。
- 拒匿名：conversations.py send-message 路由 @require_auth。
- 原子批次完整性：本 plan 的 5 件事在同一 wave 落地，无分批上线缺口。
</verification>

<success_criteria>
- DataChat 主链与 /agent/semantic/plan 走同一 AccessPolicyDecisionService 裁决。
- principal 归一在 handler 内用 PrincipalResolver() 纯函数实例化，未经 DI 注入（决策 4 principal 注入瘦身）。
- send-message 拒匿名，principal 经 command 透传，不写死空角色。
- 两条物理直表旁路彻底删除，无 env 灰度回退口子；_handle_via_legacy_llm 两条路径均落诚实兜底（不落空）。
- 三类答不出统一收敛 _build_unanswerable_fallback（source='fallback'/status='unanswerable'/via_semantic_layer is False）。
- 两个受影响测试文件全绿无 xfail。
- 用户须在部署后给 DataChat 主体绑 data_m1_reader（user_setup，否则全 deny → 全兜底）。
</success_criteria>

<output>
完成后创建 .planning/phases/08.1-consume-04/08.1-02-SUMMARY.md，须记录：
- 治理对齐落点（compile_preview 重编译 + pre_route/post_compile 插入位置 + principal 经 PrincipalResolver() 纯函数实例化归一、未注入 DI——决策 4）
- 删除的物理路清单（_execute_query/AdapterFactory/LEGACY_DISCLAIMER/generate_sql 物理分支）
- _handle_via_legacy_llm 收口：None 与有值两条路径均 return _build_unanswerable_fallback（删 else 物理体后不落空）
- _build_unanswerable_fallback 文案与三处收敛点
- DI 新增注入（access_policy_service + compiler_service；**不含** principal_resolver）
- **归档结论（observe 纠偏）**：CONTEXT 硬约束第 4 项「observe 在 router 路径阻断」经 RESEARCH Q1(b) 证伪为伪命题——deny=`data_policy_not_matched`（access.py:693）发生在 row_scope 求值之前、与 `RLS_ENFORCEMENT_MODE` 无关；本期**不改** RLS 模式，出数钥匙=主体绑 `data_m1_reader`。避免后续读者误判第 4 项被漏做。
- D4 运维桥接待办：部署后绑 data_m1_reader + 真实 DataChat 问"学生答题统计 总数"断真实出数（否则全 deny）
</output>
</content>
</invoke>
