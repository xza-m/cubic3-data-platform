---
phase: 08.1-consume-04
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/application/conversation/test_send_message_handler.py
  - tests/integration/semantic/test_datachat_official_consume.py
requirements: [CONSUME-04]
autonomous: true

must_haves:
  truths:
    - "存在失败断言坐实 DataChat 主链当前写死 viewer_roles=[] 且零 principal_context → 治理引擎角色空 → 即便构造含 data_m1_reader 的 principal_context 也未透传 → 不命中 m1_aggregate_read → deny（治理命门=principal 未透传，非无治理管线）"
    - "存在失败断言坐实 dataset_id is None 全局会话当前落 source=='legacy_llm'（应为 fallback）"
    - "存在失败断言坐实 _handle_via_agent 对全局会话未短路（建 running AgentQueryLog 死路径）"
    - "存在失败断言坐实 agent 软失败回退当前落 legacy 物理出数（应为统一诚实兜底 source=='fallback'）"
    - "存在失败断言坐实 send-message 入口 optional_auth 放行匿名（应拒匿名）"
  artifacts:
    - path: "tests/unit/application/conversation/test_send_message_handler.py"
      provides: "RED 断言：principal 透传命门 / 诚实兜底 source=fallback / agent 全局短路 / 物理路已删"
      contains: "test_unanswerable_fallback_via_semantic_layer_false"
    - path: "tests/integration/semantic/test_datachat_official_consume.py"
      provides: "RED 断言：principal 透传后命中出数 / deny(blocked)→fallback / :313 改 fallback"
      contains: "test_semantic_router_receives_principal_context"
  key_links:
    - from: "test_send_message_handler.py"
      to: "SendMessageHandler._handle_via_semantic_router / _handle_via_agent / _handle_via_legacy_llm"
      via: "pytest 断言（当前主链行为）"
      pattern: "via_semantic_layer is False|return None|source.*fallback|principal_context"
    - from: "test_datachat_official_consume.py"
      to: "semantic_router_service.execute_plan 入参 principal_context"
      via: "断言 execute_plan 收到 command.principal_context（透传命门）"
      pattern: "principal_context"
---

<objective>
Wave 1（RED）：用立即失败的断言坐实 Phase 8.1 要收口的全部缺陷。**治理命门按对账实测重述**——不是"DataChat 主链无治理管线"（实测 execute_plan→runtime_service.execute 的 post_compile 已在链路、runtime_service.py:113），而是"handler 写死 viewer_roles=[] + 零 principal_context → 真实角色到不了治理引擎 → 即便主体持 data_m1_reader 也 deny"。本 plan 不改任何 production 代码——只写/改测试，让它们对当前代码 **失败**（RED），为 Wave 2 GREEN 提供精确的转绿目标。

沿用本仓库 Phase 7/8 的 TDD 范式：wave 1 RED 立失败断言坐实缺陷 → wave 2 GREEN 最小实现转绿。

Purpose: 把"principal 未透传 → 治理引擎角色空 + 两条物理直表 + 兜底不统一 + 匿名放行"四个事实固化成可执行断言，杜绝 GREEN 阶段凭感觉改、或重复引入已在链路的 post_compile（对账修订第 1 项）。
Output: 改写后的两个测试文件，运行时对当前代码 RED（部分用 xfail 标注待 GREEN 转绿）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-consume-04/08.1-CONTEXT.md
@.planning/phases/08.1-consume-04/08.1-RESEARCH.md

<interfaces>
<!-- 执行者直接使用以下契约，无需回查代码库。来自逐行核实（2026-06-26 对账实测）。 -->

当前 SendMessageCommand（仅 3 字段，dataclass）— app/application/conversation/commands/send_message.py:7-12:
```python
@dataclass
class SendMessageCommand:
    conversation_id: int
    user_id: str
    content: str
```

当前 _handle_via_semantic_router 治理缺口 — send_message_handler.py:99-103:
```python
plan_result = self.semantic_router_service.execute_plan(
    question=command.content,
    viewer_roles=[],            # ← 写死空角色 + 零 principal_context，命门
    runtime_mode="official",
)
```

【治理已在链路（对账实测，RED 据此重述命门）】— runtime_service.py:
  :47 def execute(..., :60 principal_context, ...)
  :82 principal = self._principal_resolver.resolve(principal_context=principal_context, viewer_roles=viewer_roles)
  :113 policy_decision = self._access_policy_service.post_compile(principal=principal, compiled_targets=[preview], ...)
  :126 if policy_decision.decision != "allow": return {"status":"blocked", ...}
  → 含义：治理 post_compile 已在 execute_plan 下游强制 deny。命门是 handler 不传 principal_context，
    导致 _principal_resolver.resolve 拿到空 principal_context → 角色空 → 不命中 m1_aggregate_read → deny。
  → RED 不应断言"无治理管线"，应断言"principal 未透传"（execute_plan 被调用时 principal_context is None / viewer_roles==[]）。

execute_plan 已接受 principal 透传 — preview_service.py:582-601:
  def execute_plan(self, *, question, viewer_roles=None, principal_context=None, runtime_mode="official", ...)
  → GREEN 后 handler 应传 principal_context=command.principal_context；RED 坐实当前未传。

当前诚实兜底块（须在 GREEN 改 source） — send_message_handler.py:313-323:
  conversation.dataset_id is None → 落 Message(source='legacy_llm')，内容「未能在已发布的语义资产中找到…」

Message.source / via_semantic_layer — conversation.py:168/192:
  source = Column(String(32))                 # 'fallback'(8 字符) 合法
  'via_semantic_layer': self.source == 'semantic' if self.source else None
  → source='fallback' ⇒ via_semantic_layer is False（无需改 to_dict）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 改写单测立 RED 断言（principal 透传命门 / 诚实兜底 source=fallback / agent 全局短路 / 物理路删除）</name>
  <files>tests/unit/application/conversation/test_send_message_handler.py</files>
  <read_first>
    - tests/unit/application/conversation/test_send_message_handler.py（被改文件本身，尤其 fixture command、test_legacy_llm_success、test_agent_failure_falls_back_to_legacy_llm、test_execute_query_rejects_non_select 与 semantic_router 相关用例）
    - app/application/conversation/handlers/send_message_handler.py（:89 _handle_via_semantic_router、:99-103 写死 viewer_roles=[]、:202 _handle_via_agent、:311 _handle_via_legacy_llm、:313 dataset_id is None 块、:454 _execute_query、:279 LEGACY_DISCLAIMER）
    - app/application/semantic_router/preview_service.py:582-601（execute_plan 接受 principal_context/viewer_roles——GREEN 透传目标）
    - app/domain/entities/conversation.py:168/192（source / via_semantic_layer 计算）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q5 诚实兜底列约束、Q6 受影响测试全集表）
  </read_first>
  <behavior>
    - test_semantic_router_called_with_empty_principal_currently（新增，RED）：调 handler._handle_via_semantic_router（或 handle）后，spy semantic_router_service.execute_plan 的入参，断言**当前** principal_context is None 且 viewer_roles==[]（坐实命门）；该断言对当前代码 PASS（坐实现状），同时新增 test_semantic_router_should_receive_principal_context（RED/xfail，reason="08.1-02 透传"）断言 execute_plan 收到 command.principal_context（GREEN 转绿）。
    - test_unanswerable_fallback_via_semantic_layer_false（新增，RED）：全局会话（dataset_id is None）走兜底，断言 ai_message_entity.source == "fallback" 且 ai_message_entity.to_dict()["via_semantic_layer"] is False。对当前代码 RED（当前 source=="legacy_llm"）。
    - test_agent_global_session_short_circuits（新增，RED）：conversation.dataset_id is None 时 handler._handle_via_agent(...) 返回 None，且无 AgentQueryLog 被 db.session.add（无残留 running log）。对当前代码 RED（当前会建 log 后抛错被吞）。
    - test_agent_failure_falls_back_to_honest_fallback（重写 test_agent_failure_falls_back_to_legacy_llm）：agent 失败 → 诚实兜底，断言 source == "fallback"、llm_service.generate_sql.assert_not_called()、legacy_adapter.execute_query.assert_not_called()。对当前代码 RED（当前回退 legacy 物理出数）。
    - test_legacy_llm_success（标记 xfail/删除待 GREEN）：当前断言 source=="legacy_llm" + content.startswith(LEGACY_DISCLAIMER)——物理路删除后此行为消失，本 wave 用 @pytest.mark.xfail(reason="物理 legacy 路在 08.1-02 删除") 标注，GREEN plan 中删除该用例。
    - test_execute_query_rejects_non_select（标记 xfail/删除待 GREEN）：handler._execute_query 方法在 GREEN 删除，本 wave @pytest.mark.xfail(reason="_execute_query 在 08.1-02 删除") 标注。
  </behavior>
  <action>
    在 tests/unit/application/conversation/test_send_message_handler.py 中：
    1. 新增 test_semantic_router_should_receive_principal_context（@pytest.mark.xfail(strict=False, reason="08.1-02 principal 透传")）：构造 SendMessageCommand 含 principal_context={"principal_id":"p","roles":["data_m1_reader"]}（注意当前 Command 无此字段——本 RED 用 monkeypatch/简化 stub 表达期望，或先在测试内用 SimpleNamespace 模拟 command）；spy semantic_router_service.execute_plan，断言被调用时 kwargs["principal_context"] == command.principal_context。当前代码写死 viewer_roles=[]、不传 principal_context → RED（xfail）。
    2. 新增 test_unanswerable_fallback_via_semantic_layer_false：构造 conversation.dataset_id = None 的会话，semantic_router 返回无 execution_results（或不命中），agent 短路，断言最终 ai_message_entity.source == "fallback" 且 ai_message_entity.to_dict()["via_semantic_layer"] is False。
    3. 新增 test_agent_global_session_short_circuits：mock conversation.dataset_id = None，调 handler._handle_via_agent(command, conversation, user_message)，assert 返回 None；断言 datachat_channel.to_agent_request 未被调用且无 AgentQueryLog 被 db.session.add。
    4. 重写 test_agent_failure_falls_back_to_legacy_llm → 改名 test_agent_failure_falls_back_to_honest_fallback：保留 agent 失败触发，但断言 source == "fallback"、llm_service.generate_sql.assert_not_called()、legacy_adapter.execute_query.assert_not_called()。
    5. 给 test_legacy_llm_success 与 test_execute_query_rejects_non_select 各加 @pytest.mark.xfail(strict=False, reason="08.1-02 删除物理 legacy 路")。
    6. 不改 production 代码。新增/改写用例对当前代码失败或 xfail。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py -k "unanswerable_fallback_via_semantic_layer_false or agent_global_session_short_circuits or agent_failure_falls_back_to_honest_fallback"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_unanswerable_fallback_via_semantic_layer_false" tests/unit/application/conversation/test_send_message_handler.py` >= 1
    - `grep -c "test_agent_global_session_short_circuits" tests/unit/application/conversation/test_send_message_handler.py` >= 1
    - `grep -c "test_semantic_router_should_receive_principal_context" tests/unit/application/conversation/test_send_message_handler.py` >= 1
    - `grep -c "principal_context" tests/unit/application/conversation/test_send_message_handler.py` >= 1（坐实透传命门）
    - `grep -c "via_semantic_layer\"\] is False" tests/unit/application/conversation/test_send_message_handler.py` >= 1
    - `grep -c "xfail" tests/unit/application/conversation/test_send_message_handler.py` >= 2
    - `PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py -k "unanswerable_fallback_via_semantic_layer_false"` 该新增用例对当前代码 FAILED（RED 已坐实）
    - production 代码零改动：`git diff --name-only app/` 输出不含 send_message_handler.py
  </acceptance_criteria>
  <done>
    新增 RED 用例对当前代码失败（坐实 principal 未透传命门、source 应为 fallback、agent 全局应短路），物理 legacy 用例 xfail 标注待删，production 代码未动。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 集成层立 RED（principal 透传后命中 / deny(blocked)→fallback / :313 改 fallback）</name>
  <files>tests/integration/semantic/test_datachat_official_consume.py</files>
  <read_first>
    - tests/integration/semantic/test_datachat_official_consume.py（被改文件本身，尤其 test_official_consume_routes_to_cube_and_returns_rows、无快照诚实兜底、yaml_only_comment、:313 assert ai["source"] == "legacy_llm"、:245/:251 source=='semantic' 出数、:284 无快照 source=='semantic' blocked）
    - app/application/semantic_router/preview_service.py:582-614（execute_plan 接受 principal_context 并透传至 runtime_service.execute）
    - app/application/execution_compiler/runtime_service.py:47-130（principal_resolver.resolve→post_compile→decision!=allow→status=blocked；治理已在链路，集成层不重建治理引擎）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q1 治理塌缩纠偏、Q2 方案、Q3 物理收口）
  </read_first>
  <behavior>
    - test_semantic_router_receives_principal_context（新增，RED/xfail）：构造含 data_m1_reader 的 principal_context 经 send-message 链路，spy execute_plan 入参，断言 principal_context 已透传（GREEN 后命中出数）。当前 handler 写死 viewer_roles=[] → RED；@pytest.mark.xfail(reason="08.1-02 principal 透传")。
    - test_governance_deny_falls_back（新增，RED/xfail）：stub execute_plan 返回 status=='blocked'（模拟下游 post_compile deny，runtime_service.py:126）→ 断言 ai.source=='fallback'、无伪造出数（不在集成层重建治理引擎，治理由 runtime_service 覆盖）。当前 blocked 走 legacy → RED；xfail 标注。
    - :313 改写：assert ai["source"] == "legacy_llm" → 改 == "fallback"（D3 未命中走统一兜底）。本 wave 改断言但加 @pytest.mark.xfail(reason="兜底统一在 08.1-02")，GREEN 去掉 xfail。
    - :245/:251（test A source=='semantic' 出数）与 :284（无快照 source=='semantic' blocked）**保留不动**——成功出数与"运行时未就绪"走 semantic，不是 fallback。
  </behavior>
  <action>
    在 tests/integration/semantic/test_datachat_official_consume.py 中：
    1. 新增 test_semantic_router_receives_principal_context（@pytest.mark.xfail(strict=False, reason="08.1-02 principal 透传")）：经 send-message 链路（带含 data_m1_reader 的 principal_context）spy SemanticRouterPreviewService.execute_plan，assert 调用 kwargs 含非空 principal_context（坐实 GREEN 透传目标）。
    2. 新增 test_governance_deny_falls_back（@pytest.mark.xfail(strict=False, reason="08.1-02 统一兜底")）：stub _runtime_service.execute 或 execute_plan 返回 {"status":"blocked","reason":"data_policy_not_matched",...}（模拟下游治理 deny）→ 期望 ai["source"]=="fallback"，且无伪造出数。
    3. 改 :313 assert ai["source"] == "legacy_llm" → assert ai["source"] == "fallback"，给该用例加 @pytest.mark.xfail(strict=False, reason="08.1-02 统一兜底")。
    4. 不改 :245/:251/:284。不改 production 代码。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/integration/semantic/test_datachat_official_consume.py -k "principal or deny or source"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_semantic_router_receives_principal_context" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c "test_governance_deny_falls_back" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c "principal_context" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c 'assert ai\["source"\] == "fallback"' tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c 'assert ai\["source"\] == "legacy_llm"' tests/integration/semantic/test_datachat_official_consume.py` == 0
    - `grep -c "resource_set" tests/integration/semantic/test_datachat_official_consume.py` == 0（不再做 compiled_targets/resource_set 探查——handler 不自建 compile_preview，对账修订第 1 项）
    - production 代码零改动：`git diff --name-only app/` 输出为空
  </acceptance_criteria>
  <done>
    集成层 RED：principal 透传命门 + deny(blocked)→fallback + :313 改 fallback 以 xfail 标注待 GREEN 转绿，:245/:251/:284 不动，无 resource_set 探查残留，production 未改。
  </done>
</task>

</tasks>

<verification>
- `PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py` 运行：新 RED 用例失败或 xfail、:245/:251/:284 仍绿。
- `git diff --name-only app/` 为空（本 wave 不碰 production）。
- RED 命门口径正确：坐实"principal 未透传"（execute_plan 当前不收 principal_context），非"无治理管线"（post_compile 已在 runtime_service 链路）。
- 无 resource_set/compiled_targets 探查残留（handler 不自建 compile_preview，对账修订第 1 项）。
</verification>

<success_criteria>
- 两个测试文件含本 plan 列出的全部新增/改写用例。
- RED 命门坐实"principal 未透传 → 角色空 → deny"，而非"无治理管线"。
- RED 用例对当前代码失败或 xfail 标注。
- production 代码零改动。
- :313 已无 "legacy_llm" 断言（改 "fallback"，xfail 待 GREEN）。
- 无 resource_set 探查残留。
</success_criteria>

<output>
完成后创建 .planning/phases/08.1-consume-04/08.1-01-SUMMARY.md，须记录：
- 治理命门重述结论（principal 未透传，非无治理管线；post_compile 已在 runtime_service.py:113 链路）
- 全部新增/改写/xfail 用例清单
- Wave 2 GREEN 的精确转绿目标（principal 透传 + 物理收口 + 统一兜底，不自建治理）
</output>
</content>
