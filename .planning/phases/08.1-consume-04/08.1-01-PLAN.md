---
phase: 08.1-consume-04
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/application/conversation/test_send_message_handler.py
  - tests/integration/semantic/test_datachat_official_consume.py
requirements: [CONSUME-04]
autonomous: true

must_haves:
  truths:
    - "存在失败断言坐实 DataChat 主链当前写死 viewer_roles=[] 且未经 AccessPolicyDecisionService（治理缺口）"
    - "存在失败断言坐实 dataset_id is None 全局会话当前落 source=='legacy_llm'（应为 fallback）"
    - "存在失败断言坐实 _handle_via_agent 对全局会话未短路（建 running AgentQueryLog 死路径）"
    - "存在定向探查测试坐实 execute_plan 的 execution_results[i] 顶层不含 resource_set（post_compile 须重编译）"
    - "存在失败断言坐实 send-message 入口 optional_auth 放行匿名（应拒匿名）"
  artifacts:
    - path: "tests/unit/application/conversation/test_send_message_handler.py"
      provides: "RED 断言：诚实兜底 source=fallback / agent 全局短路 / 物理路已删"
      contains: "test_unanswerable_fallback_via_semantic_layer_false"
    - path: "tests/integration/semantic/test_datachat_official_consume.py"
      provides: "RED 断言：治理两入口一致 / deny→fallback / compiled_targets 探查"
      contains: "test_execute_plan_results_lack_top_level_resource_set"
  key_links:
    - from: "test_send_message_handler.py"
      to: "SendMessageHandler._handle_via_semantic_router / _handle_via_agent / _handle_via_legacy_llm"
      via: "pytest 断言（当前主链行为）"
      pattern: "via_semantic_layer is False|return None|source.*fallback"
    - from: "test_datachat_official_consume.py"
      to: "semantic_router_service.execute_plan 输出契约"
      via: "断言 execution_results[0] 不含 resource_set 顶层键"
      pattern: "resource_set"
---

<objective>
Wave 1（RED + 定向探查）：用立即失败的断言坐实 Phase 8.1 要收口的全部缺陷，并坐实 GREEN 阶段必须解决的 compiled_targets 来源契约（Open Question 1）。本 plan 不改任何 production 代码——只写/改测试，让它们对当前代码 **失败**（RED），为 Wave 2 GREEN 提供精确的转绿目标。

沿用本仓库 Phase 7/8 的 TDD 范式：wave 1 RED 立失败断言坐实缺陷 → wave 2 GREEN 最小实现转绿。

Purpose: 把"治理缺口 + 两条物理直表 + 兜底不统一 + 匿名放行 + compiled_targets 来源不明"五个事实固化成可执行断言，杜绝 GREEN 阶段凭感觉改。
Output: 改写后的两个测试文件，运行时对当前代码 RED（部分用 xfail/skip 标注待 GREEN 转绿的断言，部分为纯探查断言坐实当前契约）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-consume-04/08.1-CONTEXT.md
@.planning/phases/08.1-consume-04/08.1-RESEARCH.md

<interfaces>
<!-- 执行者直接使用以下契约，无需回查代码库。来自逐行核实。 -->

当前 SendMessageCommand（仅 3 字段，dataclass）— app/application/conversation/commands/send_message.py:
```python
@dataclass
class SendMessageCommand:
    conversation_id: int
    user_id: str
    content: str
```

当前 _handle_via_semantic_router 治理缺口 — send_message_handler.py:99-102:
```python
plan_result = self.semantic_router_service.execute_plan(
    ...
    viewer_roles=[],            # ← 写死空角色，治理缺口
    runtime_mode="official",
)
```

execute_plan 输出顶层键 — preview_service.py:622-642（execution_results 来自 runtime_service.execute）:
  semantic_plan_id / question / runtime_mode / business_intent / projection_result /
  resolved_bindings / planning_mode / route / plan / dependencies / expected_outputs /
  execution_targets / execution_results / execution_summary / traceability

runtime_service.execute 成功分支主返回顶层键 — runtime_service.py:277-289（**注意：不含顶层 resource_set**）:
  status / target_type / execution_request / result / bindings / policy /
  policy_decision / ticket_preview / traceability / governance_trace / audit_trace_id
  → resource_set 只埋在 governance_trace 内（_build_governance_trace :440-460），
    而 post_compile._normalize_resource_set（access.py:344+）读 target.get("resource_set") 顶层
  → 结论：execution_results 不能直接喂 post_compile，GREEN 须用 compile_preview 重编译

当前诚实兜底块（须在 GREEN 改 source） — send_message_handler.py:313-332:
  conversation.dataset_id is None → 落 Message(source='legacy_llm')，内容「未能在已发布的语义资产中找到…」

Message.source / via_semantic_layer — conversation.py:168/192:
  source = Column(String(32))                 # 'fallback'(8 字符) 合法
  'via_semantic_layer': self.source == 'semantic' if self.source else None
  → source='fallback' ⇒ via_semantic_layer is False（无需改 to_dict）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 改写单测立 RED 断言（诚实兜底 source=fallback / agent 全局短路 / 物理路删除）</name>
  <files>tests/unit/application/conversation/test_send_message_handler.py</files>
  <read_first>
    - tests/unit/application/conversation/test_send_message_handler.py（被改文件本身，尤其 :46 fixture command、:88 test_legacy_llm_success、:410 test_agent_failure_falls_back_to_legacy_llm、:470 test_execute_query_rejects_non_select）
    - app/application/conversation/handlers/send_message_handler.py（:89 _handle_via_semantic_router、:202 _handle_via_agent、:311 _handle_via_legacy_llm、:313 dataset_id is None 块、:454 _execute_query、:279 LEGACY_DISCLAIMER）
    - app/domain/entities/conversation.py:168/192（source / via_semantic_layer 计算）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q5 诚实兜底列约束、Q6 受影响测试全集表）
  </read_first>
  <behavior>
    - test_unanswerable_fallback_via_semantic_layer_false（新增，RED）：全局会话（dataset_id is None）走兜底，断言 ai_message_entity.source == "fallback" 且 ai_message_entity.to_dict()["via_semantic_layer"] is False。对当前代码 RED（当前 source=="legacy_llm"）。
    - test_agent_global_session_short_circuits（新增，RED）：conversation.dataset_id is None 时 handler._handle_via_agent(...) 返回 None，且 mock_db.session.add 未以 AgentQueryLog 调用（无残留 running log）。对当前代码 RED（当前会建 log 后抛错被吞）。
    - test_agent_failure_falls_back_to_honest_fallback（重写 :410 test_agent_failure_falls_back_to_legacy_llm）：agent 失败 → 诚实兜底，断言 source == "fallback"、llm_service.generate_sql.assert_not_called()、legacy_adapter.execute_query.assert_not_called()。对当前代码 RED（当前回退 legacy 物理出数）。
    - test_legacy_llm_success（:88，标记 xfail/删除待 GREEN）：当前断言 source=="legacy_llm" + content.startswith(LEGACY_DISCLAIMER)——物理路删除后此行为消失，本 wave 用 @pytest.mark.xfail(reason="物理 legacy 路在 08.1-02 删除") 标注，GREEN plan 中删除该用例。
    - test_execute_query_rejects_non_select（:470，标记 xfail/删除待 GREEN）：handler._execute_query 方法在 GREEN 删除，本 wave @pytest.mark.xfail(reason="_execute_query 在 08.1-02 删除") 标注。
  </behavior>
  <action>
    在 tests/unit/application/conversation/test_send_message_handler.py 中：
    1. 新增 test_unanswerable_fallback_via_semantic_layer_false：构造 conversation.dataset_id = None 的会话，semantic_router 返回无 execution_results（或不命中），agent 短路，断言最终 ai_message_entity.source == "fallback" 且 ai_message_entity.to_dict()["via_semantic_layer"] is False。
    2. 新增 test_agent_global_session_short_circuits：mock conversation.dataset_id = None，调 handler._handle_via_agent(command, conversation, user_message)，assert 返回 None；用 spy/mock 断言 datachat_channel.to_agent_request 未被调用且无 AgentQueryLog 被 db.session.add。
    3. 重写 :410 test_agent_failure_falls_back_to_legacy_llm → 改名 test_agent_failure_falls_back_to_honest_fallback：保留 agent 失败触发，但断言 source == "fallback"、llm_service.generate_sql.assert_not_called()、（删去对 legacy_adapter.execute_query.assert_called_once 的断言，改 assert_not_called）。
    4. 给 :88 test_legacy_llm_success 与 :470 test_execute_query_rejects_non_select 各加 @pytest.mark.xfail(strict=False, reason="08.1-02 删除物理 legacy 路")。
    5. 不改 production 代码。新增/改写用例对当前代码失败或 xfail。
  </action>
  <verify>
    <automated>pytest tests/unit/application/conversation/test_send_message_handler.py -k "unanswerable_fallback_via_semantic_layer_false or agent_global_session_short_circuits or agent_failure_falls_back_to_honest_fallback" 2>&1 | grep -E "FAILED|failed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_unanswerable_fallback_via_semantic_layer_false" tests/unit/application/conversation/test_send_message_handler.py` >= 1
    - `grep -c "test_agent_global_session_short_circuits" tests/unit/application/conversation/test_send_message_handler.py` >= 1
    - `grep -c "via_semantic_layer\"\] is False" tests/unit/application/conversation/test_send_message_handler.py` >= 1
    - `grep -c "xfail" tests/unit/application/conversation/test_send_message_handler.py` >= 2
    - `pytest tests/unit/application/conversation/test_send_message_handler.py -k "unanswerable_fallback_via_semantic_layer_false" -x` 该新增用例对当前代码 FAILED（RED 已坐实）
    - production 代码零改动：`git diff --name-only app/` 输出不含 send_message_handler.py
  </acceptance_criteria>
  <done>
    新增 RED 用例对当前代码失败（坐实 source 应为 fallback、agent 全局应短路），物理 legacy 用例 xfail 标注待删，production 代码未动。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 集成层立 RED + 定向探查（治理两入口一致 / deny→fallback / compiled_targets 来源契约）</name>
  <files>tests/integration/semantic/test_datachat_official_consume.py</files>
  <read_first>
    - tests/integration/semantic/test_datachat_official_consume.py（被改文件本身，尤其 :233 test_official_consume_routes_to_cube_and_returns_rows、:269 无快照诚实兜底、:296 yaml_only_comment、:313 assert ai["source"] == "legacy_llm"）
    - app/application/semantic_router/preview_service.py:582-642（execute_plan 输出结构，execution_results 来自 runtime_service.execute）
    - app/application/governance/access.py:344-422（_normalize_resource_set 读 target.get("resource_set")）、:509+（post_compile 签名）、:693-714（data_policy_not_matched 硬 deny）
    - app/application/agent/handlers/agent_plan_handler.py:43-83（pre_route/post_compile/_compile_target 范式）
    - .planning/phases/08.1-consume-04/08.1-RESEARCH.md（Q1 治理塌缩、Q2 方案 B、Open Question 1）
  </read_first>
  <behavior>
    - test_execute_plan_results_lack_top_level_resource_set（新增定向探查，坐实当前契约，通过）：用 stub runtime（execute 返回 runtime_service.py:277-289 同形 dict，含 governance_trace 但顶层无 resource_set）调 execute_plan，断言 result["execution_results"][0] 字典 **不含** "resource_set" 顶层键。这坐实 Open Question 1：GREEN 必须重编译而非直喂。该测试对当前代码 **通过**（它是探查/契约锁定，非 RED）。
    - test_two_entrances_policy_decision_parity（新增，RED/xfail）：同主体同问，DataChat 主链产出的 policy_decision.decision 与 /agent/semantic/plan 一致。当前 DataChat 主链零 policy_decision → RED；本 wave @pytest.mark.xfail(reason="DataChat 治理对齐在 08.1-02 落地")。
    - test_governance_deny_falls_back（新增，RED/xfail）：主体无 data 角色 → post_compile deny → ai.source=='fallback' + runtime.execute 未调。当前无治理裁决 → RED；xfail 标注。
    - :313 改写：assert ai["source"] == "legacy_llm" → 改 == "fallback"（D3 未命中走统一兜底）。本 wave 改断言但加 @pytest.mark.xfail(reason="兜底统一在 08.1-02")，GREEN 去掉 xfail。
    - :245/:251（test A source=='semantic' 出数）与 :284（无快照 source=='semantic' blocked）**保留不动**——成功出数与"运行时未就绪"走 semantic，不是 fallback。
  </behavior>
  <action>
    在 tests/integration/semantic/test_datachat_official_consume.py 中：
    1. 新增 test_execute_plan_results_lack_top_level_resource_set：构造 SemanticRouterPreviewService（或复用本文件既有 fixture 装配），stub _runtime_service.execute 返回 {"status":"executed","target_type":"sql","execution_request":{...},"result":{...},"governance_trace":{"resource_set":[...]},...}（顶层无 resource_set）；调 execute_plan(...)；assert "resource_set" not in result["execution_results"][0]。此用例 **通过**（锁定契约）。
    2. 新增 test_two_entrances_policy_decision_parity（@pytest.mark.xfail(strict=False, reason="08.1-02 治理对齐")）：占位断言 DataChat 与 agent_plan_handler 对同一 compiled_targets 经 AccessPolicyDecisionService 得到的 decision 相等。
    3. 新增 test_governance_deny_falls_back（@pytest.mark.xfail(strict=False, reason="08.1-02 治理对齐")）：principal 无 data_m1_reader → 期望 ai["source"]=="fallback" 且 runtime.execute 未被调。
    4. 改 :313 assert ai["source"] == "legacy_llm" → assert ai["source"] == "fallback"，给该用例加 @pytest.mark.xfail(strict=False, reason="08.1-02 统一兜底")。
    5. 不改 :245/:251/:284。不改 production 代码。
  </action>
  <verify>
    <automated>pytest tests/integration/semantic/test_datachat_official_consume.py -k "execute_plan_results_lack_top_level_resource_set" -x 2>&1 | grep -E "passed|PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_execute_plan_results_lack_top_level_resource_set" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c "resource_set\" not in" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c "test_two_entrances_policy_decision_parity" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c "test_governance_deny_falls_back" tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c 'assert ai\["source"\] == "fallback"' tests/integration/semantic/test_datachat_official_consume.py` >= 1
    - `grep -c 'assert ai\["source"\] == "legacy_llm"' tests/integration/semantic/test_datachat_official_consume.py` == 0
    - `pytest tests/integration/semantic/test_datachat_official_consume.py -k "execute_plan_results_lack_top_level_resource_set" -x` exits 0（探查用例通过，契约锁定）
    - production 代码零改动：`git diff --name-only app/` 输出为空
  </acceptance_criteria>
  <done>
    定向探查测试通过并坐实 execution_results 顶层无 resource_set（GREEN 须重编译），治理对齐/deny 兜底/:313 改写以 xfail 标注待 GREEN 转绿，:245/:251/:284 不动，production 未改。
  </done>
</task>

</tasks>

<verification>
- `pytest tests/unit/application/conversation/test_send_message_handler.py tests/integration/semantic/test_datachat_official_consume.py` 运行：探查用例通过、新 RED 用例失败或 xfail、:245/:251/:284 仍绿。
- `git diff --name-only app/` 为空（本 wave 不碰 production）。
- 探查结论书面记录：execution_results[i] 顶层不含 resource_set → Wave 2 GREEN 用 compile_preview 重编译 targets 喂 post_compile（参照 agent_plan_handler._compile_target）。
</verification>

<success_criteria>
- 两个测试文件含本 plan 列出的全部新增/改写用例。
- test_execute_plan_results_lack_top_level_resource_set 通过（Open Question 1 坐实，GREEN 用重编译路径）。
- RED 用例对当前代码失败或 xfail 标注。
- production 代码零改动。
- :313 已无 "legacy_llm" 断言（改 "fallback"，xfail 待 GREEN）。
</success_criteria>

<output>
完成后创建 .planning/phases/08.1-consume-04/08.1-01-SUMMARY.md，须记录：
- compiled_targets 来源探查结论（execution_results 顶层无 resource_set，GREEN 走 compile_preview 重编译）
- 全部新增/改写/xfail 用例清单
- Wave 2 GREEN 的精确转绿目标
</output>
