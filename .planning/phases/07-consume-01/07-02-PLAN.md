---
phase: 07-consume-01
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/application/semantic/semantic_release_service.py
  - tests/unit/application/semantic/test_semantic_release_accumulation.py
  - tests/unit/application/semantic/test_semantic_release_service.py
  - tests/unit/application/semantic/test_semantic_baseline_rebuild.py
autonomous: true
requirements: [CONSUME-01]
must_haves:
  truths:
    - "连续发布 cube A、再发布 cube B 后，active manifest 同时含 A 与 B（按 asset_key 去重、新覆盖旧）"
    - "rollback_to 恢复 target release 的全量目录，active manifest 与 target 的 assets 集合一致，无重复 asset_key"
    - "release 状态机不回归：published→superseded 显式落库、单 namespace 单 active 不变量保持"
    - "compatibility 声明在累积语义下不再把保留资产误报为 removed/breaking（既有 compatibility 测试调整为累积口径并通过）"
    - "存在可重复执行的基线重建 service 方法，把指定 asset 集合（至少答题 cube + comment cube）合并发一个全量 active release"
  artifacts:
    - path: "app/application/semantic/semantic_release_service.py"
      provides: "publish 累积合并 + compatibility 累积口径 + rebuild_active_baseline 方法"
      contains: "rebuild_active_baseline"
    - path: "tests/unit/application/semantic/test_semantic_baseline_rebuild.py"
      provides: "基线重建 service 方法测试"
      min_lines: 40
  key_links:
    - from: "app/application/semantic/semantic_release_service.py::publish"
      to: "self._release_repository.get_active_release_id / list_release_assets / get_revision"
      via: "取 prev active release 全量 assets，与本批 revisions 按 asset_key 合并"
      pattern: "get_active_release_id"
    - from: "app/application/semantic/semantic_release_service.py::rebuild_active_baseline"
      to: "self._release_repository.publish_with_snapshot"
      via: "合并指定 asset 集合为一个全量 published release + active snapshot"
      pattern: "publish_with_snapshot"
---

<objective>
实现发布累积（D1 核心）让 Plan 01 的 RED 测试转 GREEN：`publish` 构建新 release 的 assets 时先取 namespace 当前 active release 的全量 assets，与本批 `revisions` 按 `asset_key` 合并去重（新覆盖旧）；同步修复因累积而失效的 `compatibility` 声明语义；校验 `rollback_to` 在累积下不引入重复 asset_key；提供可重复执行的基线重建 service 方法（D3）。

Purpose: 消除「每次发布整盘替换、活菜单只剩 1 个 cube」的结构缺陷，让 active runtime manifest 成为 namespace 内完整多 cube 目录，为 Phase 8 问数切 official 铺路。
Output: 改后的 `semantic_release_service.py`（publish 累积 + compatibility 累积口径 + `rebuild_active_baseline`）；GREEN 的累积测试；调整后的 compatibility 测试；基线重建测试。
约束：改动最小、不翻新；不写 YAML；不碰消费侧（send_message_handler / grounding / discovery / GET /semantic/cubes）；不破坏 release 状态机；不打挂 comment demo（p34）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-consume-01/07-CONTEXT.md
@.planning/phases/07-consume-01/07-PLAN.md

# 本期主改文件 + 下游契约（不改但必须保证多 asset 仍被解析）
@app/application/semantic/semantic_release_service.py
@app/application/semantic/runtime_manifest_catalog.py
@app/application/agent/services/runtime_semantic_tool_service.py
@tests/unit/application/semantic/test_semantic_release_service.py

<interfaces>
<!-- 事实源契约，executor 直接用 -->

publish 改前构建逻辑（semantic_release_service.py:138-163）—— 仅遍历本批 revisions：
```python
release_assets = []
manifest_assets = []
for revision in revisions:
    asset = self._release_repository.get_asset_by_id(revision.asset_id)   # -> SemanticAsset(id, asset_type, asset_key, ...)
    release_assets.append(SemanticReleaseAsset(release_id=release.id, asset_id=asset.id, revision_id=revision.id, asset_type=asset.asset_type, asset_key=asset.asset_key))
    manifest_assets.append({
        "asset_id": asset.id, "asset_type": asset.asset_type, "asset_key": asset.asset_key,
        "revision_id": revision.id, "spec_checksum": revision.spec_checksum,
        "spec": self._activated_spec(revision.spec_json), "status": "published",
    })
```

repository 真实签名（app/infrastructure/semantic/sql_asset_registry_repository.py）：
```python
def get_active_release_id(self, namespace) -> Optional[str]                  # 当前 active(=status published) release id，首次发布时为 None
def list_release_assets(self, release_id) -> list[SemanticReleaseAsset]      # SemanticReleaseAsset: asset_id / revision_id / asset_type / asset_key
def get_revision(self, revision_id) -> Optional[SemanticAssetRevision]       # SemanticAssetRevision: spec_json / spec_checksum
def get_asset_by_id(self, asset_id) -> Optional[SemanticAsset]
def get_active_snapshot(self, namespace="default") -> Optional[RuntimeSnapshot]
def publish_with_snapshot(self, release, release_assets, snapshot, *, audit_writer=None) -> SemanticRelease
```

SemanticReleaseAsset 实体（导入自 app.domain.semantic.asset_registry，semantic_release_service.py 顶部已 import）：字段 release_id / asset_id / revision_id / asset_type / asset_key。

compatibility 现状（semantic_release_service.py:342-388）：
`_build_compatibility_declaration` 把「上一 active manifest 中不在本批的资产」判为 removed → breaking。
**累积后这是错的**：保留资产不再 removed。现有测试 test_publish_declares_compatibility_against_previous_manifest（:279-335）第三段断言「发布 cube_b → cube_a removed → breaking」会失败，必须按累积口径调整。

rollback_to 现状（semantic_release_service.py:196-280）：遍历 `list_release_assets(target_release_id)` 复制全量 assets。累积后 target release 本就是全量、其内不应有重复 asset_key（list_release_assets 按 (asset_type, asset_key) 排序、每 release 每 asset 一行），rollback 语义自洽。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: publish 按 asset_key 累积合并（GREEN 核心）</name>
  <files>app/application/semantic/semantic_release_service.py, tests/unit/application/semantic/test_semantic_release_accumulation.py</files>
  <read_first>
    - app/application/semantic/semantic_release_service.py:102-194（publish 全文，改点在 138-163 的 manifest/release_assets 构建处）
    - app/infrastructure/semantic/sql_asset_registry_repository.py:219-229（get_active_release_id）、:280-287（list_release_assets）、:151-153（get_revision）、:59-61（get_asset_by_id）
    - tests/unit/application/semantic/test_semantic_release_accumulation.py（Plan 01 的 RED 测试，本任务让其转 GREEN）
  </read_first>
  <action>
    在 `publish`（约 :138 处）替换「只遍历本批 revisions」的构建逻辑为「prev active 全量 ∪ 本批 revisions，按 (asset_type, asset_key) 合并去重、本批覆盖旧」。具体步骤：

    1. 在构建 `release_assets`/`manifest_assets` 之前，先取 prev active 全量：
    ```python
    merged_assets: dict[tuple[str, str], dict[str, Any]] = {}   # key=(asset_type, asset_key) -> {"asset_id","asset_type","asset_key","revision_id","spec_checksum","spec"}
    prev_id = self._release_repository.get_active_release_id(namespace)
    if prev_id:
        for prev_asset in self._release_repository.list_release_assets(prev_id):
            prev_revision = self._release_repository.get_revision(prev_asset.revision_id)
            if prev_revision is None:
                continue   # 防御：跳过孤儿 revision，不阻断发布
            merged_assets[(prev_asset.asset_type, prev_asset.asset_key)] = {
                "asset_id": prev_asset.asset_id,
                "asset_type": prev_asset.asset_type,
                "asset_key": prev_asset.asset_key,
                "revision_id": prev_asset.revision_id,
                "spec_checksum": prev_revision.spec_checksum,
                "spec_json": prev_revision.spec_json,
            }
    ```

    2. 再用本批 revisions 覆盖同 key 项（保留既有 get_asset_by_id 缺失校验）：
    ```python
    for revision in revisions:
        asset = self._release_repository.get_asset_by_id(revision.asset_id)
        if asset is None:
            raise ValueError(f"semantic asset not found: {revision.asset_id}")
        merged_assets[(asset.asset_type, asset.asset_key)] = {
            "asset_id": asset.id,
            "asset_type": asset.asset_type,
            "asset_key": asset.asset_key,
            "revision_id": revision.id,
            "spec_checksum": revision.spec_checksum,
            "spec_json": revision.spec_json,
        }
    ```

    3. 从合并结果生成 `release_assets` 与 `manifest_assets`（manifest asset 形状与改前完全一致：spec 经 `self._activated_spec(spec_json)`、status="published"）：
    ```python
    release_assets = []
    manifest_assets = []
    for entry in merged_assets.values():
        release_assets.append(SemanticReleaseAsset(
            release_id=release.id, asset_id=entry["asset_id"], revision_id=entry["revision_id"],
            asset_type=entry["asset_type"], asset_key=entry["asset_key"],
        ))
        manifest_assets.append({
            "asset_id": entry["asset_id"], "asset_type": entry["asset_type"], "asset_key": entry["asset_key"],
            "revision_id": entry["revision_id"], "spec_checksum": entry["spec_checksum"],
            "spec": self._activated_spec(entry["spec_json"]), "status": "published",
        })
    ```

    `prev_id` 为空（首次发布）时 merged_assets 仅含本批 → 行为与改前一致（D1 退化要求）。
    不改 snapshot 构建、不改 audit writer、不改 binding gate、不改状态机调用。不碰 rollback_to（Task 3 单独校验）。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/semantic/test_semantic_release_accumulation.py</automated>
  </verify>
  <acceptance_criteria>
    - 累积测试全绿：上述 pytest 输出含 `3 passed`、不含 `failed`。
    - 改点引用 prev active：`grep -q "get_active_release_id" app/application/semantic/semantic_release_service.py` 退出码 0（原 publish 已有该调用于 previous_release_id；新增需出现在 manifest 构建段——executor 在 SUMMARY 贴出合并段代码确认）。
    - 合并段按 asset_key 去重：`grep -q "merged_assets" app/application/semantic/semantic_release_service.py` 退出码 0。
    - 既有发布测试不回归（除 compatibility 一项留 Task 2）：`PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/semantic/test_semantic_release_service.py -k "not compatibility"` 输出含 `passed`、不含 `failed`。
  </acceptance_criteria>
  <done>
    publish 改为「prev active 全量 ∪ 本批，按 (asset_type,asset_key) 合并去重」；Plan 01 三个累积测试 PASSED；test_semantic_release_service.py 除 compatibility 外全绿；首次发布退化行为不变。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compatibility 声明对齐累积语义 + rollback 累积护栏</name>
  <files>app/application/semantic/semantic_release_service.py, tests/unit/application/semantic/test_semantic_release_service.py</files>
  <read_first>
    - app/application/semantic/semantic_release_service.py:342-388（_build_compatibility_declaration，含 removed/breaking 逻辑）
    - app/application/semantic/semantic_release_service.py:196-280（rollback_to 全文）
    - tests/unit/application/semantic/test_semantic_release_service.py:279-335（compatibility 测试，需按累积口径调整）、:338-383（rollback 测试，作为不回归锚点）
  </read_first>
  <action>
    A) 修 `_build_compatibility_declaration`（:342-388）使其语义与累积一致：
    累积后「上一 active manifest 中不在本批 revisions 的资产」不再退出运行时（会被保留累积），因此不应判为 removed/breaking。把 `removed` 的计算口径从「previous.keys() - current.keys()」改为恒空集（累积模型下发布不移除资产），或显式置 `removed = []`，并相应让 `level` 在无 removed 时恒为 "compatible"。`added`（current - previous）与 `changed`（同 key checksum 变化）保留。在方法 docstring 顶部更新说明：「累积发布模型下，本批未覆盖的既有资产保留在 active manifest，发布不移除资产，故 removed_assets 恒空、level 不再因 removed 判 breaking」。

    B) 调整 compatibility 测试 test_publish_declares_compatibility_against_previous_manifest（:279-335）第三段断言以匹配累积口径：
    - 原断言 `level == "breaking"` / `removed_assets == ["cube:cube_a"]` 改为：发布 cube_b 后 `level == "compatible"`、`removed_assets == []`、`added_assets == ["cube:cube_b"]`（cube_a 因累积保留、不再 removed）。
    - 前两段（added cube_a / changed cube_a）断言保持不变。

    C) rollback 累积护栏：在 test_semantic_release_service.py 现有 rollback 测试（:338-383）之后，新增 `test_rollback_to_accumulated_release_has_no_duplicate_keys`：
    - 连续发布 cube_a、再发布 cube_b（触发累积，active release 含 a+b 两条），再发布 cube_a 的 v2（active 仍 a+b、a 为 v2），
    - rollback 到「含 a+b 的那个 release」，断言 rollback 后 active manifest 的 asset_key 集合 == {"cube_a","cube_b"} 且 `len(assets) == len({(x["asset_type"],x["asset_key"]) for x in assets})`（无重复 asset_key）。
    rollback_to 生产代码**不改**（已是复制 target 全量、自洽）；本任务只加测试坐实不回归。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/semantic/test_semantic_release_service.py</automated>
  </verify>
  <acceptance_criteria>
    - 全文件绿：上述 pytest 输出含 `passed`、不含 `failed`、不含 `error`。
    - removed 恒空逻辑落地：`grep -q "removed_assets" app/application/semantic/semantic_release_service.py` 仍在；compatibility 测试第三段不再断言 `breaking`（`grep -c '"breaking"' tests/unit/application/semantic/test_semantic_release_service.py` 输出 `0`）。
    - rollback 护栏测试存在：`grep -q "test_rollback_to_accumulated_release_has_no_duplicate_keys" tests/unit/application/semantic/test_semantic_release_service.py` 退出码 0。
    - rollback_to 生产代码未改 manifest 构建外的逻辑：`git diff app/application/semantic/semantic_release_service.py` 中 rollback_to 函数体（:196-280 段）无改动（executor 在 SUMMARY 说明 rollback_to 零改动）。
    - 状态机不回归：test_publish_supersedes_previous_release_explicitly 与 test_release_deprecate_and_revoke_state_machine 仍 PASSED（包含在全文件绿内）。
  </acceptance_criteria>
  <done>
    compatibility 声明在累积下 removed 恒空、不再误判 breaking；compatibility 测试改为累积口径并 PASSED；rollback 累积护栏测试坐实 rollback 后无重复 asset_key；release 状态机相关测试全绿；rollback_to 生产逻辑零改动。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 基线重建 service 方法（D3，可重复执行）</name>
  <files>app/application/semantic/semantic_release_service.py, tests/unit/application/semantic/test_semantic_baseline_rebuild.py</files>
  <read_first>
    - app/application/semantic/semantic_release_service.py:102-194（publish，rebuild 复用其合并/snapshot 构建范式）
    - app/infrastructure/semantic/sql_asset_registry_repository.py:280-378（list_release_assets / publish_with_snapshot 事务边界）
    - tests/unit/application/semantic/test_semantic_release_service.py:13-44（基础发布测试装配，rebuild 测试复用）
  </read_first>
  <action>
    在 `SemanticReleaseService` 新增方法 `rebuild_active_baseline`，把「当前应在线的 cube 集合」合并发一个全量 active release 作为起点。最小可行口径（D3）：调用方传入要纳入基线的 `(asset_type, asset_key)` 列表（或 asset_id 列表）；方法取每个 asset 的「最新已发布 revision」（无则取 current_revision），按 asset_key 去重合并为一个 published release + active snapshot。

    签名与实现草图：
    ```python
    def rebuild_active_baseline(
        self,
        *,
        namespace: str,
        asset_keys: list[tuple[str, str]],   # [(asset_type, asset_key), ...]
        actor: str | None,
        idempotency_key: str | None = None,
        audit_writer: Callable[[dict[str, Any]], None] | None = None,
    ) -> SemanticRelease:
        # 1. 解析每个 (asset_type, asset_key) 的目标 revision：
        #    asset = repo.get_asset(namespace, asset_type, asset_key)  # 用 repo.get_asset(ns, type, key)
        #    revision = repo.get_revision(asset.current_revision_id)（或 list_revisions 取最新 published）
        # 2. 复用 publish 的合并构建：merged dict by (asset_type, asset_key)，与 Task 1 同形
        # 3. 构建 SemanticRelease(status="published", scope_json={"baseline_rebuild": asset_keys}) +
        #    release_assets + RuntimeSnapshot(status="active") +
        #    return self._release_repository.publish_with_snapshot(release, release_assets, snapshot, audit_writer=...)
    ```
    - 复用 `self._activated_spec` 落 active；manifest asset 形状与 publish 一致。
    - 缺失 asset/revision 时抛 `ValueError(f"baseline_asset_not_found: {asset_type}:{asset_key}")`，不静默吞。
    - 可重复执行：每次调用产生新 published release（沿用 publish_with_snapshot 的 supersede 旧 active 行为），不写迁移、不写 YAML。
    - 抽取 publish 与 rebuild 共用的合并构建为一个私有 helper（如 `_build_assets_from_merged(merged, release_id)`）以避免重复，守住「改动最小」。

    测试 `tests/unit/application/semantic/test_semantic_baseline_rebuild.py`：
    - `test_rebuild_active_baseline_merges_answer_and_comment`：
      预置答题 cube（asset_key="dws_study_student_answer_kb_stat_di"）与 comment cube（asset_key="student_comment"）各自发布一次（各为独立 active），
      调 `rebuild_active_baseline(namespace=ns, asset_keys=[("cube","dws_study_student_answer_kb_stat_di"),("cube","student_comment")], actor="ops", idempotency_key="baseline_1")`，
      断言新 active manifest 同时含两个 asset_key、status 为 published、release.status == "published"、`repo.get_active_release_id(ns)` == 新 release.id。
    - `test_rebuild_active_baseline_is_repeatable`：连续调两次（不同 idempotency_key），第二次仍产出含两 asset 的 active release，且 `repo.get_active_release_id` 指向第二次 release（旧的 superseded）。
    - `test_rebuild_active_baseline_missing_asset_raises`：传一个不存在的 asset_key，断言 `pytest.raises(ValueError, match="baseline_asset_not_found")`。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/semantic/test_semantic_baseline_rebuild.py</automated>
  </verify>
  <acceptance_criteria>
    - 方法存在：`grep -q "def rebuild_active_baseline" app/application/semantic/semantic_release_service.py` 退出码 0。
    - 经 publish_with_snapshot 落库：`grep -q "publish_with_snapshot" app/application/semantic/semantic_release_service.py`（已有；rebuild 复用同一落点，executor 在 SUMMARY 确认 rebuild return 走 publish_with_snapshot）。
    - 基线测试全绿：上述 pytest 输出含 `3 passed`、不含 `failed`。
    - 缺失资产 fail-loud：测试含 `baseline_asset_not_found` 断言（`grep -q "baseline_asset_not_found" tests/unit/application/semantic/test_semantic_baseline_rebuild.py` 退出码 0）。
    - 不写 YAML：`git diff --name-only | grep -E "\.ya?ml$" | wc -l` 输出 `0`。
  </acceptance_criteria>
  <done>
    `rebuild_active_baseline` 可重复执行，把指定 asset 集合（答题 cube + comment cube）合并发一个全量 active release；缺失资产 fail-loud；三个基线测试 PASSED；未写 YAML / 未加迁移。
  </done>
</task>

</tasks>

<verification>
全期回归（GREEN 总验收）：
- 累积：`PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/semantic/test_semantic_release_accumulation.py` → 3 passed。
- 发布服务全文件：`... tests/unit/application/semantic/test_semantic_release_service.py` → 全绿（含 supersede / deprecate / revoke / rollback / compatibility 累积口径）。
- 基线重建：`... tests/unit/application/semantic/test_semantic_baseline_rebuild.py` → 3 passed。
- 下游契约不回归：`... tests/unit/application/semantic/test_runtime_manifest_catalog.py tests/unit/application/agent/test_runtime_semantic_tool_service.py tests/unit/application/semantic/test_runtime_snapshot_service.py` → 全绿（多 asset manifest 仍被正确解析、list_cubes 能列出多 cube）。
- 不碰消费侧 / 不写 YAML：`git diff --name-only` 仅含 semantic_release_service.py + 三个测试文件；无 *.yml/*.yaml、无 send_message_handler / grounding / discovery 改动。
</verification>

<success_criteria>
- active runtime manifest 累积 namespace 内所有已发布资产，成为完整多 cube 目录（A+B 同在、按 asset_key 去重新覆盖旧）。
- rollback_to 恢复全量目录且无重复 asset_key；release 状态机（published→superseded、单 active 不变量）不回归。
- compatibility 声明在累积语义下不再误报 breaking。
- 提供可重复执行的基线重建 service 方法，把答题 cube + comment cube 合并为一个全量 active release 起点。
- 全程未写 YAML、未改消费侧、未加迁移；下游 RuntimeSemanticCatalog / RuntimeSemanticToolService 解析多 asset 不回归。
</success_criteria>

<output>
After completion, create `.planning/phases/07-consume-01/07-02-SUMMARY.md`（粘贴各文件 pytest 计数行 + publish 合并段代码 + rebuild return 走 publish_with_snapshot 的确认）。
</output>
