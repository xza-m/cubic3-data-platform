---
phase: 10-default-partition
plan: "02"
type: tdd
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/domain/semantic/compiler.py
  - tests/unit/domain/semantic/test_compiler.py
autonomous: true
requirements: [CONSUME-06]
must_haves:
  truths:
    - "分区 date 型 cube 无显式过滤 → 编译注入最近 7 天默认 ds 范围（绕开 ODPS-0130071）"
    - "带显式 filters/time_dimensions 命中分区字段 → 不注入默认（不 override 用户）"
    - "非 date 型分区 / source_sql 派生 cube → 不注入"
    - "latest_expr 非空 cube → 仍走 MAX_PT（既有 8 dim cube 契约不变）"
    - "未知 partition.format → raise CompilationError（确定性兜底，不静默产错字面量）"
    - "整套 test_compiler.py 全绿，无既有 exact/negative 断言因多出 WHERE 段失败"
    - "scoped_table_refs 不受默认注入影响（注入只改 where_parts/join_on_parts）"
  artifacts:
    - path: app/domain/semantic/compiler.py
      provides: "块7 扩为 latest_expr 优先 > 默认 7 天窗口 > 不注入；模块常量 + _fmt_strftime + _has_explicit_partition_filter + __init__ today"
      contains: "DEFAULT_PARTITION_WINDOW_DAYS"
      min_lines: 600
  key_links:
    - from: "compiler.py 块7"
      to: "self._dialect.partition_condition"
      via: "默认窗口范围谓词渲染（非 MaxCompute 绝不注 MAX_PT）"
      pattern: "partition_condition\\("
    - from: "compiler.py 块7"
      to: "_has_explicit_partition_filter"
      via: "DSL 结构守护，防 override 用户显式过滤"
      pattern: "_has_explicit_partition_filter"
    - from: "QueryCompiler.__init__"
      to: "date.today()"
      via: "self._today or date.today()（可测时钟，生产位置参零改动）"
      pattern: "self\\._today"
---

<objective>
Wave 2（GREEN）：在 `app/domain/semantic/compiler.py` 块7（:204-218）实现默认分区注入，让 10-01 的 RED 全绿。

核心改动（D1–D5，单文件 + 测试）：
- 块7 扩为优先级链：**latest_expr 优先 > 默认日期窗口（最近 7 天）> 不注入**。
- 只对 `partition.type == "date"` 且 `cube.source_sql` 为空、且 `_has_explicit_partition_filter` 为 False 的 cube 注入。
- 默认窗口走既有 `self._dialect.partition_condition(...)`（四方言均渲染范围谓词，非 MaxCompute 绝不注 MAX_PT）。
- 可测时钟：`__init__` 加 keyword-only `today: date | None = None`；块7 取 `today = self._today or date.today()`。
- 未知 format → `raise CompilationError`（D5，确定性兜底）。

Purpose: 绕开 MaxCompute 全表扫描保护 ODPS-0130071，让 DataChat 对「无时间口径」问数（如"学生答题统计 总数"）真出数（默认返最近 7 天，已拍板取舍）。
Output: compiler.py 块7 + 模块常量/辅助方法落地，整套 test_compiler.py 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-default-partition/10-CONTEXT.md
@.planning/phases/10-default-partition/10-01-SUMMARY.md

# 主改文件（执行者必读，勿凭记忆改）
@app/domain/semantic/compiler.py
@app/domain/semantic/dialects.py
@app/domain/semantic/entities.py
@app/infrastructure/semantic/cubes/answer_records.yml

<interfaces>
<!-- 实现契约：执行者直接按此改，不要扩签名/改方言/写 YAML -->

当前块7（compiler.py:204-218）逐字保留 latest_expr 分支，扩展默认分支：
```python
# 7. WHERE — partition latest_expr  → 改为 latest_expr 优先 > 默认窗口 > 不注入
for cube in cubes.values():
    part = cube.partition
    if not part:
        continue
    if self._has_explicit_partition_filter(dsl, cube):
        continue                              # 守护：用户已显式过滤 → 不动
    if part.latest_expr:                      # 既有契约：8 dim cube 走 MAX_PT
        condition = f"{cube.name}.{part.field} = {part.latest_expr}"
    elif part.type == "date" and not (cube.source_sql or "").strip():
        today = self._today or date.today()   # 可测时钟
        win = min(DEFAULT_PARTITION_WINDOW_DAYS, max(part.max_range_days - 1, 1))
        start = today - timedelta(days=win - 1)
        fmt = self._fmt_strftime(part.format)  # 未知 format → CompilationError
        start_ds = start.strftime(fmt)
        end_ds = today.strftime(fmt)
        condition = self._dialect.partition_condition(
            f"{cube.name}.{part.field}", start_ds, end_ds, part.format
        )
    else:
        continue                              # 非 date / source_sql 派生 → 不注入
    if cube.name == primary.name:
        where_parts.append(condition)
    elif cube.name in join_on_parts:
        join_on_parts[cube.name].append(condition)
```
注意：旧块7 的 `has_time_range`（:207-210 只比 cube.name 不比 field）被 `_has_explicit_partition_filter`（比 field）取代 —— 收紧 D3 缺口。

模块级（compiler.py 顶部）：
```python
from datetime import datetime, date, timedelta   # 现仅 import datetime，补 date, timedelta
DEFAULT_PARTITION_WINDOW_DAYS = 7
_FMT_STRFTIME = {"yyyyMMdd": "%Y%m%d", "yyyy-MM-dd": "%Y-%m-%d"}
```

新增方法（QueryCompiler 内）：
```python
def __init__(self, join_graph, dialect=None, *, today: date | None = None):
    self._graph = join_graph
    self._dialect = dialect or MaxComputeDialect()
    self._today = today

@staticmethod
def _fmt_strftime(fmt: str) -> str:
    mapped = _FMT_STRFTIME.get(fmt)
    if mapped is None:
        raise CompilationError(f"Unsupported partition format for default window: '{fmt}'")
    return mapped

def _has_explicit_partition_filter(self, dsl: QueryDSL, cube: CubeDefinition) -> bool:
    part = cube.partition
    if part is None:
        return False
    for td in dsl.time_dimensions:
        if td.date_range and self._parse_ref(td.dimension) == (cube.name, part.field):
            return True
    for f in dsl.filters:
        if self._parse_ref(f.target) == (cube.name, part.field):
            return True
    return False
```

partition_condition 渲染（dialects.py:52，四方言同形，复用不改）：
```python
def partition_condition(self, field, start, end, fmt) -> str:
    return f"{field} >= '{start}' AND {field} <= '{end}'"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现块7 默认分区注入 + 模块常量 + _fmt_strftime + _has_explicit_partition_filter + __init__ today（转绿）</name>
  <files>app/domain/semantic/compiler.py, tests/unit/domain/semantic/test_compiler.py</files>
  <read_first>
    - app/domain/semantic/compiler.py（顶部 import :1-15；__init__ :86-94；time_dimension date_range 守护参照 :150-162；filters :188-202；块7 :204-218；_parse_ref :345-350；_validate_time_range :507-529）
    - app/domain/semantic/dialects.py（partition_condition :52/83/108/133 渲染范围谓词，复用不改）
    - app/infrastructure/semantic/cubes/answer_records.yml（:19-24 主验证 fixture）
    - tests/unit/domain/semantic/test_compiler.py（10-01 新增的 TestCompilerDefaultPartitionInjection + 翻转的 test_latest_partition_fallback —— 本任务的目标断言）
  </read_first>
  <behavior>
    （断言由 10-01 RED 提供；本任务实现使其全绿）
    - answer_records 无过滤 + today=20260626 → SQL 含 "answer_records.answer_date >= '20260620' AND answer_records.answer_date <= '20260626'"。
    - 显式 filters/time_dimensions 命中 answer_date → 不注入默认（无 '20260620'）。
    - string 型分区 / source_sql cube → 不注入。
    - latest_expr 非空 → "= MAX_PT(...)"，不出默认窗口。
    - 未知 format（如 "weird-fmt"）→ CompilationError。
    - scoped_table_refs 不变（注入只改 where_parts/join_on_parts）。
  </behavior>
  <action>
    严格按 <interfaces> 块改 app/domain/semantic/compiler.py，仅以下五处：
    1. 顶部 import：`from datetime import datetime` → `from datetime import datetime, date, timedelta`。
    2. 模块级（class 之上）加常量 `DEFAULT_PARTITION_WINDOW_DAYS = 7` 与 `_FMT_STRFTIME = {"yyyyMMdd": "%Y%m%d", "yyyy-MM-dd": "%Y-%m-%d"}`。
    3. `__init__` 加 keyword-only `*, today: date | None = None` 并存 `self._today = today`（生产调用点全是位置参 → 零破坏，已核对 semantic_query_service.py:157/166/168、execution_compiler/preview_service.py:264）。
    4. 块7（:204-218）整段替换为 <interfaces> 的优先级链（latest_expr 优先 > 默认 date 窗口 > continue）。
       窗口算法（D2）写死语义：`win = min(DEFAULT_PARTITION_WINDOW_DAYS, max(part.max_range_days - 1, 1))`；
       `start = today - timedelta(days=win - 1)`；`end = today`；用 `_fmt_strftime(part.format)` 渲染 ds。
    5. 新增 `_fmt_strftime`（未知 format → CompilationError）与 `_has_explicit_partition_filter`（按 DSL 结构判定，比对 (cube.name, part.field)，覆盖 time_dimensions.date_range 与 filters.target）。

    禁止：改 dialects.py 签名 / 改 entities.py / 写或改任何 YAML / 动生产调用点 / 改 :133-162 与 :188-202 既有注入逻辑。
    注意 join cube 默认窗口进 ON（沿用 :217-218 `join_on_parts[cube.name].append`），LEFT JOIN 正确。

    提交：`git add app/domain/semantic/compiler.py && git commit -m "feat(10-02): 编译器默认分区注入（最近7天窗口/守护显式过滤/方言安全/可测时钟/未知format兜底）"`
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/domain/semantic/test_compiler.py::TestCompilerDefaultPartitionInjection "tests/unit/domain/semantic/test_compiler.py::TestCompilerDerivedMeasures::test_latest_partition_fallback" 2>&1 | tail -20</automated>
  </verify>
  <done>
    TestCompilerDefaultPartitionInjection 全部 + 翻转的 test_latest_partition_fallback 全绿。grep compiler.py 确认 `DEFAULT_PARTITION_WINDOW_DAYS = 7`、`_fmt_strftime`、`_has_explicit_partition_filter`、`self._today`、`from datetime import datetime, date, timedelta` 均存在。dialects.py/entities.py/YAML 无 diff。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 跑满整套 test_compiler.py + 逐条确认无既有 exact/negative 断言因多出 WHERE 段失败（审查补正①）</name>
  <files>tests/unit/domain/semantic/test_compiler.py, app/domain/semantic/compiler.py</files>
  <read_first>
    - tests/unit/domain/semantic/test_compiler.py（重点核对会进默认注入路径的既有用例：test_single_cube_simple_count :211 / test_filter_* :283-320 / test_segment :322 / test_order_and_limit :331 / test_default_filter_injected :344 / test_cross_cube_join :363 / test_scoped_table_refs_* :375-390 / test_derived_measure_expansion :499）
    - app/domain/semantic/compiler.py（已改块7）
    - .planning/phases/10-default-partition/10-CONTEXT.md（审查补正①：~10+ 现有测试靠子串 in 幸存，须逐条确认）
  </read_first>
  <behavior>
    所有用 ANSWER fixture（分区 date 型、无显式时间过滤）的既有用例编译输出现在会多出默认窗口 WHERE 段。须确认：
    - 子串 `in` 断言（如 "COUNT(answer_records.answer_record_id)" in sql）不受影响 → 仍绿。
    - exact-equal 断言：仅 `test_scoped_table_refs_single_cube_only_from_anchor`(:385-390) 对 scoped_table_refs 精确相等 —— 默认注入只改 where_parts，不改 scoped_table_refs → 仍绿。
    - negative 断言（如 test_default_filter_injected:356 "answer_records.answer_result = 1" not in sql；test_latest_partition_fallback "MAX_PT" not in sql）→ 默认窗口注入的是 answer_date 范围谓词，不引入 answer_result/MAX_PT → 仍绿。
    - test_time_dimension_with_range / test_time_granularity_* / test_time_range_exceeds_partition_limit：均有显式 date_range → _has_explicit_partition_filter 命中 → 不注默认、不与既有 :150-158 重复 → 仍绿。
  </behavior>
  <action>
    1. 运行整套 test_compiler.py（命令见 verify）。
    2. 若有失败：逐条判定属于哪类——
       (a) 既有 exact/negative 断言因多出 WHERE 段误失败 → 这是「测试断言需随新行为校准」，按 <behavior> 期望最小化更新该断言（例如把过严的 exact-equal 放宽为对非分区部分的 in 断言，或显式断言新增的默认窗口段）；不得为了过测试而改 compiler 行为偏离 D1–D5。
       (b) 实现 bug（注入了不该注入的 cube / 字面量算错 / scoped_table_refs 被动）→ 回 Task 1 修 compiler。
    3. 重点逐条复核清单（在 SUMMARY 登记每条结论：未触发注入 / 触发但断言仍绿 / 断言已校准）：
       test_single_cube_simple_count、test_dimension_and_measure、test_filter_equals/in/contains/set_notset、
       test_segment、test_order_and_limit、test_default_filter_injected、test_cross_cube_join、
       test_scoped_table_refs_emit_from_and_join_anchors、test_scoped_table_refs_single_cube_only_from_anchor、
       test_derived_measure_expansion、test_helper_paths_*。
    4. 全绿后提交：`git add -A && git commit -m "test(10-02): 跑满 test_compiler.py 全套 + 既有 exact/negative 断言随默认注入校准（全绿）"`（若无断言需改则此步可并入说明）。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/domain/semantic/test_compiler.py 2>&1 | tail -15</automated>
  </verify>
  <done>
    整套 test_compiler.py 全绿（既有 46 + 10-01 新增/翻转）；SUMMARY 登记逐条复核结论（哪些未触发注入、哪些触发但仍绿、哪些断言被校准及理由）；任何断言校准均符合 D1–D5，未为过测试偏离决策。
  </done>
</task>

</tasks>

<verification>
- `TestCompilerDefaultPartitionInjection` 全绿 + `test_latest_partition_fallback` 翻转绿。
- 整套 `test_compiler.py` 全绿（无既有断言回归）。
- `git diff --name-only` 仅 `app/domain/semantic/compiler.py` + `tests/unit/domain/semantic/test_compiler.py`；dialects.py / entities.py / 任何 YAML / 生产调用点零 diff。
- grep 锚点：compiler.py 含 `DEFAULT_PARTITION_WINDOW_DAYS = 7` / `def _fmt_strftime` / `def _has_explicit_partition_filter` / `self._today or date.today()` / `partition_condition(`。
</verification>

<success_criteria>
- 分区 date 型 cube 无过滤注入最近 7 天 ds 范围；带显式过滤不注入；非 date·source_sql cube 不注入；latest_expr cube 仍走 MAX_PT；未知 format → CompilationError；整套 test_compiler.py 绿；scoped_table_refs 不受影响。
- 单文件改动边界守住（仅 compiler.py + test_compiler.py）。
- 容器 TZ 风险（date.today() 依赖系统 TZ，UTC 容器"今天"可能比 MaxCompute +08 业务日历早一天）登记 notes，本期内网单机接受（确认容器 TZ=+08 或留 risk）。
</success_criteria>

<notes>
**部署/运维验证（非代码任务，执行者运维侧做，不计入本 plan 验收门）：**
- 真实出数：DataChat 问"学生答题统计 总数"应绕过 ODPS-0130071，返回最近 7 天结果（非 500、非全表扫描报错）。记入 must_haves 的真实出数验证。
- docker 复跑：单机 Docker 全栈 `make smoke` 或核心链路冒烟不回归。
- 容器 TZ：确认 docker 容器 TZ=Asia/Shanghai(+08)；若为 UTC，默认窗口"今天"可能比 MaxCompute 业务日历早一天 —— 内网单机本期接受，登记 risk（Deferred：统一容器 TZ / 时钟时区注入）。

**Deferred（本期不做，见 10-CONTEXT.md）：**
- PartitionDef.default_window_days 让建模者按 cube 覆盖窗口（本期用模块常量 7）。
- 时钟时区注入 / 统一容器 TZ。
- string 型分区安全默认策略（本期保持不注入）。
- join cube RIGHT/FULL JOIN 默认窗口进 ON 的方向性复核（本期 LEFT 正确，沿用 :217-218）。
</notes>

<output>
完成后创建 `.planning/phases/10-default-partition/10-02-SUMMARY.md`（记录 compiler.py 五处改动、整套绿、逐条既有用例复核结论、容器 TZ risk 登记、真实出数/docker 运维待办）。
</output>
