---
phase: 08-consume-02
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/application/conversation/handlers/send_message_handler.py
  - app/interfaces/api/v1/semantic/cubes.py
  - tests/integration/semantic/test_datachat_official_consume.py
autonomous: true
requirements: [CONSUME-02]
user_setup: []
must_haves:
  truths:
    - "DataChat 全局问数经 SendMessageHandler 调 execute_plan 时传 runtime_mode='official'，08-01 的 RED 断言转 GREEN"
    - "official 下问对得上口径的问法（学生答题统计 总数）走 semantic 主链：route_type∈{cube,hybrid}、有 execution_results、不落 legacy「未能找到口径」兜底"
    - "无 active 快照时 official 路径诚实回「语义运行时尚未就绪」，不 500、不伪造、不落「未能找到口径」"
    - "GET /semantic/cubes discovery 与 grounding 同源：cube 列表来自 active manifest（runtime_snapshot_service / RuntimeSemanticCatalog），与 official 命中范围一致"
    - "comment / YAML-only cube 切 official 后不再被 DataChat 命中是预期方向（D3），不为保 comment 做 YAML 并集"
  artifacts:
    - path: app/application/conversation/handlers/send_message_handler.py
      provides: "execute_plan(runtime_mode='official') 调用"
      contains: 'runtime_mode="official"'
    - path: app/interfaces/api/v1/semantic/cubes.py
      provides: "list_cubes 从 active manifest 出（D2 discovery 同源）"
      contains: "runtime_snapshot_service"
    - path: tests/integration/semantic/test_datachat_official_consume.py
      provides: "official 出数 + 无快照诚实兜底的集成断言（stub runtime，不实连 MaxCompute）"
      contains: "runtime_mode"
  key_links:
    - from: app/application/conversation/handlers/send_message_handler.py
      to: SemanticRouterPreviewService.execute_plan
      via: 'runtime_mode="official" kwarg'
      pattern: 'execute_plan\(.*runtime_mode="official"'
    - from: app/interfaces/api/v1/semantic/cubes.py
      to: runtime_snapshot_service.get_active_manifest
      via: "RuntimeSemanticCatalog.from_manifest(...).list_entities('cube')"
      pattern: "get_active_manifest|RuntimeSemanticCatalog"
---

<objective>
Wave 2（GREEN）：把 DataChat 全局问数从读 YAML（preview）切到读 active manifest（official），打通"自助建模发布的 cube → 问数能消费出数"，并让 discovery 与 grounding 同源。

Purpose: 落地 D1（核心，一行：handler 传 `runtime_mode="official"`）+ D2（discovery 同源）+ 确认 D1 兜底语义正确（无快照→诚实未就绪；未命中→legacy 诚实兜底），并用集成测试坐实真实闭环形状（stub runtime 返固定行数，不实连 MaxCompute）。

机制无需新建：preview_service 的 official 分支（`_load_runtime_manifest` → `RuntimeSemanticCatalog.from_manifest` → `_runtime_entities` 走 manifest）已实现且有测试。本期只让 send_message_handler 传 official + discovery 改读 manifest。

Output: handler 转绿 08-01 RED；cubes discovery 同源；新增集成测试（official 出数 + 无快照诚实兜底）。

**不改 grounding 匹配算法、不激活 LLM 意图抽取、不动编译器分区保护**（均 deferred 到 Phase 9 / 另期）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-consume-02/08-CONTEXT.md
@.planning/phases/08-consume-02/08-01-SUMMARY.md

<interfaces>
<!-- 事实源：handler 调用点、preview_service official 分支、blocked 兜底形状、cubes discovery、runtime catalog。直接复用，无需翻库。 -->

【D1 改点】app/application/conversation/handlers/send_message_handler.py :89-96
当前：
```python
plan_result = self.semantic_router_service.execute_plan(
    question=command.content,
    viewer_roles=[],
)
```
目标：增加 `runtime_mode="official"` kwarg（其余不动）。

【兜底语义事实源 — 已正确，本期只确认/加注释】
- preview_service `_blocked_runtime_route`（:763-802）：无 active 快照时返回 `route_type="blocked"`、`reason=runtime_manifest.error_code or "semantic_runtime_not_ready"`、`execution_results=[]`。
- execute_plan（:582）经 plan()（:518 `"route": route`）把该 route 原样放到顶层 `route` 字段；故 `plan_result["route"]["route_type"]=="blocked"`、`plan_result["route"]["reason"]=="semantic_runtime_not_ready"`、`execution_results==[]`。
- handler `_build_blocked_route_message`（:168-184）：读 `route.get("route_type")=="blocked"` ✓ 且 `reason` 含 "runtime"/"not_ready" → 命中 `("runtime","not_ready","manifest","运行清单","未就绪")` 分支 → 回 "语义运行时尚未就绪，暂时无法回答..."。**这条路径无需改行为**。
- "未命中已发布业务语义"（official 未命中，preview_service :174）：reason 以 "未命中" 开头 → `_build_blocked_route_message` 返回 None → handler 走 legacy `_handle_via_legacy_llm`，dataset_id is None 时回 "未能在已发布的语义资产中找到可回答该问题的口径..."（:298-302，诚实兜底）✓。

【D2 改点】app/interfaces/api/v1/semantic/cubes.py :23-36 `list_cubes`
当前从 `_get_cube_listing_service().list_cubes_with_derivatives()`（registry/YAML）出。
目标：cube 列表从 active manifest 出，与 grounding 同源。

ctx 已注入 `runtime_snapshot_service`（app/interfaces/api/v1/semantic/_context.py :37、:335 透传到 register）。在 cubes.py 经 `ctx.runtime_snapshot_service` 取用。
RuntimeSnapshotService.get_active_manifest(namespace="default")（runtime_snapshot_service.py :21）：
- 有 active → 返回 manifest dict（含 `ok: True`、`asset_manifest_json` 等）；
- 无 active → `{"ok": False, "error_code": "semantic_runtime_not_ready"}`。
RuntimeSemanticCatalog.from_manifest(manifest).list_entities("cube")（runtime_manifest_catalog.py :68 / :113）→ list[CubeDefinition]（已发布 cube）。

【集成测试事实源】
- official 路由 + stub runtime 样式参考 tests/unit/application/semantic_router/test_preview_service.py:
  - `_RuntimeSnapshotServiceStub`（:70-75，get_active_manifest 返回固定 dict）；
  - official 出数测试 test_official_runtime_routes_and_compiles_from_snapshot_manifest_without_yaml（:396-512，含构造含答题 ontology/metric aliases 的 manifest 并经 official route 命中）；
  - 无快照 blocked 测试 test_official_runtime_requires_active_sql_snapshot（:230-275，stub 返 `{"ok": False, "error_code": "semantic_runtime_not_ready"}` → reason=="semantic_runtime_not_ready"）。
- 后端测试命令：`PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider <path>`。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D1 — send_message_handler 调 execute_plan 传 runtime_mode="official"（转绿 08-01 RED）+ 确认兜底语义</name>
  <files>app/application/conversation/handlers/send_message_handler.py</files>
  <read_first>
    - app/application/conversation/handlers/send_message_handler.py :89-96（_handle_via_semantic_router 调用点，D1 唯一改点）、:168-184（_build_blocked_route_message 兜底语义，本期只确认/加注释）、:295-302（legacy dataset_id is None 诚实兜底）
    - app/application/semantic_router/preview_service.py :582-643（execute_plan 接受 runtime_mode）、:763-802（_blocked_runtime_route：无快照 reason=semantic_runtime_not_ready）
    - tests/unit/application/conversation/test_send_message_handler.py（08-01 新增的 test_semantic_router_called_with_official_runtime_mode = RED，本任务后必须转 GREEN；其余 semantic router 测试不得回归）
  </read_first>
  <action>
    1. 在 `_handle_via_semantic_router`（send_message_handler.py :93-96）的 `execute_plan(...)` 调用增加 kwarg：
       ```python
       plan_result = self.semantic_router_service.execute_plan(
           question=command.content,
           viewer_roles=[],
           runtime_mode="official",
       )
       ```
       这是 D1 的核心一行。**不改其它参数、不改方法签名、不动 legacy/agent 路径。**
    2. 在该调用上方加一行注释说明切 official 的语义来源（引 CONTEXT D1 / `semantic-binding-and-rls.md` §1.4），例如：`# D1: 全局问数读 active manifest（runtime official），只消费已发布 cube；无快照→诚实未就绪兜底。`
    3. **确认兜底语义（不扩行为）**：核对 `_build_blocked_route_message`（:168-184）对 `reason=="semantic_runtime_not_ready"` 走 "语义运行时尚未就绪" 文案分支、对 "未命中..." 返回 None 交 legacy。若注释不清，仅补注释说明这两条 official 兜底路径，不改判定逻辑。
    4. 不要改 grounding 匹配、不要激活 intent_extraction、不要碰编译器。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - 08-01 的 RED 转 GREEN：`PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/conversation/test_send_message_handler.py::TestSendMessageHandlerAgent::test_semantic_router_called_with_official_runtime_mode` **PASS**。
    - 整个 test_send_message_handler.py 全绿（无回归）。
    - `grep -n 'runtime_mode="official"' app/application/conversation/handlers/send_message_handler.py` 命中且仅在 `_handle_via_semantic_router` 内。
    - 改动最小：`git diff app/application/conversation/handlers/send_message_handler.py` 仅含该 kwarg + 注释，不含 grounding/intent/编译器改动。
  </acceptance_criteria>
  <done>handler 传 runtime_mode="official"，08-01 RED 转 GREEN，兜底两条路径语义经确认正确，无回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 集成测试 — official 出数（stub runtime）+ 无快照诚实兜底 + comment 不命中边界</name>
  <files>tests/integration/semantic/test_datachat_official_consume.py</files>
  <read_first>
    - tests/unit/application/semantic_router/test_preview_service.py :70-75（_RuntimeSnapshotServiceStub）、:230-275（无快照 blocked → reason=semantic_runtime_not_ready）、:396-512（含答题 ontology/metric aliases 的 manifest + official 命中出数样式）
    - app/application/conversation/handlers/send_message_handler.py :89-166（official 成功→ai_message + execution_results）、:168-184（blocked 兜底）、:295-302（legacy 诚实兜底）
    - tests/integration/semantic/test_student_comment_runtime_assets.py :1-40（集成测试 import/fixture 组织样式，同目录落点）
  </read_first>
  <behavior>
    - Test A（official 出数）：构造含答题 ontology（metric `student_total_count` + aliases 对得上"学生答题统计 总数"）的 active manifest（stub get_active_manifest 返该 manifest，ok=True），用 stub runtime（execute 返固定 1 行、row_count 固定值如 39_890_000）。经 SendMessageHandler（注入真实 SemanticRouterPreviewService + stub runtime + stub snapshot service）/official 问"学生答题统计 总数" → 断言 ai_message source=='semantic'、route_type∈{cube,hybrid}、有非空 execution_results、ai_message 不是 legacy「未能找到口径」文案。
    - Test B（无快照诚实兜底）：stub get_active_manifest 返 `{"ok": False, "error_code": "semantic_runtime_not_ready"}`。经 handler/official 问任意问法 → 断言回复含「语义运行时尚未就绪」（_build_blocked_route_message 文案），且 source=='semantic'、不 500、不落「未能找到口径」。
    - Test C（comment 不命中 = 预期方向 D3）：用 Test A 的答题 manifest（不含 comment）问"统计学生评论数" → 断言不出数（route blocked「未命中」或落 legacy 诚实兜底「未能找到口径」），坐实 YAML-only comment 切 official 后不再被命中是既定方向；**断言不为保 comment 做并集**。
  </behavior>
  <action>
    新建 `tests/integration/semantic/test_datachat_official_consume.py`，用 stub 隔离（不实连 MaxCompute、不实连 DB）：

    1. **复用 preview_service 测试里已验证的 manifest 构造法**：照 test_official_runtime_routes_and_compiles_from_snapshot_manifest_without_yaml（:396-512）构造含答题 cube + metric `student_total_count`（aliases 覆盖"学生答题统计/总数"）的 `runtime_snapshot` manifest dict；`_RuntimeSnapshotServiceStub(runtime_snapshot)` 提供 `get_active_manifest`。
    2. **stub runtime_service**：实现最小 `execute(...)` 返 `{"status": "executed", "target_type": "sql", "execution_request": {"sql_query": "SELECT ..."}, "result": {"columns": [...], "data": [{"student_total_count": 39_890_000}], "row_count": 1}, "traceability": {...}}`（固定行数，不实连）。
    3. 用真实 `SemanticRouterPreviewService`（注入 yaml/空 repo + mapper/compiler preview + `runtime_service=stub` + `runtime_snapshot_service=_RuntimeSnapshotServiceStub(...)`）作为 `semantic_router_service`，注入 `SendMessageHandler`（conversation/message repo 用内存 stub 或 MagicMock，dataset_id=None 走全局问数；llm_service=MagicMock 不应被调用）。
    4. 写 Test A / B / C 三个函数，断言见 <behavior>。Test A 额外 `assert llm_service.generate_sql.assert_not_called()` 或等价（确认未退 legacy）。Test B 复用 _RuntimeSnapshotServiceStub 返 `{"ok": False, "error_code": "semantic_runtime_not_ready"}`。
    5. **不实连 MaxCompute、不跑真实编译器全链**（stub runtime 截断在 execute 边界）；问法严格用"学生答题统计 总数"（CONTEXT 实测命中 student_total_count）。

    若 SendMessageHandler 直接组装真实 SemanticRouterPreviewService 成本过高，可退一步：在 handler 层注入一个"薄 semantic_router_service"（其 execute_plan 内部转调真实 SemanticRouterPreviewService.execute_plan(runtime_mode 透传)），但 **必须真实经过 preview_service 的 official 分支**（不得直接 mock execute_plan 返回值——那会退化成 08-01 的契约测试而非出数集成测试）。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/integration/semantic/test_datachat_official_consume.py</automated>
  </verify>
  <acceptance_criteria>
    - 三个测试全 PASS。
    - `grep -n 'student_total_count\|学生答题统计 总数' tests/integration/semantic/test_datachat_official_consume.py` 命中（出数问法）。
    - `grep -n 'semantic_runtime_not_ready\|语义运行时尚未就绪' tests/integration/semantic/test_datachat_official_consume.py` 命中（无快照兜底）。
    - `grep -n 'MaxCompute\|pyodps\|real_connect' tests/integration/semantic/test_datachat_official_consume.py` 无命中（确认未实连）。
    - Test A 断言含 route_type∈{cube,hybrid} 且 execution_results 非空且回复非「未能找到口径」。
  </acceptance_criteria>
  <done>official 出数（stub）、无快照诚实兜底、comment 不命中三条边界均被集成测试坐实，全程不实连 MaxCompute。</done>
</task>

<task type="auto">
  <name>Task 3: D2 — GET /semantic/cubes discovery 从 active manifest 出（与 grounding 同源）</name>
  <files>app/interfaces/api/v1/semantic/cubes.py</files>
  <read_first>
    - app/interfaces/api/v1/semantic/cubes.py :12-36（register_cube_routes + list_cubes 当前从 _get_cube_listing_service 出）
    - app/interfaces/api/v1/semantic/_context.py :37、:295-335（ctx 注入 runtime_snapshot_service 与 _get_cube_listing_service；确认 cubes.py 可经 ctx.runtime_snapshot_service 取用）
    - app/application/semantic/runtime_snapshot_service.py :21-25（get_active_manifest：有 active 返 manifest，无 active 返 {"ok": False, "error_code": "semantic_runtime_not_ready"}）
    - app/application/semantic/runtime_manifest_catalog.py :68-126（from_manifest + list_entities("cube") 给出已发布 cube 列表）
    - tests/integration/semantic/test_cube_list_derivatives.py（GET /semantic/cubes 现有集成测试样式，避免回归 _build_list_payload 结构）
  </read_first>
  <action>
    让 `GET /semantic/cubes`（cubes.py list_cubes :23-36）的 cube 列表来源切到 active manifest，与 grounding 命中范围一致（D2）：

    1. 在 `register_cube_routes` 顶部从 ctx 取 `runtime_snapshot_service = getattr(ctx, "runtime_snapshot_service", None)`。
    2. 在 `list_cubes` 内：优先经 `runtime_snapshot_service.get_active_manifest("default")` 取 manifest；若 `manifest.get("ok")` 为 True → 用 `RuntimeSemanticCatalog.from_manifest(manifest).list_entities("cube")` 得到已发布 cube，转成与现有 `_build_list_payload(filtered, "cubes")` 兼容的 dict 列表（至少含 name/title/description/domain_name 字段，缺省置空，沿用现有排序/keyword 过滤）。
    3. **边界**：`runtime_snapshot_service` 为 None 或 `manifest.ok` 为 False（无 active 快照）→ 回落到现有 `_get_cube_listing_service().list_cubes_with_derivatives()` / `semantic_service.list_cubes()` 逻辑（保持当前行为，不 500）。即 manifest 优先、registry 兜底。
    4. **收窄护栏**：若把 manifest cube 映射成 list payload 牵连面过大（字段缺失导致前端列表/详情链路断裂），按 CONTEXT D2「可在 plan 里拆为次要任务或收窄」——此时仅在 list payload 标注/附带"来自 active manifest"的来源标记并保持 registry 为主，把"完全切 manifest"降级为 follow-up（记入 SUMMARY），**主闭环（D1）不受影响**。决策写进 SUMMARY。
    5. 不改其它 cube 端点（describe/draft/create/...），不改 grounding。
  </action>
  <verify>
    <automated>PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/integration/semantic/test_cube_list_derivatives.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'runtime_snapshot_service\|get_active_manifest\|RuntimeSemanticCatalog' app/interfaces/api/v1/semantic/cubes.py` 命中（discovery 已接 manifest 源）。
    - 现有 `tests/integration/semantic/test_cube_list_derivatives.py` 全绿（无 active 快照下回落不回归 _build_list_payload 结构）。
    - 后端语义路由相关单测不回归：`PYTHONPATH=. /Users/xuan/miniconda3/bin/python -m pytest --no-cov -q -p no:cacheprovider tests/unit/application/semantic_router/test_preview_service.py` 全绿。
    - 若采用收窄方案，SUMMARY 明确记录"D2 降级为 follow-up + 原因"。
  </acceptance_criteria>
  <done>discovery cube 列表 manifest 优先、registry 兜底，与 grounding 同源；无快照不 500；现有 cube 列表测试不回归。</done>
</task>

</tasks>

<verification>
- 08-01 RED → GREEN（Task 1）。
- official 出数 + 无快照诚实兜底 + comment 不命中三条边界经集成测试坐实（Task 2，不实连 MaxCompute）。
- discovery 同源（Task 3，manifest 优先 + registry 兜底，无快照不 500）。
- 全程未改 grounding 匹配算法、未激活 LLM 意图抽取、未动编译器分区保护（git diff 核对）。
- comment / YAML-only cube 不命中是预期方向（D3），未做 YAML 并集。
</verification>

<success_criteria>
- DataChat 全局问数读 active manifest（runtime_mode="official"），自助建模发布的 cube 可被问到并出数（stub 坐实形状）。
- 「能问什么」discovery 与 grounding 同源。
- 无 active 快照诚实回「运行时尚未就绪」，不 500、不伪造。
- Phase 8 成功标准（ROADMAP :103-106）全部有测试覆盖。
</success_criteria>

<notes>
**D4 运维桥接 + 真实闭环验证 = 部署/验证动作，非本 plan 代码任务，由执行者（用户）在运维阶段亲自做：**
1. 重启 docker 加载 Phase 7+8 代码。
2. 跑 `rebuild_active_baseline(namespace="default", asset_keys=[("cube","dws_study_student_answer_kb_stat_di"),("cube","dim_app_dify_app_info_df")], actor="internal:local:admin", idempotency_key=...)`（app/application/semantic/semantic_release_service.py :341）→ active manifest 变多 cube（坐实 Phase 7 累积在线生效）。
3. **真实闭环判定点**：经真实 DataChat（或 SendMessageHandler/official）问"学生答题统计 总数" → 断言走 semantic 主链、route_type=cube、有 query_result/真实出数（真打 MaxCompute 或经 runtime tool 已证 3989 万）。这是整条命脉打通的判定点。
4. 存量回填前提（见 STATE.md Quick Tasks）：旧表（如 dws_study_student_answer_kb_stat_di）需部署后重新同步才带上 partitions；schema 快照分区透传修复只对新写入生效。

**Deferred（不在本期 / Phase 9 / 另期）**：不改 grounding 匹配算法、不激活 LLM 意图抽取（召回准度命门，[[qa-scope-mismatch-global-ask]]）；不回写/重发 YAML-only cube（comment/answer_records）；不动编译器分区保护；文档对齐 + 全平台 verify → Phase 9。
</notes>

<output>
完成后创建 `.planning/phases/08-consume-02/08-02-SUMMARY.md`，记录：D1 改点（runtime_mode="official" 行）、兜底两条路径确认结论、D2 落地或降级决策（含原因）、集成测试三条边界结果、D4 运维桥接待办（转交执行者）。
</output>
