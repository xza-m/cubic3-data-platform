# Tasks

- [x] ADR-011 确认 data-platform 与 dw-query-gateway 职责边界。
- [x] 查询工作台 `/api/v1/queries/execute` 收敛到 DataSource Adapter SPI。
- [x] Agent semantic execute 提交到 `dw-query-gateway`。
- [x] Gateway telemetry summary / query runs 保持 data-platform BFF 展示。
- [x] 下线本仓内部查询执行代码、API、worker、测试入口和 Makefile 目标。
- [x] 新增 forward migration 删除旧内部执行表。
- [x] 更新基线文档，避免再次把本仓描述为 all-in-one 查询执行面。
