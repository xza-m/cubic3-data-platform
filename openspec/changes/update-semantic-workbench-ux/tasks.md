## 1. 实施清单
- [x] 1.1 为语义中心定义统一的 workbench 页面编排规范，固定“页头 / 状态条 / 工作区 / Inspector / 主动作”层级
- [x] 1.2 梳理语义中心现有页面首屏 focus，统一 utility copy、状态命名与动作命名
- [x] 1.3 建立实现准备包，明确页面优先级、最小可交付、共享组件边界与验证策略

## 2. Cube 管理页
- [x] 2.1 将 Cube 管理页调整为“问题优先”的工作列表，首屏优先暴露待处理对象
- [x] 2.2 增加搜索、状态、绑定状态、领域、复用度、更新时间等筛选能力，并保留 URL 状态
- [x] 2.3 用表格主视图替代当前对象卡片主视图，保留右侧对象预览面板
- [x] 2.4 在列表中补充领域关联、View 引用、数据源绑定与最近变更等关键信息
- [x] 2.5 拆分 `CubeToolbar`、`CubeTable`、`CubePreviewPanel`，收敛页面入口职责

## 3. Cube Studio
- [x] 3.1 将新建 / 编辑流程拆分为“基础信息 / 来源绑定 / 维度指标 / 语义规则 / 校验预览 / 保存发布”阶段
- [x] 3.2 保留现有步骤轨，但让中间编辑区只突出当前步骤任务，非当前步骤折叠为摘要
- [x] 3.3 前置关键校验，在保存前集中展示阻塞项、提醒项和影响范围
- [x] 3.4 明确与领域建模的边界，在页面内提供回到领域建模的清晰衔接
- [x] 3.5 拆分步骤轨、步骤面板和 Inspector，避免继续堆大文件

## 4. 领域目录
- [x] 4.1 强化左侧目录 rail，补充空目录、草稿积压和发布状态等治理信号
- [x] 4.2 为领域列表增加治理透镜和风险筛选，不再只做普通对象列表
- [x] 4.3 让右侧详情在“目录摘要 / 当前领域摘要”之间切换，支持从目录直接进入建模闭环
- [x] 4.4 保持 `catalog -> domain` 轻量模型，不引入多级树或复杂权限能力

## 5. 领域建模
- [x] 5.1 调整领域建模页首屏权重，确保顶部操作、左库、画布、Inspector 的主次更稳定
- [x] 5.2 优化节点信息层级、选中态、冲突态和连线语义表达
- [x] 5.3 收敛 Join 编辑链路，统一右侧 Inspector 的字段映射、基数、聚合和说明编辑
- [x] 5.4 增强空画布引导、发布前检查与大规模节点场景下的可读性
- [x] 5.5 拆分上下文条、资源库、画布图例和 Inspector 模块

## 6. 开发工具
- [x] 6.1 优化资源树分组、当前对象上下文条和工作区顶部信息
- [x] 6.2 提升 YAML 编辑、编译调试和 Schema 同步三个 Tab 的差异化结构与扫描效率
- [x] 6.3 优化 Domain / Catalog 不支持在线编辑时的空状态文案和跳转动作
- [x] 6.4 拆分资源树、WorkspaceHeader 与空状态组件

## 7. 共享组件与代码组织
- [x] 7.1 提炼语义中心内部共享组件，如工具栏、快筛、问题列表、对象预览面板、上下文条
- [x] 7.2 将 CubeList、CubeStudio、DomainList、DomainCanvas、DevTools 拆分为更小的页面模块
- [x] 7.3 统一语义中心的状态优先级、筛选逻辑和 URL 状态管理工具
- [x] 7.4 将页面级线框与实现准备文档纳入提案，作为开发前置输入

## 8. 验证
- [x] 8.1 为语义中心关键页面补充桌面端与移动端的布局验收项
- [x] 8.2 验证核心链路：Cube 管理 -> 设计 -> 领域建模 -> 发布 -> DevTools 调试
- [x] 8.3 验证共享组件复用后，页面间的命名、状态和交互保持一致
- [x] 8.4 在每个阶段结束后执行对应的单测、视觉回归和 smoke，而不是集中到最后一次性验证

## 9. 评审后收口
- [x] 9.1 将登录页和控制台入口从营销化/卡片化表达收敛为更克制的工作台入口
- [x] 9.2 压缩领域建模页顶部重复条带，减少对画布垂直空间的挤压
- [x] 9.3 收紧 DevTools 的页级上下文层级，并补足 YAML 编辑器加载态
- [x] 9.4 更新语义中心视觉快照基线，修正 `cube-browse` 与 `devtools-browse` 在空环境下的 smoke 夹具
