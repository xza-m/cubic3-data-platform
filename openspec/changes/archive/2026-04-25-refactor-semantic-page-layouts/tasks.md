## 1. 页面职责冻结
- [x] 1.1 为 `Cube 管理 / Cube 设计 / 领域管理 / 领域设计` 写入页面职责、允许动作和禁止动作
- [x] 1.2 清点现有页面中的重复摘要、无效跳转和首屏说明卡
- [x] 1.3 为四个页面定义统一的首屏布局规则和主按钮规则

## 2. Cube 页面重排
- [x] 2.1 将 `Cube 管理` 收敛为“筛选 + 列表 + 预览”
- [x] 2.2 从 `Cube 管理` 中移出快速查询、DSL 编辑和跨工作区主跳转
- [x] 2.3 将 `Cube 设计` 收敛为“基础信息 / 维度 / 指标 + 校验”
- [x] 2.4 校验 `Cube 设计` 中不再承载领域关系和查询验证职责

## 3. 领域页面重排
- [x] 3.1 将 `领域管理` 收敛为“Catalog 树 + 领域列表 + 领域概览”
- [x] 3.2 从 `领域管理` 中移出当前领域大表单、新建草稿大表单和发布阻塞详情卡
- [x] 3.3 将 `领域设计` 收敛为“Cube 库 + 画布 + Inspector + 发布动作”
- [x] 3.4 校验 `领域设计` 中不再暴露目录台账、查询器和 YAML 编辑器

## 4. 跳转与状态契约
- [x] 4.1 删除无效跨页跳转，只保留顺主流程跳转
- [x] 4.2 为四个页面补齐稳定的 `data-testid` 和主操作契约
- [x] 4.3 统一状态条展示规则，避免同一对象摘要重复出现

## 5. 测试闭环
- [x] 5.1 新增组件层测试，覆盖预览面板、Inspector、校验面板和轻量创建弹窗
- [x] 5.2 新增页面交互测试，验证管理页不混入设计能力、设计页不混入管理能力
- [x] 5.3 保留并更新语义主链路 E2E，补齐 `Cube 管理` 浏览主链路
- [x] 5.4 为四个页面建立视觉回归基线
- [x] 5.5 新增聚合校验脚本，覆盖类型检查、单测、关键 E2E 和视觉回归
