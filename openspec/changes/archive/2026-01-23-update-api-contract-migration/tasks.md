## 1. 调研与对齐
- [x] 盘点所有 Dataset 相关接口与页面调用路径（新旧架构）
- [x] 形成接口清单：路径、方法、请求/响应、状态码
- [x] 确认是否存在外部系统依赖旧 `/api/*`

## 2. 契约规范化
- [x] 定义统一 `ApiResponse`、分页结构与错误结构
- [x] 定义 Dataset/Field 对象完整字段与枚举值
- [x] 统一统计接口字段命名与含义

## 3. 后端迁移实现
- [x] datasets/datasources/extraction 等 v1 接口输出字段补齐
- [x] 统一统计接口字段命名
- [x] 旧 `/api/*` 端点：直接下线并返回 410
- [x] 保持 DDD/CQRS/Hexagonal 分层不被破坏

## 4. 前端迁移实现
- [x] API client 全量切换为 `/api/v1`
- [x] Dataset 相关页面字段映射与枚举统一
- [x] 去除兼容层与临时映射逻辑

## 5. 验证与回归
- [x] 关键页面联调：数据集列表/详情/创建/统计
- [x] 契约回归清单（至少覆盖 10 个核心接口）
- [x] 记录兼容性变更与迁移说明
