# frontend/.env.example
#
# Vite 在 build / dev / preview 时会读取 .env / .env.local / .env.<mode>。
# 仅 `VITE_` 前缀的变量会被打进前端 bundle，其余忽略。
#
# 使用方法：
#   cp .env.example .env.local   # 本地开发
#   或在 CI / 生产环境注入对应环境变量

# ── Observability — leave empty in dev to use console + buffer sinks only ────
# 当 VITE_OBS_ENDPOINT 为空时，installObservability() 仅装配 ConsoleSink + BufferSink，
# 不发起任何网络请求；window.__cubic3_obs__ 提供调试句柄。
# 设置后会额外挂一个 HttpSink，POST 到该 URL，body 形如：
#   { "type": "event"|"error", "payload": ObsEvent|ObsError }
VITE_OBS_ENDPOINT=
VITE_OBS_SAMPLE_RATE=1.0
